# Goal

Create a series of agents that are capable of improving over time by feeding them projects.

I want to then use these agents to build software.

# How this repo operates

See `README.md` for the full system. In short:

- Agents live in `.claude/agents/` (orchestrator + flow-manager, product,
  solution-architect, cicd, engineer, ui-designer, tester, documenter, plus the
  `linear` + `jira` board-projection agents). The orchestrator regulates flow only
  and delegates every product/architecture/engineering decision; the flow-manager
  derives flow decisions (what to pull, when to replenish) from the item event-logs —
  it no longer owns a separate queue store.
- **v82 — event-sourced work items.** The **work item is the single source of
  truth**: state is `fold(events)` over an append-only event log kept in per-item
  files (`work/<project>/items/{active,done}/<ID>.md`). Queues, DORA, the
  dependency tree and the Linear/Jira boards are all **derived views**, never
  persisted-and-hand-synced. The pull loop (`/loop-run`) reads the *derived* Ready
  view and pulls the maximal independent set. The sole writer of item state is
  `make wi-append` (edge-checked against the type graph); views are regenerated by
  `make wi-project`; the drift **gate** is `make wi-validate` (invariants by
  construction). One blocking human gate remains — requirement/defect intake
  (`/intake`); infra-bearing deploy auto-approves under an automated policy
  assurance (§F5/§F5a, EXP-093), and the only residual human touch is a genuinely
  irreversible prod-DATA op (§0b). Build contract: `process/machinery/CONTRACT.md`;
  cross-agent rules: `process/process-current.md` **STAGE F**.
  Rationale/diagrams/worked-retro: `Version2-design/`. Use `/flow-status` to see
  the derived queues, buffers, and time thieves.
- Drive work with the slash commands in `.claude/commands/`. The drivers are
  `/intake`, `/loop-run`, `/flow-status`. Full set (13): `/intake`, `/loop-run`,
  `/flow-status` (the drivers); `/requirement-new`, `/slice-next` (JIT
  replenishment), `/retro`, `/defect`, `/refactor-process` (planning &
  improvement); `/project-new`, `/project-switch`, `/project-list`,
  `/project-stop`, `/aws-profile` (project & config).
- `/process` is PERSISTENT agent self-state (process, principles, DORA, learned
  failures) and must never reference a specific project. `/work` is RESETTABLE
  project output.
- Before crawling files, read the `process-framework` skill — it says what to
  load for each task and keeps context small. Metrics are **derived**, not
  recorded by hand: `make wi-project` folds the item event-logs into
  `work/<project>/views/stats.json` (the 4 DORA metrics + gross-lead-time
  contribution by owner + quality + recovery). See the `work-items` skill. The old
  `process/dora/ledger*.csv` is a frozen QueueApproach archive (read-only, not
  extended); the `dora-ledger` skill is now a stub pointing at `work-items`.

# Working-directory conventions

**All commands run from the project root** (the directory containing this file).

- Use `npm --prefix work/<project>/src/app run <cmd>` instead of `cd ... && npm run <cmd>`
- Run the work-item machinery via its **cross-platform launcher** — `sh .claude/skills/work-items/scripts/work-items <subcommand>` (or the `make wi-*` targets: `wi-append`, `wi-project`, `wi-validate`) from project root only; the path is root-relative. NEVER call bare `python3 …`: on Windows `python`/`python3` are Microsoft Store stubs that fail silently — the launcher resolves the real interpreter (real `python3` on macOS, `uv`-provided on Windows) and caches it machine-locally. See the `work-items` skill. (The retired `dora` launcher only reads the frozen CSV archive.)
- The committed allowlist is in `.claude/settings.json`; add novel patterns there, not in `.local.json`
- **Work items are project output, one file per item:** each item's whole truth
  (definition + append-only event log + edges) is a single file under
  `work/<project>/items/active/<ID>.md`, moving verbatim to
  `work/<project>/items/done/<ID>.md` on completion — NEVER a bare root-level
  `items/`. The old `slices/<nnn>-<slug>/*.md` split (slice/acceptance/route/result)
  is retired: a slice is just an item of `type: slice`, and use-cases/defects are
  its children by `parents:` edge. Derived views (queues, state, tree, stats) live
  under `work/<project>/views/` and are regenerated by `make wi-project` — never
  hand-edited. A bare `items/` or `slices/` at the system root leaks one project's
  output into the system root and is a principle failure. (Note:
  `/process/improvement-slices/` is a separate, persistent process concept and is
  unrelated.)
- **Two repositories — project output vs the agent system (v50).** Each
  `work/<project>/` is its **own independent git repo** (so a project can be
  lifted out and live standalone). Commit **project output** inside it:
  `git -C work/<project> add <paths> && git -C work/<project> commit -m "…"`.
  Commit **agent-structure / process** changes (`.claude/`, `process/` incl.
  `process/machinery/`, `CLAUDE.md`, `README.md`) in THIS parent repo. The parent `.gitignore`s
  `/work/*/`, so it never tracks project contents; `work/README.md` and
  `work/_TEMPLATE/` stay in the parent as agent-system state. Never mix the two
  in one commit (process §14).
- **Multi-instance operating model (process §0a).** More than one Claude instance
  may run against this shared parent repo at once (different machines, different
  projects). Two rules keep them from clobbering each other: (1) **`work/ACTIVE`
  is machine-local and gitignored** — each instance owns its own active-project
  pointer; never commit it. (2) **The write substrate is per-item files** —
  `work/<project>/items/{active,done}/<ID>.md`, one file per item, so concurrent
  writes land in **disjoint files** (this is what removed the shared-index race
  that used to force worktree isolation, EXP-097; the old ledger-sharding is moot —
  `process/dora/ledger*.csv` is a frozen archive). Each instance works on its
  **own branch** (`instance/<project>`) and reconciles to `main` continuously
  (never batched — reconcile latency is a gross-lead-time cost the retro
  minimises). Run the machinery only via the cross-platform launcher
  (`sh .claude/skills/work-items/scripts/work-items`), never bare `python3`.