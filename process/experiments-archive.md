# Experiment archive (terse index)

One line per experiment that reached a terminal state (`integrated` / `retired` /
`reworked→`) and was pruned from `experiments.md` (v45 §25a). The behaviour now
lives in the owning agent file; the full row is in git. Append-only, terse —
this is the index of what we've learned and folded in, not a working registry.

- EXP-001 — real-browser-not-node probe + browser-transport spec; 0 browser-only causes reaching prod — integrated 7db8d99 (engineer.md/tester.md)
- EXP-002 — local standability: build-phase browser tests against a stood-up system — integrated 7db8d99 (engineer.md, principles/02)
- EXP-003 — trunk-CD prereq-before-first-push corollary — reworked→EXP-005 (folded into the change-impact model)
- EXP-004 — failure semantics 5xx/4xx ownership + backoff taxonomy — integrated 7db8d99 (engineer/tester/product/solution-architect)
- EXP-005 — shared change-impact model (mermaid deps, read-before-build/test, @covers) — integrated 7db8d99 (engineer/tester/solution-architect/product)
- EXP-006 — use-case flags = two-phase rollout lifecycle (flags not branches) — integrated 7db8d99 (engineer.md/cicd.md)
- EXP-007 — walking-skeleton probe on new platform mechanisms — integrated 7db8d99 (engineer.md, make ws-skeleton)
- EXP-009 — budget-aware validation on rate-limited surfaces (serialise connection-consuming specs) — integrated at s007 retro (tester.md)
- EXP-010 — deployable-UC done = deployed + prod probe green; deploy order by concurrency group — integrated at s007 retro (engineer.md §11a)
- EXP-032 — edit files with Edit/Write, record ledger with dora.py record, never shell redirection — validated 2/2 (s001–s004 run + the v44/v45 retros: 0 file-edit prompts) — integrated v45 (process §15 + orchestrator.md, plain practice)
- EXP-012 — gate-4 go/no-go at route completion, before the deploy-bearing wave — validated 4/4 (s008/s005-h3/s009/s014, 0 ungated infra deploys) — integrated v48 (process §9a, plain practice)
- EXP-014 — canonical kebab node-id === @covers tag, no fuzzy-match — validated on arrival (IMP-007 caught real drift) — integrated v48 (process §12a.5)
- EXP-015 — multi-party modelling: state machine per party + sync-point table — validated 2/2 (s009, s014 chat) — integrated v48 (§12b + product/engineer/tester defs)
- EXP-017 — defect lifecycle intake→reproduce→prioritise→fix-as-spec→gap-closing retro — validated 3/3 — integrated v48 (/defect + §6, plain practice)
- EXP-020 — push→PULL flow control (continuous loop, costed queues, JIT replenish) — validated 2/2 — integrated v48 (§F2/§F3 are the durable home; citations stripped)
- EXP-030 — continuous background loop + parallel replenishment + enqueue-to-empty wake — validated 2/2 — integrated v48 (§F9; refined by EXP-031 rework)
- EXP-034 — proactive replenishment: product decomposes ahead in the same parallel batch as the build wave — validated 2/2 — integrated v48 (§F3 + orchestrator/flow-manager defs)
- EXP-039 — model tiering by judgment density + in-session per-call override bridge + availability re-tier — validated-on-opus (s014/s015/s018 CFR + SLC-002/003 mechanism) — integrated v57 (process §7a, plain assignment; fable scaffolding closed, model retired from access)
- EXP-046 — model-tier-change scoring quarantine (model-confounded experiments can't validate on a DORA move alone) — validated 1+/2 (v49 fable→opus window opened/held/closed cleanly at SLC-003) — integrated v57 (process §7a, plain practice)
- EXP-049 — no-git-push: agents commit LOCAL trunk only, pushing is human-gated — validated 3/3 (s001/SLC-002/SLC-003 OagEventSource, 0 push attempts) — integrated v57 (process §14, plain practice)
- EXP-016 — visual-structural correctness: assert layout geometry + no-reflow invariant on overlay surfaces — validated 2/2+ (board geo, s009 table, observatory GEO guards, DEFECT-006 no-reflow) — integrated v58 (ui-designer.md/tester.md, plain practice)
- EXP-026 — parallel dispatch by maximal independent set (DAG + claimed-path registry), §40 flags — validated 2/2 (s001 par_eff 1.00; s013/14/15 wave 0.86–0.89) — integrated v58 (process §F6 + flow-manager.md; mine-only-blob isolation folded into engineer.md)
- EXP-031 — keep-trucking: non-gate boundaries are autonomous; ending the turn IS the stop; dispatch next work in the same turn — validated 2/2 (s014 delivery run, 0 boundary stop-asks) — integrated v58 (process §F9.4 + orchestrator.md, plain practice)
- EXP-041 — atomic pull: queue-remove + items.csv in-flight + ledger rows keyed by work-item id in one act; sweeps reconcile not originate; coherence_warning detector — validated 2/2 (UC-S013-4, UC-S018-1; detector 2 true-positives) — integrated v58 (engineer.md/tester.md/flow-manager.md, plain practice)
- EXP-043 — repository split: each work/<project>/ its own git repo; project output inside, agent/process in parent; parent gitignores /work/*/ — validated 2/2 (s001 + mega-session, 0 cross-boundary leaks) — integrated v58 (process §14 + CLAUDE.md, plain practice)
- EXP-044 — CFR validity: separate deploy_failure (CFR numerator) from defect_intake (excluded, reported as arrival rate); classify retroactively by DEFECT- ref — validated 2/2 (cumulative 43%→20% measurement correction; CFR moved on real deploy break) — integrated v58 (process §3 + dora.py classifier, plain practice)
- EXP-045 — windowed DORA: last-12-deploys table beside the cumulative medians so improvement is visible inside a scoring horizon — validated 2/2 (window caught movement the history-dominated median hid, both directions) — integrated v58 (process §4 + dora.py cmd_compute, plain practice)
- EXP-052 — cheap idempotency-extension UC class: a UC extending an existing dedup-before-diff guard keyed on a stable key costs ≈ test-authoring only — validated 2/2 (UC-11 + UC-16 both zero production code) — integrated v58 (product.md cost-class, plain practice)
- EXP-023 — two-gate model: keep only requirement/defect INTAKE + infra-bearing DEPLOY; each removed gate (vision/slice/arch) replaced by a named assurance — validated 3/3 (SLC-004 infra-deploy gate caught DEFECT-OAG-001; the 3 removed gates 0 regression) — baselined v59 (process §F5, plain practice)
- EXP-055 — token-efficiency retro review: every retro estimates token consumption + dominant consumers and routes the single highest-leverage reduction, scored on DORA-value-per-token (never minimum tokens) — validated 2/2 (SLC-004 chose deploy pre-flight; mega-session chose build-time defect pins) — baselined v59 (process §26, plain practice)

- EXP-008 — changes-as-experiments lifecycle (scoring/retirement/null-hypothesis) — core process §25a/§26; registry maintained + acted on across every retro — integrated v60
- EXP-011 — validated-experiment INTEGRATION + prune-to-archive — core process §25a; multiple integration passes (7db8d99, v45, v48, v60) — integrated v60
- EXP-013 — `make impacted-tests` model-diff→impacted specs — tester planning -33% (12 vs 18min, s009) — integrated v60 (tester.md)
- EXP-018 — experiment applies-to declared + listed at work-selection — core process §25a/§10 — integrated v60
- EXP-021 — work-item model REQ/CHK/SLC/UC/DEF + per-item DORA from ledger — core process §F1 — integrated v60
- EXP-024 — dedicated flow-manager owns queues/costing/buffers/dispatch — core agent roster + STAGE F — integrated v60
- EXP-028 — time-thief attribution as primary retro input — core §F4/§26 (v60 retro used it: constraint=orchestrator) — integrated v60
- EXP-029 — retro cadence (slice-completion + event-triggered, tunable) — core process §F8 — integrated v60
- EXP-033 — validate against live data + figure-legibility/human-meaningfulness checklist — strong pattern (DEFECT-001/002/004/005/007/008) — integrated v60 (ui-designer.md/tester.md)
- EXP-035 — derived now-state = RECENCY-ONLY predicate (4-turn craft lesson: simplest predicate wins) — superseded by EXP-048 single-source for new projects — integrated v60 (engineer.md/product.md)
- EXP-037 — keep ledger/items.csv/queues coherent per-UC — superseded by EXP-048 for new projects; forward principle carried by EXP-047 — integrated v60
- EXP-038 — retro scriptifies the cycle's most-repeated MECHANICAL op (one allowlisted command, not N Read+Edit) — validated 2/2 (SLC-003) — integrated v60 (process §26 + dora.py)
- EXP-080 — verify CI/deploy/validation status at the SOURCE (gh run conclusion + job/step, deployed X-Service-Version, actual metric value); orchestrator verifies before closing a slice/gate; flow-manager never projects an in-flight item terminal — validated 2/2 (SLC-014 caught 3 spec-not-code false-fails; SLC-023 cited verified deploy sha + caught the ci-watch wrong-repo read) — integrated v67 (engineer.md/tester.md/orchestrator.md/flow-manager.md). Extended to the deploy environment by EXP-082.

> **v68 GRADUATE-TO-SKILL pass (rule lifecycle, process-framework skill).** The
> following PROVEN, STABLE, CROSS-AGENT rules graduated OUT of the active /process
> into skills (the durable methodology layer) + their owning agent files, and
> their EXP rows were pruned from experiments.md to keep /process lean:
- EXP-025 — defect-as-high-value-intake (defects re-enter via /intake, costed, pre-empt Ready) — validated 2/2 (DEFECT-001, DEFECT-OAG-025) — graduated v68 to plain practice (process §F5/intake.md/defect already core)
- EXP-062 — migration done-condition INCLUDES converting+proving the CI/CD pipeline green in-slice (never deferred) — validated 1/1 PROVEN GREEN (infra.yml SST run 27958502999) — graduated v68 to cicd.md plain practice (process §19a)
- EXP-064 — observability validated by a span/trace ARRIVING (read-back), not "collector started" — graduated v68 → delivery-principles "validate the fitness function" (tester.md mechanics). Founding DEFECT-OAG-002.
- EXP-065 — externally-published contract validated against a schema DERIVED FROM the frozen spec, not the implementation — graduated v68 → delivery-principles "oracle is ground truth". Founding DEFECT-OAG-003.
- EXP-066 — external-source semantic assumptions validated against the captured corpus before encoding (assumption register + corpus-adversarial test) — graduated v68 → delivery-principles "oracle is ground truth". Founding DEFECT-OAG-004.
- EXP-068 — a deployed processing component validated by it still DOING ITS PRIMARY JOB after deploy (core-function smoke), not "deployed + units pass" — graduated v68 → delivery-principles "gate asserts the outcome". Founding DEFECT-OAG-008 (24h projector outage).
- EXP-072 — consumer-facing READ surface ships a stated p95 latency budget + round-trip check (flag serial dependent-less queries / scan-instead-of-counter) — graduated v68 → delivery-principles "gate asserts the outcome". Founding DEFECT-OAG-010.
- EXP-073 — fixtures for an unowned data shape CAPTURED FROM THE REAL SOURCE, never hand-authored to match the code — graduated v68 → delivery-principles "oracle is ground truth" (engineer.md §2). Founding DEFECT-OAG-016.
- EXP-074 — a UI surface validated only when the RENDER is observed showing real content + key-field correctness; missing tooling is a blocker to wire — graduated v68 → delivery-principles "gate asserts the outcome" (tester.md). Founding DEFECT-OAG-016/018.
- EXP-083 — the §F8 auto-retro is a MECHANICAL gate (`dora.py retro-debt` exits non-zero = RETRO DUE; loop may not advance; never offered to the human) — validated 2/2 (SLC-026/027/028 routine 3/3 fire + v71 SLC-029/DEFECT-OAG-032 incident fire, both correct, loop never advanced past, never offered) — integrated v71 as plain loop machinery (dora.py retro-debt + make retro-debt + loop-run.md step 7 + §F8). Cadence refinement continues under EXP-085. Founding: v68 8-deep retro-debt from discretionary enforcement.
- EXP-081 — e2e/integration/contract specs assert the INVARIANT, not an incidental live-data condition (branch on data-state / derive from per-entity truth) — graduated v68 → delivery-principles "spec asserts the invariant" (tester.md/engineer.md). Founding SLC-014 batch (3 false-fails).

> **v82 event-sourced cutover prune.** The event-sourced work-item model (F0; state=fold(events) in per-item files, sole writer `wi-append`, all views/metrics DERIVED via `wi-project`, drift gate `wi-validate`; DORA CSV ledger FROZEN) obsoleted the mechanisms these rows tested. Pruned from experiments.md:
- EXP-047 — single-source-of-truth for derived now-state (N stores → N−1 must be projections) — VALIDATED by construction under v82: fold-state makes multi-store drift unrepresentable (one store per fact, all else derived on read).
- EXP-048 — SINGLE SOURCE OF TRUTH refactor (ledger the one writer; item-state + queue-membership derived) — VALIDATED by construction under v82: the coherence-defect family it targeted cannot be represented (state=fold(events); queues/state/tree are pure projections, invariant I2 enforced by wi-validate).
- EXP-053 — `dora.py flow` par_eff honours the §F6 shared-file seam (cap theoretical-max at the §40-achievable set) — SUPERSEDED by F0 (mechanism retired): par_eff via the dora.py flow path is gone; flow metrics now come from wi-project over event timestamps.
- EXP-086 — normaliser-handler split so behaviourally-independent UCs claim disjoint files and par_eff rises — SUPERSEDED by F0 (mechanism retired): the par_eff-via-dora.py flow signal this row moved no longer exists; the handler-split refactor itself continues as IMP-011 build work.
- EXP-098 — ready `min_items` 2→3 policy.csv tune to stop head-starvation — SUPERSEDED by F0 (mechanism retired): the queues/policy.csv min_items tuning mechanism is retired; queues are derived from state via queue_map, not policy.csv buffers.


## v88 backfill-triage — 33 rows retired (2026-07-12, ROC)

Applied process-current.md §25a (v88): HARD WIP cap of 8 active, a-fix-is-not-an-experiment, 3-strikes score-or-kill, archive-with-outcome mandatory. The active registry was cut 41 → 8. Each entry below carries its disposition + one-line result and the FULL original row text verbatim so nothing is lost.

### ADOPTED — behaviour sound + load-bearing + in force; row pruned, behaviour kept as plain agent/process practice

- **EXP-094 — ADOPTED.** new-AWS-service deploy grant = `<service>:*` region-scoped (not enumerated) + bootstrap version-prune; in force in cicd.md + bootstrap-deploy-role.sh.
  - Original row: | EXP-094 | v79 (2026-07-05) | cicd.md (new-AWS-service deploy grants = `<service>:*` region-scoped) + bootstrap-deploy-role.sh (version-prune) + infra/policies allowlist note | **Problem:** SLC-039 dev deploy failed FOUR times (CD runs 28734040102 → 28734267920 → 28734406141 → 28734554471-green) on a SUCCESSION of under-scoped deploy-role IAM permissions for a NEW service (API Gateway v1) added to the deploy role: (1) `apigateway:PUT` on `/tags/*`, (2) `iam:UpdateRoleDescription`, (3) `apigateway:UpdateRestApiPolicy`. Each was fixed by a REACTIVE one-verb cicd patch (15f5e6b, 047dcb2) then finally by switching to `apigateway:*` region-scoped (8a425ea). Root cause: the deploy-policy widening **ENUMERATED actions** and structurally could NOT be complete — API GW v1 has both HTTP-verb actions AND NAMED actions (`UpdateRestApiPolicy` is not an HTTP verb), so the enumeration missed them one at a time → 3 CFR hits + inflated deploy MTTR. Second occurrence of the verb-complete gap (the founding leg noted "under-scoped twice"). SEPARATELY, `bootstrap-deploy-role.sh` hit AWS's 5-managed-policy-version cap (no auto-prune) → orchestrator pruned by hand (tooling gap). Every failure was contained in DEV; prod untouched (dev-first held). **Solution:** for a NEW AWS service added to a deploy role, grant `<service>:*` **region-scoped** (matching the established `ec2:*`/`lambda:*` precedent already in the policy) rather than enumerate verbs — the enumeration is the defect, not the fix; a region-scoped service wildcard is the least-privilege unit for a deploy role that fully owns that service's resources. (Alternative considered: a CloudTrail-derived verb-complete template — rejected as heavier for the same outcome; the service-wildcard matches the precedent and is verb-complete by construction.) PLUS add version-pruning to `bootstrap-deploy-role.sh` so it prunes the oldest non-default managed-policy version before hitting the 5-version cap. | CFR (verb-incomplete IAM for a new-service deploy grant) + deploy MTTR (no one-verb-at-a-time re-deploy loop) | over the next 2 slices that add a NEW AWS service to a deploy role, the grant is `<service>:*` region-scoped (not enumerated) AND 0 deploys fail on a missing verb for that service AND bootstrap re-applies never hit the version cap (auto-prune); if a new-service deploy grant is enumerated and then false-reds on a missing verb, or bootstrap hits the 5-version cap again, FAILED | 2 new-service deploy grants (or 3 retros) | active (0/2) | applies-to: any slice adding a NEW AWS service to a deploy role's IAM policy (new API GW / new managed service / first-time provisioning of a service the deploy role did not previously touch). Founding PATTERN (SLC-039, 2nd occurrence): API GW v1 deploy grant under-scoped 3× (apigateway:PUT /tags/*, iam:UpdateRoleDescription, apigateway:UpdateRestApiPolicy) → 4 CD runs, fixed by 8a425ea `apigateway:*` region-scoped; bootstrap 5-version cap hand-pruned. Sibling of EXP-082 (deploy preflight) — distinct: EXP-082 checks deploy PREREQUISITES (lock/daemon/binding); EXP-094 makes the deploy-role GRANT verb-complete by construction so it can't fail one-verb-at-a-time. |
- **EXP-095 — ADOPTED.** retro-debt is a HARD advance precondition — blocks next-pull / re-deploy / hand-recovery until the retro drains it; in force in process §F8 + orchestrator.md + `make retro-debt`.
  - Original row: | EXP-095 | v79 (2026-07-05) | process-current.md §F8 (retro-debt is an ADVANCE precondition, not just a pull precondition) + orchestrator.md + `make retro-debt` wired into advance/deploy/re-deploy steps | **Problem (latent PROCESS gap — the reason this retro was OVERDUE):** during the SLC-039 deploy-failure recovery, the orchestrator HAND-RAN the CFR recovery on `main` (bootstrap re-applies, git pushes, CI watches, reactive cicd one-verb patches) AND advanced the loop (FOUR re-deploys) WHILE the §F8 retro-debt was ALREADY TRIPPED (incident threshold crossed at CD run 28734040102, 2026-07-05T10:00:01Z) — instead of running the gap-closing retro FIRST and routing the failure as a prioritised LOOP item through the flow-manager / product / architect. The v68 mechanical retro-debt gate (`make retro-debt`, non-zero exit = RETRO DUE) only guards the NEXT PULL; it does NOT guard RE-DEPLOY or hand-cranked recovery, so the orchestrator advanced the incident by IMPROVISING around the loop — the EXP-030 / v68-class recurrence: a gate the orchestrator can skip by improvising. Also: the 2nd/3rd/4th failure legs + the recovery were never ledger-logged until this retro (the hand-cranking bypassed the instrument too). **Solution:** make retro-debt a HARD ADVANCE precondition, not orchestrator discretion — a non-zero `make retro-debt` exit BLOCKS **every advance action**: next-pull, RE-DEPLOY, and any orchestrator hand-run recovery step on `main`. When retro-debt is tripped, the ONLY permitted orchestrator action is to run the retro (drain the debt); the CFR recovery itself is then routed as a **flow-manager-prioritised loop item** (defect pre-empts per §F5), NOT hand-cranked. The orchestrator does NOT do the specialist's job (cicd owns the IAM fix, engineer/tester the validation) and does NOT bypass the ledger. Cross-agent rule (orchestrator + flow-manager + cicd all bound) ⇒ version bump. | CFR (incidents get a gap-closing retro BEFORE more deploys, so the fix is systemic not reactive) + gross lead time (no un-logged hand-cranked recovery loop; recovery flows through the loop) + retro-debt-drain latency (retros run when DUE, not deferred) | over the next 2 incidents (deploy_failure or defect), the orchestrator runs the gap-closing retro BEFORE any re-deploy / next-pull / hand-run recovery, AND the recovery is dispatched as a flow-manager loop item (not orchestrator hand-cranking), AND every failure/recovery leg is ledger-logged as it happens; if the orchestrator re-deploys or advances the loop while `make retro-debt` is non-zero, or hand-cranks a recovery on main instead of routing it through the flow-manager, FAILED | 2 incidents (or 3 retros) | active (0/2) | applies-to: EVERY incident (deploy_failure or defect_intake) on any project — the moment retro-debt trips on an incident, no advance/re-deploy/hand-recovery until the retro drains it. Founding: SLC-039 (2026-07-05) — orchestrator hand-ran 4 re-deploys + bootstrap re-applies + reactive cicd patches on main while retro-debt was tripped and this §F8 retro sat undone; failure legs 2-4 + recovery were un-logged until the retro. Sibling of EXP-083/v68 (the mechanical retro-debt PULL gate) — distinct: EXP-083 gated the next PULL; EXP-095 extends the SAME hard gate to RE-DEPLOY + hand-recovery so the orchestrator cannot advance an incident by improvising around the loop. |
- **EXP-019 — ADOPTED.** tester runs parallel validation over the impacted∪core set, not the full suite (playwright workers:4 + `validate-impacted`); in force in the Makefile + tester practice.
  - Original row: | EXP-019 | IMP-009 (2026-06-08) | playwright.config (workers:4) + Makefile validate-impacted | attack tester EXECUTION: parallel validation + run impacted∪core not full suite | tester median task time (<900s) + lead time | per-slice smoke execution drops | 2 slices | active (1/2, partial) | s015 was a no-code slice (not a clean test). Structural cut in place; a code-bearing slice needed. applies-to: any slice validation |
- **EXP-027 — ADOPTED.** collision detection → correct dependency tree; hidden edges caught at integration + false edges null-hypothesis-tested; in force in process §F7 + flow-manager.md + edge-ledger.
  - Original row: | EXP-027 | v40 (2026-06-08) | process §F7 + flow-manager.md + edge-ledger.md | collision detection → correct dependency tree; hidden-edge→0 + false-edge null-hypothesis trials | CFR (hidden edges) + GLT (false edges) | collisions caught mechanically + fed back | 3 parallel slices | active (2/3, strong) | s001: scaffold + server.js seams caught PRE-build by §39 scheduling → 0 collisions/0 rework. s013/s015 wave: genuine hidden edge S13UC2→S15UC2 (ObservatoryView composition root) caught at integration, serialised by commit order (~5min, 0 rework), edge added to use-case-deps + edge-ledger — the mechanism fired end-to-end; refactor candidate noted (tab-wiring registry pattern would dissolve the edge). 1 more |
- **EXP-036 — ADOPTED.** live/monitoring surfaces signal staleness/disconnection (non-colour-redundant) + re-fetch on reconnect; orchestrator keeps a persistent server + verifies on ephemeral ports; folded into ui-designer.md this pass, in force in engineer.md/orchestrator.md.
  - Original row: | EXP-036 | DEFECT-003 (2026-06-10) | ui-designer.md + engineer.md + orchestrator.md | observability/live surfaces must SIGNAL staleness/disconnection (non-colour-redundant) and re-fetch on reconnect — never present stale data as live; orchestrator keeps a persistent server for the operator + verifies on ephemeral ports (never pkills the shared one) | CFR + data-trust (operator misled by stale-as-live) | a live view never shows old data without a clear stale/disconnected cue; orchestrator never kills the operator's running view | 2 live-view slices | active | applies-to: any live/monitoring surface + any time the project is a long-running app the operator watches. Founding data point: DEFECT-003 (map froze on 4/1 while backend was down — orchestrator had killed :3001; no stale signal). Fixed: stale banner + reconnect re-fetch |
- **EXP-060 — ADOPTED.** IAM grant = the FULL operation set of the code path (reads+writes), derived from the SDK-op set with a code↔policy pin; in force in process §17.2 + solution-architect.md + engineer.md.
  - Original row: | EXP-060 | v58 (2026-06-18) | process §17.2 + solution-architect.md (security review) + engineer.md (code↔policy pin) | IAM grant = the FULL operation set of the code path, not its headline verb: an event-store APPEND grant = the READ ops (`Query`/`GetItem`/`kms:Decrypt`) + the WRITE ops, never write-only; architect derives the grant from the issued SDK-op set, engineer's pin asserts the grant covers exactly that set | CFR (prod AccessDenied on first real event) + MTTR | a reads-then-writes path is never granted write-only; no first-event AccessDenied | 2 infra slices with a write path | active (0/2) | applies-to: any slice granting IAM to a code path that both reads and writes a resource. Founding PATTERN (3 data points, not one): OagEventSource ingest hit prod AccessDenied 3× this session — missing `dynamodb:Query`, then `kms:Decrypt`, then the append-path loadStreams read (`7a31d8a:ingest-iam-read-ops`). The existing code↔policy pin (§30) asserted code-matches-grant but the GRANT itself was authored write-only at design time → the pin can't catch a grant that was wrong from the start. Score: next write-path slice grants the full op set at design time; 0 first-event AccessDenied. |
- **EXP-061 — ADOPTED.** Node ESM bundles carry the createRequire shim (or bundle CJS) + an import-the-bundle smoke; in force in process §17.3 + cicd.md.
  - Original row: | EXP-061 | v58 (2026-06-18) | process §17.3 + cicd.md (Node ESM bundling rule) | Node ESM bundles carry the createRequire CJS shim banner (or bundle as CJS) + a committed import-the-bundle smoke that fails until the shim is present; DynamoDB reserved-keyword aliasing is the same build-clean/run-fail class | CFR (prod `Dynamic require` crash) + MTTR | an ESM bundle's runtime require crash is a red build, never a prod surprise | 2 Node-bundle slices | active (0/2) | applies-to: any slice bundling a Node Lambda/Fargate handler as ESM with `@aws-sdk`/`@azure`-class deps. Founding PATTERN (3 data points): the `Dynamic require` crash recurred across fold-demo, the Fargate consumer, AND the feed-projector this session (fixed by `--banner:js='…createRequire…'`, sha 6df7d79 — CJS switch not needed); EXP-059's `ttl` reserved-keyword crash is the same class. Bundles clean, fails only at runtime → reaches prod. Score: next ESM Node bundle ships with the banner/CJS + import-smoke; 0 `Dynamic require` crashes in prod. |
- **EXP-051 — ADOPTED.** a shared source-file seam serialises otherwise-independent UCs (flow-manager layer PASS 2/2); in force in flow-manager.md + process §F6. The par_eff dora.py-flow metric residue is moot — that path retired under v82/F0.
  - Original row: | EXP-051 | v54 (2026-06-17) | flow-manager.md + process §F6 | shared-source-file seam edge: a UC's claimed paths include the source file(s) its route MUTATES; ready UCs claiming the same source file are seam-serialised (not independent), so theoretical-max reflects the §40-achievable set | gross lead time (par_eff becomes a TRUE signal) + scheduling integrity | flow-manager stops reporting phantom max>1 on shared-file pulls; par_eff measures real efficiency, not forbidden parallelism | 2 multi-UC pulls on a shared file | active (1/2, flow-manager PASS; metric residue) | applies-to: any pull where ≥2 ready UCs mutate one source file. Founding: 3× (SLC-001/002/003) `achieved=1 max=4` par_eff 0.25 on UCs all editing normaliser-core.ts — max=4 was unachievable under §40. Genuine parallelism remedy = split-the-file refactor (a §F7 false-edge lever), tracked separately. **SLC-003 (2026-06-17): flow-manager layer PASS (2/2 pulls).** PULL-C2 (UC-12..15) + PULL-C3 (UC-16) were both pulled as a SERIAL batch with the schedule recorded as achievable-under-§40 (reason annotated: "shared normaliser-core.ts seam, serial on trunk"), NOT as a missed-parallelism time-thief — the reasoning fix fired exactly as designed; 0 phantom-opportunity claims; built green (140/22, 0 rework). **BUT the `dora.py flow` METRIC still computes par_eff=0.25 from raw `achieved=1 max=4` rows** — it does the division blindly without reading the seam reason, so flow.md prints a phantom efficiency even though the flow-manager was right. The behaviour is validated; the METRIC needs to honour the seam annotation (cap max at the achievable set when the pull reason is a shared-file seam). Routed forward → EXP-053 (v55). Hold EXP-051 at 1/2 until the metric reports a correct par_eff on a shared-file pull (full integration). **SLC-004 (no opportunity, carry):** the infra cycle pulled an OFFLINE-buildable set {UC-17,22,18} then held the deploy set {UC-19,20,21} behind the §F5 infra gate — a correct schedule, but the flow-manager logged `par_eff=0.25` again (the offline three were sequenced serial on the shared OagFeedStack wiring + the deploy set is genuinely gate-serial). This is the same phantom-max measurement gap EXP-053 must close; flow.md still computes 0.25 from raw rows. No NEW shared-file pull on a multi-file-splittable seam → no integration opportunity for EXP-051. Carry; the metric fix (EXP-053) remains the binding work. |
- **EXP-042 — ADOPTED.** a comment that describes misbehaviour is a defect — file it or delete the falsehood on touch; folded into engineer.md this pass.
  - Original row: | EXP-042 | DEFECT-014 (2026-06-13) | engineer.md practice + retro command (§26) | a comment that DESCRIBES misbehaviour is a defect, not documentation: any engineer touching a file with a known-issue/symptom comment ("X drops over Y", "known issue", "doesn't work when") either files the defect record or deletes the falsehood in that commit; the retro greps for the pattern class as a sweep | CFR + symptom-to-record latency | no documented-but-unrecorded misbehaviour survives a touch; the DEFECT-014 case (symptom written in metric-source.css for days, found only when the human hit it) cannot recur silently | 2 retros | active | applies-to: every engineer commit touching a file with such a comment + every retro. Founding data: DEFECT-014 — the overlap was IN THE CSS COMMENTS while four panels stacked in prod |
- **EXP-056 — ADOPTED.** external/cross-account resource ids asserted to resolve at a committed deploy pre-flight before the first real deploy (scored 1/2 positive v60); in force in process §17.1 + solution-architect.md + cicd.md + `make preflight`.
  - Original row: | EXP-056 | v57 (2026-06-18) | process §17.1 + solution-architect.md + cicd.md + Makefile (deploy pre-flight) | external/cross-account resource identifiers (Lambda layer ARN, shared resource in another account, third-party endpoint) are ASSERTED to resolve BEFORE the first real `cdk deploy` — a written "CONFIRM at build" note is not a confirmation; the confirmation is a committed check that FAILS until the id resolves (synth-time existence/permission probe or a `--require-approval` diff). Tool leg: a committed deploy pre-flight that runs the resolve check before the expensive deploy | CFR (first-deploy rollback) + MTTR + token cost (no failed-deploy fat-cicd re-spawn) | a cross-account/external dependency cannot reach a real deploy unconfirmed; the rollback→re-spawn loop is replaced by a cheap pre-flight failure | 2 infra-bearing slices | active (0/2) | applies-to: any slice whose route deploys infra referencing a resource it does not create. Founding: DEFECT-OAG-001 — first OagFeedStack deploy rolled back on `dash0-*` layer ARNs absent from OTel-community account 184161586896; delta-001's confirm-note was written, never executed → reached `cdk deploy` (999s round-trip). Also EXP-055's chosen token optimisation (pre-flight removes the most expensive cicd re-spawn). Score: next infra slice's external ids resolve at pre-flight or fail cheaply; 0 deploy rollbacks on a known-external id. **SCORED v60 (2026-06-21) 1/2, positive:** `make preflight-oag` ran BEFORE the push/deploy this session and passed — OTel collector+nodejs layer ARNs, Dash0 secret ARN, Dash0 ingress DNS, OAG Event Hub secret all asserted to resolve (a committed check, not a written note). The pre-flight tool fired as designed; the deploy's later failures (profile, role perms) were OUT of its scope (CI credential/identity, not external-resource ids) → correctly routed to EXP-069/070, not a pre-flight miss. 1 more infra slice to validate it CATCHES a missing id. |
- **EXP-057 — ADOPTED.** an unconfirmed-external-dependency infra capability ships behind a default-OFF infra flag so the skeleton deploys now; in force in process §11a.1 + engineer.md + cicd.md.
  - Original row: | EXP-057 | v57 (2026-06-18) | process §11a.1 + engineer.md + cicd.md | infra-flag: an infra capability whose external dependency is unconfirmed (per §17.1) ships behind a DEFAULT-OFF infra flag so the core walking skeleton deploys NOW and the dependency is deferred to an open item — never held back from deploy until resolved (the §40 use-case-flag pattern extended to infra) | deployment frequency + gross lead time (skeleton ships) guarded by CFR (OFF default = no live risk) | a slice with one unconfirmed external dependency still ships its core; the deferred capability + its §17.1 check become an open-items entry | 2 infra slices | active (0/2) | applies-to: any infra slice with an unconfirmed external dependency. Founding: DEFECT-OAG-001 — `otelEnabled=false` shipped the pull-feed skeleton (UC-21 AC-21a-f green in prod) while AC-21g (OTel telemetry) was deferred as an open item, instead of the whole slice rolling back. Score: next such slice ships its core flag-OFF; the deferred dependency is a tracked open item, not a deploy blocker. |
- **EXP-063 — ADOPTED.** the experiment-validity bar (Problem+Solution+named-DORA-metric+falsifiable-measure); now core in process §25a + retro step 5a — this very triage applies it (scored 1/2 positive v60).
  - Original row: | EXP-063 | human-directed (2026-06-18) | process §25a (validity bar) + experiments.md header + retro command step 7 | EXPERIMENT-VALIDITY BAR: every registry row must be a falsifiable HYPOTHESIS with all four — Problem (evidenced friction), Solution (the concrete change tested), a NAMED target DORA metric (lead time / deploy freq / CFR / MTTR; a meta-metric only if explicitly justified as a DORA proxy), and a falsifiable Measurement (an observable signal + scoring horizon that CAN come back negative). A row that merely describes a piece of work / a feature / a capability, has no named DORA metric, or has a measurement that cannot fail is REJECTED at creation and deleted on sight. The retro must adopt (metric moved → fold into the agent, prune the row) or delete (metric did not move → undo). Drift-purge applied this pass: EXP-058/059 deleted as work-items | registry validity (share of rows meeting the four-part bar; reject-rate of work-item rows) + agent-def simplicity (no mis-tracked work parked as live experiments) | the next 2 retros create ONLY valid hypothesis rows (each has Problem+Solution+named-DORA-metric+falsifiable-measure) and NO work-item-shaped row reappears in the registry; if a work-item row is created, the bar failed | 2 retros | active (0/2) | applies-to: every retro + every routed change that gets a registry row. Founding: human-directed drift audit (2026-06-18) — many rows described work rather than stated a falsifiable hypothesis; EXP-058 (architect enumerates fitness functions) and EXP-059 (documenter produces consumer-skill docs) were sound shipped behaviours mis-tracked as experiments with no DORA metric + a did-we-do-the-work "measurement" that cannot come back negative. Both behaviours KEPT as plain agent practice (solution-architect.md / documenter.md, no EXP scaffolding); the rows deleted. Score: at the next 2 retros, audit every new row against the four-part bar — PASS iff all are valid hypotheses and no work-item row was admitted. **SCORED v60 (2026-06-21) 1/2, positive:** the v60 retro (a) admitted only valid hypothesis rows — EXP-069 (deploy freq/lead) + EXP-070 (MTTR/CFR), each with Problem+Solution+named-metric+falsifiable-measure, no work-item row; and (b) ran the FULL validity-bar audit of all 37 live rows, pruning 12 that were core-process-machinery / validated-integrated (008/011/013/018/021/024/028/029/033/035/037/038) to the archive. Registry 37→25 live rows. The bar is being applied and is shrinking the registry as intended. 1 more retro. |
- **EXP-067 — ADOPTED.** the retro reviews the plumbing-vs-delivery cost-split + its trend; logic in wi-project §E, behaviour in process §26. Data-enablement continues as the live EXP-103.
  - Original row: | EXP-067 | v59 (2026-06-20) | dora.py (tokens col + cost_class + cost-split) + dora-ledger SKILL + process §26/§F4 + orchestrator.md | **Problem:** the retro sees TOTAL token/time cost (EXP-055) but not the PLUMBING share — how much of each cycle runs the agent OS (orchestration, flow, gates, bookkeeping, retros) vs delivers customer value. The standing constraint is "orchestrator"/plumbing yet unquantified per-class, so overhead-reduction is steered by intuition not evidence. **Solution:** ledger gains a `tokens` column (orchestrator logs each dispatch's subagent_tokens on task_end); `dora.py cost-split` + a baseline section split logged time+tokens into plumbing vs delivery with the plumbing share; §26 adds a standing retro step to review the share + its trend and route the highest-leverage overhead cut, guarded so delivery isn't harmed. | gross lead time (lower plumbing overhead → more delivery per cycle) + orchestrator-context constraint | the next 2 retros each show a plumbing share (time+tokens) WITH a cross-retro trend AND route a reduction that moves the share, with NO delivery regression (lead time/CFR); if the share can't be moved without harming delivery, the routed cut FAILS and is undone | 2 retros | active (0/2, BLOCKED on instrumentation) | v83 retro: the cost-split LOGIC is present in `wi-project` §E but came back UNCOMPUTABLE — every OFS stage event carried TOKENS=0 (token coverage 18.2% of 55 events; plumbing share 0.0%), because subagents cannot introspect their own `subagent_tokens` and the orchestrator was not stamping from the dispatch-level figure. Not FAILED (the split is correct, it is starved of data). RESCUE: EXP-103 makes the orchestrator stamp `--tokens` from the dispatch-level `subagent_tokens` it receives; re-score EXP-067 once coverage rises. applies-to: every retro. Extends EXP-055 (graduated token-review) + §F4 time-thieves with a plumbing/delivery class. Coverage caveat: precise for delegated/logged work; inline orchestrator time + main-loop tokens stay an estimate (pair with the §26 token-estimate). Founding: human-directed (2026-06-20) — "the retro should see how many tokens and how much time is spent on the plumbing of the system." |
- **EXP-069 — ADOPTED.** push trunk on each UC's green done-condition when a verified remote exists (push-on-green, no big-batch pooling); in force in process §14 + engineer.md.
  - Original row: | EXP-069 | v60 (2026-06-21, human-directed) | process §14 + engineer.md (commit→push on done) | **Problem:** the blanket "never `git push`" (v59/EXP-049) made the agent commit to local trunk only and never integrate — OagEventSource reached **44 commits ahead** of `origin/main` before anything was pushed; the entire integration+deploy risk pooled into one big-bang push whose CI then failed twice. Work that is "done locally but un-integrated" is invisible WIP that defers all integration risk. **Solution:** when a project repo has a configured, VERIFIED remote (`git remote get-url origin` = the origin recorded in project.md/decision-log), the engineer pushes trunk as part of each use-case's done-condition (suite+lint green) — one green UC = one push, never batched. No/unverified remote → do not push (the 2026-06-17 unverified-destination guard still binds). | deployment frequency + gross lead time (work integrates continuously instead of pooling), guarded by CFR (each push is green locally) | the next 2 projects-with-a-remote keep commits-ahead-of-origin ≤ ~1 per finished UC (no >10-commit local batch reappears) AND deployment frequency rises with no CFR increase from premature pushes; if a large unpushed local batch recurs, or push-on-green raises CFR, FAILED | 2 remote-bearing projects (or 3 retros) | active (0/2) | applies-to: any project repo with a verified remote. Supersedes the blanket-no-push of EXP-049 (which is reduced to the unverified-destination guard). Founding: this session's 44-commit batch + the first-ever CI run failing on push. |
- **EXP-070 — ADOPTED.** every push sets off a non-blocking ci-watch; a red-after-green run is raised as a defect closed by add-the-local-check OR capture+automate; in force in process §19b + engineer.md + cicd.md + ci-watch.sh.
  - Original row: | EXP-070 | v60 (2026-06-21, human-directed) | process §19b + engineer.md (push→watch→raise) + cicd.md (divergence dichotomy) + `ci-watch.sh`/Make target | **Problem:** a CI run that fails when the local suite+lint passed was, this session, caught only by hand-watching `gh run watch` and reading 298-line logs; nothing made a green-local/red-CI divergence a tracked failure, and nothing forced its root-cause to be removed. **Solution:** every push sets off a non-blocking CI watch (committed `make ci-watch` — returns only the failing step's error, not the whole log); a run that fails while local was green is raised as a DEFECT whose fix MUST be exactly one of {add the local check that would have caught it | capture the manual config in the runbook AND automate it as a committed script/Make target} — never re-run-and-hope. | MTTR (a red push is raised within one watch-cycle, not discovered later) + CFR (each divergence permanently removes a local/CI gap) | the next 2 red-after-green CI runs each generate a defect within one watch-cycle AND close via category-1 (coverage) or category-2 (runbook+automate) — 0 silently-abandoned red pushes, and the same divergence class does not recur; if a red push is left red / re-run-without-fix, FAILED | 2 red-after-green runs (or 3 retros) | active (0/2) | applies-to: any push to a remote with CI. Token co-benefit (§26): `ci-watch` returns a minimal failure summary vs whole-log triage. Founding: this session — `AWS_PROFILE=default` (CI-only credential path local never exercised = category 1) + deploy role with no permissions (manual config = category 2 → `bootstrap-deploy-role.sh`). |
- **EXP-071 — ADOPTED.** externally-consumed event bodies designed consume-first (minimal applyable delta; no redundant change-representations / per-event snapshot); folded into solution-architect.md this pass + product.md.
  - Original row: | EXP-071 | DEFECT-OAG-009 (2026-06-22) | solution-architect.md + product.md (consume-first event-contract design) | **Problem:** the event change-representation was designed EMIT-first — the field-differ already produced `changedFields[]` + a `previous{}` map, so the canonical event body accreted FOUR overlapping ways to express one change (`change.changedFields` paths + `change.previous` + `payload` + the full `flight` snapshot). No design step asked "is this the MINIMAL structured delta a consumer applies?" → two contract redesigns landed on the same surface (OI-017 fat→thin snapshot; DEFECT-OAG-009 changedFields-array→delta-object). **Solution:** when defining or changing an externally-consumed event/message contract, the architect (with product) validates the body is the minimal structured delta a consumer APPLIES — the changed leaves, explicitly keyed — with NO redundant change-representations and (for non-genesis events) NO full-state snapshot; pinned by a "consumer applies this event with no other source" test. The owning agents derive the shape consume-first (what the consumer folds), not emit-first (what the differ already has). | CFR (contract-shape defects on the deployed event contract) + gross lead time (re-cutting a shipped contract is expensive rework) | the next 2 event-contract definitions/changes ship a consume-first minimal body validated by an apply-the-delta test AND no over-stuffed contract (redundant change-representations or an unneeded per-event snapshot) ships; if a contract ships and later needs a shape redesign for redundancy/bloat, FAILED | 2 event-contract slices | active (0/2) | applies-to: any slice defining or changing an externally-consumed event/message body shape. Founding: DEFECT-OAG-009 + OI-017 — both redesign the same body because it was emit-shaped. Sibling of EXP-065 (conform to the frozen spec) — distinct: EXP-065 is "match the spec", EXP-071 is "the spec body is the minimal applyable delta in the first place". |
- **EXP-076 — ADOPTED.** event versioning + total forward-mapping + a maintained event catalog so a valid stored event never poisons a consumer; in force in principles/03 + solution-architect.md + documenter.md.
  - Original row: | EXP-076 | v63 (2026-06-24) | process/principles/03 + solution-architect.md + documenter.md | event versioning + TOTAL forward-mapping + event catalog: consuming a valid stored event must never fail; events versioned, every consumer supports all versions, changes non-destructive + total-mappable vN→vN+1, new data ships a default defined in the new version; architect maintains the versioned catalog (version history + mapping rules + per-field defaults), documenter keeps it a core actual/ doc, consumers carry version-coverage tests | CFR + MTTR | the consumer-brittleness class (poison on a known-type stored event: leg/subtree/gate/terminal) stops recurring; the next OAG event-shape change ships with a catalog entry + version-coverage fixtures and 0 poison-on-known-event defects reach prod | 2 event-shape slices (or 3 retros) | active (0/2) | applies-to: any slice consuming/emitting a versioned event. Founding PATTERN (2 data points): DEFECT-OAG-024 (incoming-body leg guard) then DEFECT-OAG-025 (prior-aggregate leg/subtree guard) — same class, a completeness fix not propagated to the sibling read path; defensive guards fixed symptoms, principle 03 is the structural prevention. Sibling of EXP-065/071 — here the guarantee is total forward-mappability so no version/shape poisons a consumer. |
- **EXP-075 — ADOPTED.** the upstream stage (product+solution-architect+cicd+ui-designer) pipelines on the next independent item while the engineer builds; in force in process §F3a + orchestrator.md.
  - Original row: | EXP-075 | v62 (2026-06-23, human-directed) | process §F3a + orchestrator.md (upstream pipelining) | **Problem:** the engineer (the constraint) idles when an upstream artifact it needs — the next item's architecture delta, cicd capability/flag/grant, or ui structure — was only prepared at a stage boundary rather than during the prior build. §F3 already pipelined PRODUCT ahead but not solution-architect / cicd / ui-designer, so a pull could still land on un-designed / un-provisioned work. **Solution:** each loop cycle the orchestrator dispatches the whole upstream stage (product + solution-architect + cicd + ui-designer) CONCURRENTLY on the NEXT sequentially-independent item while the engineer builds the current one, bounded by §F6 independence + queue wip_limit + buffer depth; they write disjoint artifacts. The engineer's next pull finds design + capabilities already done. | gross lead time (eliminate engineer-waits-for-upstream gaps) + throughput | over the next ~6 multi-item slices, every engineer pull finds its architecture delta + required capabilities already prepared (0 build-starts blocked waiting on an upstream artifact that could have been prepped during the prior build); if an engineer stalls at pull time for a missing upstream artifact on an independent item, FAILED. Guard: must NOT raise CFR (pipelined-ahead design that proves wrong when the item is pulled = rework) — score CFR alongside. | ~6 multi-item slices (or 3 retros) | active (0/6) | applies-to: any multi-item slice/chunk where upstream planning (architecture/capability/structure) precedes the build. Founding: human direction 2026-06-23 — "product, solution-architect, maybe cicd can be working on the next thing whilst the engineer is working away". Extends EXP-022/§F3 (product replenishment) to the full upstream stage. |
- **EXP-078 — ADOPTED.** verify the external-API interface contract (path/auth/params/envelope) at the authoritative source before encoding it (scored 1/2 positive); in force in solution-architect.md.
  - Original row: | EXP-078 | v64 (2026-06-24) | solution-architect.md | verify the external-API INTERFACE CONTRACT (endpoint path, auth header, required params, response envelope) at the AUTHORITATIVE source before encoding it in any probe/delta/build artifact — portal-first (human-assisted) when a skill marks it ⚠ PORTAL; never best-guess, never brute-force the live API | gross lead time + CFR | the next 2 external-API slices confirm the interface contract at the authoritative source BEFORE probe/build (0 dead live-API discovery calls, 0 wrong-endpoint/header in a build artifact); if a slice best-guesses an external interface and burns live calls / ships a wrong endpoint, FAILED | 2 external-API slices (or 3 retros) | active (1/2, positive) | **SLC-013 (2026-06-25): 1/2 positive** — both the Flight-Info `/flight-instances` AND the Master-Data `/locations` endpoints came from the authenticated portal (never guessed/brute-forced); the Master-Data 401 surfaced cleanly as a subscription gate, not a dead-call hunt. 1 more. applies-to: any slice consuming an external API whose request contract isn't already pinned. Founding: OI-021 UC-R0 — architect guessed `/flight-info/v2/flights` + `Ocp-Apim-Subscription-Key` (both wrong), orchestrator brute-forced ~5 dead calls; the authenticated portal gave `/flight-instances` + `Subscription-Key` + `CodeType` in one shot. Extends EXP-066 from payload-semantics to the interface contract; see process/principle-failures/2026-06-24-guessed-external-api-spec-instead-of-authoritative-portal.md |
- **EXP-079 — ADOPTED.** same-item parallel dispatch partitions owned paths in each agent brief, never two agents authoring one file (scored 1/2 positive); in force in orchestrator.md.
  - Original row: | EXP-079 | v64 (2026-06-24) | orchestrator.md | disjoint artifacts on SAME-item parallel dispatch: when >1 agent runs on one work-item concurrently, the orchestrator partitions owned paths in each brief — UC test+code = engineer, cicd wires lane/infra only; never two agents authoring one file | gross lead time + CFR | the next 2 same-item multi-agent dispatches partition owned paths with 0 double-claim collisions; if a same-item dispatch collision recurs, FAILED | 2 same-item parallel dispatches (or 3 retros) | active (1/2, positive) | **SLC-013 (2026-06-25): 1/2 positive** — CC1/CC5/CC4 seams partitioned in dispatch + the SLC-013 build HELD behind the DEFECT-026 normaliser seam; 0 double-claim collisions (vs the UC-R1 collision that founded the row). 1 more. applies-to: any cycle dispatching >1 agent on one item concurrently. Founding: OI-021 UC-R1 — engineer (UC build) + cicd (AC-R1.9 lane) both authored rest-seed-live-api.integration.test.ts (collision row; reconciled byte-identical, wasted rework). Orchestrator-dispatch-overlap class, distinct from EXP-027's hidden-code-seam collisions. |
- **EXP-084 — ADOPTED.** the rule lifecycle (experiment → integrate/graduate → prune to archive) that keeps /process lean; now core in the process-framework skill — this triage IS the lifecycle running.
  - Original row: | EXP-084 | v68 (2026-06-26, human-directed) | process-framework skill (rule lifecycle) + delivery-principles/cicd skills (first-pass graduations) + experiments-archive.md | **Problem:** /process accrues rules/EXPs/principle-failures faster than it sheds them (36 active EXP rows at v68), bloating the active process — and per EXP-083 the bloat does not even buy adherence. The design intent (CLAUDE.md + process-framework skill) is that /process stays LEAN and proven rules graduate to the stable SKILL layer. No explicit lifecycle existed to drain proven rules out. **Solution:** define the rule lifecycle in the process-framework skill — experiment (EXP in /process) → integrate into the owning agent file (single-agent) OR GRADUATE into the relevant skill (cross-agent methodology, proven ≥K=2 + stable) → prune the EXP row from active /process to the archive. First pass executed: graduated 10 proven cross-agent rows into delivery-principles/cicd + agent files. | process leanness (active EXP-row count + active-process applicability) as a DORA proxy — a leaner process is faster to load/apply and removes un-adhered bloat; guarded by CFR (a graduated rule must keep preventing its defect class) | over the next 2 retros the active EXP-row count does not net-grow (graduations ≥ new non-graduated additions) AND no graduated rule's defect class recurs because the rule "got lost" in a skill; if the registry net-grows with proven-stable rows that should have graduated, or a graduated rule stops being applied, FAILED | 2 retros | active (0/2) | applies-to: every retro. First-pass leanness gain measured: active EXP rows 36 → 26 (graduated 025/062/064/065/066/068/072/073/074/081). Founding: human-directed 2026-06-26 — "rules that are proven/working get pushed into the skills so the active process isn't carrying them." |
- **EXP-085 — ADOPTED.** retro-debt cadence split — routine closes batch to 3, incidents fire immediately (scored 2/3, both legs exercised); in force in dora.py + process §F8 + loop-run.md + orchestrator.md.
  - Original row: | EXP-085 | v69 (2026-06-26) | dora.py `cmd_retro_debt` (routine/incident split, default --threshold 3) + Makefile retro-debt comment + process §F8 + loop-run.md + orchestrator.md | **Problem:** the v68 retro-debt gate (EXP-083) shipped with threshold=1 — a retro is DUE the moment ANY slice/chunk closes. On its FIRST real firing (SLC-009, a validation-only close of pre-built work + the trivial TD-002 dead-code delete) that proved too aggressive: a per-slice-close retro cadence reintroduces exactly the retro-overhead/bloat churn that v68 fought (a bloated/too-frequent retro is itself a §F9.4 time thief), and it does not distinguish a routine clean close (little to learn) from a real incident (much to learn). **Solution:** SPLIT the gate's events by class. ROUTINE slice/chunk closes batch up to --threshold (raised default 1→3) before a retro is due, so a clean run of small closes does not pay per-slice retro overhead. INCIDENT events (prod defect resolve, deploy_failure) are NOT batched — a single one forces RETRO DUE immediately (effective threshold 1), so real learning is never deferred. This is the v68 gate SELF-CORRECTING on its first real firing (the intended behaviour), not a bypass — the gate still fires mechanically, still may not be offered to the human, and an incident still triggers immediately. | retro-overhead vs missed-learning balance (a DORA proxy on gross lead time — fewer low-yield per-slice retros free the constraint, while incident learning stays immediate so CFR/MTTR protection is unchanged); guarded by CFR (a deferred routine retro must not let a defect class go un-routed — incidents bypass the batch) | over the next 3 retros: routine slice/chunk closes batch to ~3 before a retro fires (no per-close retro on a clean run) AND every prod defect resolve / deploy failure still forces an immediate retro (incident debt never batched) AND no learnable incident is missed because a retro deferred; if a clean slice close still forces a per-slice retro, or an incident is batched and its retro deferred, FAILED | 3 retros | active (2/3, positive — BOTH legs now exercised) | applies-to: every loop cycle that closes a slice/chunk or resolves a defect/deploy-failure. Founding: this v69 retro — threshold=1 fired on SLC-009 (validation-only close), the gate's first real firing, exposing per-slice cadence as overhead-heavy. Extends EXP-083 (mechanical enforcement KEPT; only the cadence right-sized + guarded). **SLC-026/027/028 wave (1/3, positive — FIRST batched fire):** routine closes BATCHED to 3 before RETRO DUE — no per-slice retro on a clean run. **v71 (2/3, positive — INCIDENT leg now EXERCISED):** the previously-untested incident leg fired exactly as designed — DEFECT-OAG-032 (a defect resolve) drove `retro-debt` to "RETRO DUE [incident (immediate)]" while routine count was only 2/3 (SLC-016 + SLC-029 closes, below the routine threshold). i.e. the incident was NOT batched: a single defect resolve forced the retro immediately even though routine debt had not reached 3. Both legs (routine-batches-to-3, incident-fires-at-1) now confirmed behaving as designed. 1 more retro to complete the window, then integrate alongside EXP-083's machinery. |
- **EXP-082 — ADOPTED.** deploy target runs a fail-fast preflight (stale-lock/daemon/creds/verified-target binding) + ci-watch reads the resolved origin (scored 1/2 positive); in force in cicd.md §20a + make targets.
  - Original row: | EXP-082 | v67 (2026-06-25) | cicd.md §20a (deploy preflight + verified-target binding) + deploy-fids/ci-watch make targets | **Problem:** the §F5 FIDS deploy failed TWICE before landing — on a STALE SST state lock (left by a killed loop) AND a Docker-daemon-down bundling failure — and `ci-watch` then MISREPORTED "no runs" because the CI/deploy lookup resolved to the WRONG repo (`john-aerocloud/claude_learning_agents`) not the real `origin` (`AeroCloudSystems/Spike-FlightEventSource-OAG`). `deploy-fids` checked the SSO token but NOT the state lock, the Docker daemon, or the target binding — so prerequisite/health/binding faults surfaced as a mid-deploy crash inside the gate (lead-time thief) and a status read against the wrong target (false "nothing happened", the EXP-080 class in the deploy ENVIRONMENT). **Solution:** the deploy target runs a FAIL-FAST preflight before any irreversible step — (a) release/clear any stale SST state lock, (b) assert the Docker daemon is up, (c) credential valid, (d) the CI/deploy binding resolves to the verified `origin`/account, never an ambient `gh` default; a missing prerequisite is an actionable message, not a crash; `ci-watch` passes the resolved `origin` repo explicitly. | gross lead time (no failed-deploy retries inside the §F5 gate) + CFR (no status read against the wrong target) | over the next 2 deploys, 0 deploys fail on a stale lock / daemon-down / wrong-target-binding that a preflight would have caught AND every CI/deploy status reads against the verified `origin`; if a deploy crashes on an un-checked prerequisite or a status is read against the wrong repo/account, FAILED | 2 deploys (or 3 retros) | active (1/2, positive) | applies-to: any deploy target on a project with an external state lock / build daemon / remote CI binding. Founding PATTERN (3 data points, SLC-023 §F5): stale SST lock + Docker-down (deploy retried twice) + ci-watch wrong-repo "no runs". Natural extension of EXP-080 (verify-at-source) from the build artifact to the deploy environment + its binding. See process/principle-failures/2026-06-25-deploy-preflight-and-ci-watch-wrong-repo.md **SLC-026/027/028 wave (1/2, positive — the anti-false-green machinery held):** this was a CLEAN backend deploy, the precise contrast to the prior serviceType wave (DEFECT-028/030/031 false-greens). The bundles-current gate (the DEFECT-030 fix) PASSED — bundles were rebuilt+committed before deploy so the deployed artifact matched source; CI green BOTH lanes; the consumer rolled to primary; and per the EXP-080 verify-at-source doctrine the tester read the deployed BUILD_SHA at the running service (e5587a7 confirmed) and validated the NEW fields on events the NEW consumer ACTUALLY ingested (recovery/generalAviation observed on a live FlightCreated; 14 live OagFlightTakenOff with correct offGround shape; DLQ 0). 0 deploy retries, 0 wrong-target reads, NO false-green, NO defect. The whole v68/v69 anti-false-green stack (bundles-current gate + validate-running-service doctrine + verify-at-source) produced a clean wave on the same surface that previously false-greened 3×. 1 more deploy to integrate. |
- **EXP-087 — ADOPTED.** prod-smoke build-identity asserted from an INJECTED EXPECTED_SHA (skip-when-unset), never a frozen literal; in force in cicd.md.
  - Original row: | EXP-087 | v71 (2026-06-30) | cicd.md (prod-smoke build-identity = injected EXPECTED_SHA, never a literal) | **Problem:** a prod-smoke / live-integration test that asserts the deployed build identity against a HARDCODED/DERIVED expected sha goes STALE on every deploy and false-reds the lane on EVERY push between a source change and its redeploy. THREE sightings of this exact class on OagEventSource: (1) FIDS UC-FD1 — stale `EXPECTED_SHA` literal in fids-board.spec.ts (fixed by env-sourcing via the `e2e-fids` target); (2) the SLC-026 main.mjs embed-proof; (3) DEFECT-OAG-032 — the REST/serviceType prod-smoke `serviceVersion` pin asserted `e5587a7` while the deployed service was `40ade754`, false-failing CI on a green-local push (UC-SR1 is offline-pure, the red was a pure build-identity drift, NOT a functional regression). Each was fixed REACTIVELY per-lane after it false-failed → wasted adjudication + an inflated CI-red signal that masks real reds. **Solution:** ANY live/prod-smoke build-identity assertion derives the expected build-id from an INJECTED `EXPECTED_SHA` (env, defaulting to the just-deployed / CI `github.sha`) and SKIPS with a clear message when unset — never a frozen literal; plus a deploy-then-smoke ordering note (the identity assertion only holds AFTER the CD redeploy lands the pushed bundle; running it against a not-yet-redeployed service is expected-skip, not fail). Generalises the FIDS `e2e-fids EXPECTED_SHA ?= $(BUILD_SHA)` seam to ALL prod-smoke lanes as standing cicd practice. | CFR (no false-red prod-smoke lane masking a real red; build-identity drift is not a deploy failure) + gross lead time (no per-lane reactive whack-a-mole, no wasted adjudication on an independent build-identity red) | over the next 2 prod-smoke/live-integration lanes that assert a deployed build identity, the assertion is env-sourced (EXPECTED_SHA defaulting to the deployed/CI sha, skip-when-unset) and 0 lanes false-red on a post-deploy sha drift; if a NEW prod-smoke lane ships a hardcoded build-id literal, or an existing one false-reds on sha drift again, FAILED | 2 prod-smoke lanes (or 3 retros) | active (0/2) | applies-to: any slice adding/changing a prod-smoke or live-integration test that asserts the deployed build identity (serviceVersion / BUILD_SHA / embedded sha). Founding PATTERN (3 data points): FIDS UC-FD1 stale EXPECTED_SHA literal → SLC-026 main.mjs embed-proof → DEFECT-OAG-032 REST serviceVersion pin (resolved 75cedbf: env-sourced EXPECTED_SHA=github.sha + skip seam, mirroring the FIDS machinery). Sibling of EXP-080/082 (verify-at-source / verified-target) — distinct: those verify the status is read against the right target; this makes the EXPECTED build-id self-defaulting so it cannot go stale. |
- **EXP-092 — ADOPTED.** windowed/bounded-scan folds get a real-VOLUME/aging fixture that ages the target event past the scan window so the assumption is falsifiable; in force in engineer.md + tester.md.
  - Original row: | EXP-092 | v78 (2026-07-03) | engineer.md + tester.md (fold/hydration test-design: real-VOLUME/aging fixtures) | **Problem:** DEFECT-OAG-040 — the FIDS board showed "Scheduled" for ALL flights + empty Baggage tab in the real high-volume dev feed, while 412 unit tests were green. Root cause: status was derived from a `lastStatusEvent` marker seeded ONLY from events within a BOUNDED backward-scan window; on the real feed one page carried 66 non-status events (EstimatedArrivalChanged), which pushed each flight's OnBlock/OffBlock/TakenOff event OUT of the scanned window → the marker was never set → fallback "Scheduled". The correct OOOI fields (`actual.inGate/onGround/outGate`) were in the aggregate all along; Gate/Arrival columns worked because they read fields, not the marker. The fold's dependence on **scan-window completeness** was an UNSTATED INVARIANT, and every fixture was real-SHAPE (EXP-073) but LOW-VOLUME / single-page, so the bounded-window assumption was never falsified. This is a NEW facet of the ground-truth-oracle family: real shape ≠ real volume/aging. **Solution:** when the code under test makes a WINDOWED / bounded-scan / pagination / recency-cutoff assumption over an event stream (a fold, a backward-hydration, an incremental-poll cursor), its test corpus MUST include a real-VOLUME fixture with enough intervening non-target events that the target event AGES PAST the window — so the bounded-window assumption is exercised and can be FALSIFIED. The engineer writes the fold/hydration test against such a corpus (captured/synthesised to real page-density); the tester's validation asserts the derived state (status/marker) against per-entity ground truth on a high-volume window, not a hand-sized one. The scan-window bound itself is stated as an explicit invariant in the route/spec, not left implicit. | CFR (windowed-scan folds that silently drop state on real-volume feeds) + MTTR | over the next 2 fold/hydration/windowed-scan use-cases, the test corpus includes a real-volume/aging fixture that ages the target event past the scan window AND the derived-state assertion passes on it (0 all-fallback / silently-dropped-state defects reach a running feed); if a windowed-scan surface ships with only low-volume/single-page fixtures and later false-greens on a real-volume feed, FAILED | 2 fold/hydration/windowed-scan use-cases (or 3 retros) | active (0/2) | applies-to: any use-case whose route folds/hydrates/scans an event stream under a bounded window, page limit, recency cutoff, or backward-scan-from-head assumption (FIDS fold/bootstrap, BIDS, incremental-poll cursor, any read-model hydration). Founding: DEFECT-OAG-040 (fix 800efca) — bounded-scan-window `lastStatusEvent` marker aged out on the 66-non-status-events page; 412 green unit tests + real-SHAPE fixtures never reproduced it. Sibling of EXP-073 (real-source fixtures = right shape) — distinct: EXP-073 fixes the SHAPE, EXP-092 fixes the VOLUME/AGING so a windowed assumption is falsifiable. Related open items: OI-PRE-EXISTING-042 (fids-schedule-fold.spec.ts live-sparsity → synthetic fixtures) is the same fixture-realism class from the other direction (too-sparse). |
- **EXP-096 — ADOPTED.** the CD deploy-`<stage>` job re-applies the target stage's deploy-role policy as a self-healing pre-flight + `make promote-preflight`; in force in cicd.md + process §F5a.
  - Original row: | EXP-096 | v80 (2026-07-05) | cicd.md (deploy-`<stage>` job self-applies the target stage's managed deploy-role policy as a fail-fast pre-flight) + process §F5a (promotion pre-flight rule) + `make promote-preflight STAGE=<s>` | **Problem:** SLC-039 UC-CA-PROD-PROMOTE prod deploy failed with `apigateway:PUT on /tags/* AccessDenied` in the PROD account (842827416606) — the SAME first error the DEV deploy hit weeks-ago-in-session (EXP-094). The prod deploy-role policy CONTENT was correct (`infra/policies/sst-deploy-prod.json` carries `apigateway:*`, committed 8a425ea for BOTH dev and prod), but `bootstrap-deploy-role.sh --stage prod` was NEVER RUN — only `--stage dev` was. So the `oag-sst-deploy-prod` IAM role carried a STALE policy version and the correct content never reached the role. This is a PROMOTION-PROCESS gap distinct from EXP-094 (which fixed policy CONTENT completeness): promoting a slice to a NEW stage requires RE-APPLYING that stage's deploy-role policy to that stage's role as a pre-flight, and nothing enforced or reminded that. Dev-first HELD — dev + acceptance-dev were green and the failure was prod-only-contained (the value of dev-first working as intended) — but the promotion still needed a manual pre-flight that was missed. **Solution (recommended: self-healing, option (a) over a manual checklist):** the CD `deploy-<stage>` job RE-APPLIES the target stage's managed deploy-role policy (`bootstrap-deploy-role.sh --stage <stage>` / the equivalent apply-managed-policy step) at the TOP of the job before any SST/CDK deploy — so the role's policy is ALWAYS current-with-source at deploy time and cannot go stale-per-stage; a §F5a promotion rule states this as the contract; plus a `make promote-preflight STAGE=<s>` that applies+asserts the policy for orchestrator/manual promotions. Chosen over a pure orchestrator checklist because a "remember to run bootstrap --stage <target>" note is exactly the reminder that was missed here — EXP-056's lesson: the confirmation must be a committed check that self-heals/fails, not a written reminder the orchestrator must remember. Cross-agent (cicd owns the CD-job step + `make promote-preflight`; orchestrator/§F5a owns the promotion rule) ⇒ version bump. | CFR on cross-stage promotions (a promotion to a new/other stage does not fail on a stale/never-applied deploy-role policy) + deploy MTTR (no per-stage re-bootstrap-then-redeploy loop) | over the next 2 slice promotions to a NEW stage (dev→prod, or standing up a new stage), the target stage's deploy-role policy is auto-applied/asserted BEFORE the first infra deploy (self-healing CD step or `make promote-preflight` run) AND 0 promotions fail on a stale/never-applied deploy-role policy; if a promotion to a stage reaches a real deploy with a stale/never-applied deploy-role policy and false-reds on a grant that IS in the committed policy content, FAILED | 2 stage promotions (or 3 retros) | active (0/2) | applies-to: any slice promoted dev→prod OR standing up a NEW deploy stage (any per-stage IAM deploy role). Founding: SLC-039 UC-CA-PROD-PROMOTE (2026-07-05) — prod `apigateway:PUT AccessDenied` because `bootstrap-deploy-role.sh --stage prod` was never run though the prod policy content was correct (8a425ea). Sibling of EXP-094 (verb-completeness of the policy CONTENT) — DISTINCT: EXP-094 makes the granted content complete; EXP-096 ensures the correct content is APPLIED to each stage's role at promotion time. Owner: cicd + §F5a. |
- **EXP-097 — ADOPTED.** concurrent code-committing agents each get a private git worktree/index (the one §14 worktree exception) so commits cannot cross-attribute; in force in orchestrator.md + engineer.md + process §14/§F6 + the worktree-per-project topology.
  - Original row: | EXP-097 | v80 (2026-07-05) | orchestrator.md (concurrent-code-committing dispatch = git worktree isolation) + engineer.md + process §14/§F6 | **Problem (deferred from OI-044, now the standing fix):** the concurrent-agent shared-index commit-attribution hazard has now recurred 4+ times — a commit from one concurrent agent session picks up staged changes from another because the git INDEX is shared across concurrent sessions on one working tree. 4th sighting: UC-SF2's engineer commit (content byte-identical) was swept into the tester commit 389d86f. The v39 mitigation (explicit-pathspec `git commit -- <paths>`, never bare `git add`+`git commit`) reduces but does not ELIMINATE the class — a shared index still means one session's staged content is visible to another's commit. §F6 mandates §40 flag-isolation in CODE (no branches/worktrees) for the SEAM-independence dimension, but that does not address the INDEX-sharing attribution hazard for genuinely concurrent code-committing dispatches. **Solution (formalize the OI-044 recommendation as a standing dispatch rule):** when the orchestrator dispatches 2+ agents that will COMMIT code concurrently (parallel engineers, or engineer+tester both committing), each gets its own git WORKTREE (`git worktree add`) so each has a PRIVATE index — commits cannot cross-attribute. This is the ONE §14 exception to the trunk/no-worktree default: worktree isolation is for COMMIT-ATTRIBUTION safety on concurrent committers, orthogonal to §40 flag-isolation (which stays the rule for behavioural seam-independence within a single tree). Single-committer cycles keep the plain trunk working tree. Cross-agent (orchestrator dispatch rule + engineer worktree discipline) ⇒ version bump. | CFR / commit-attribution-correctness (a concurrent committer's changes are never swept into another's commit) + gross lead time (no reconciliation rework when a byte-identical/cross-attributed commit is discovered) | over the next 2 cycles that dispatch 2+ concurrent code-committing agents, each committer works in its own worktree/private index AND 0 shared-index cross-attribution/sweep incidents occur; if a concurrent-committer cycle runs on a shared index and a cross-attribution/sweep recurs (5th instance), FAILED | 2 concurrent-committer cycles (or 3 retros) | active (0/2) | applies-to: any cycle dispatching 2+ concurrent engineer/code-committing agents on one project repo. Founding PATTERN (4 data points): route-sweep, both sides of the s009 split, and now UC-SF2→389d86f (2026-07-05, principle failure logged). Sibling of the v39 pathspec rule (§14) — DISTINCT: the pathspec rule mitigates within a shared index; EXP-097 gives each concurrent committer a PRIVATE index so the class cannot occur. Owner: orchestrator (dispatch) + engineer. |
- **EXP-088 — ADOPTED.** committed `make render-diagrams` gate + no diagram (or diagram fix) reported done until it renders green; in force in the Makefile + process §17 + documenter.md.
  - Original row: | EXP-088 | v72 (2026-06-30) | committed `make render-diagrams` gate + process §17 contract + documenter.md | **Problem:** agents (documenter, solution-architect, orchestrator) author/edit Mermaid diagrams and report them done WITHOUT ever rendering them — "diagrams are mandatory" was a content rule with no executable check. DEFECT-OAG-033: 7 OagEventSource diagrams were silently un-renderable (parse errors from unescaped `()`/`{}`/`<>&"'` in labels), and the orchestrator even committed a `fix(diagram)` (0f96d3e) that STILL did not render — a fix claimed without re-rendering. **Solution:** a committed `make -C work/<project> render-diagrams` gate (mmdc over every `.mmd` + every `.md` mermaid block; honours both non-zero exit AND scanned "Parse error" output; exits non-zero on any failure) PLUS a §17 cross-agent contract: no agent reports a diagram (or a diagram fix) done until the gate is green. | CFR (silently-broken diagrams shipped as "done"; doc-render defects) + gross lead time (no reactive per-diagram defect cycles like DEFECT-OAG-033) | over the next 3 diagram-bearing changes (a slice/defect/doc-pass that creates or edits a Mermaid diagram), 0 diagrams are reported done that fail `make render-diagrams`, and the gate is run+green before each diagram claim; if a diagram-bearing change is reported done with a gate-failing diagram, or an agent claims a diagram fix without a green gate run, FAILED | 3 diagram-bearing changes (or 2 retros) | active (0/3) | applies-to: any slice/defect/doc-pass that CREATES or EDITS a Mermaid diagram (`.mmd` or a ```mermaid block) — documenter, solution-architect, or orchestrator. Founding: DEFECT-OAG-033 (2026-06-30) — `make render-diagrams` over the full scope surfaced 7 un-renderable diagrams (the original 12-file scan found 4; 3 more latent: current.md, 05-observability, SKILL.md); orchestrator overclaimed 0f96d3e without re-rendering. Fix-for-gap = the executable gate (the failing render IS the test) + the must-be-green-before-done contract. Sibling of EXP-087 — both make a "did you actually verify it?" check executable rather than trusting a claim. |
- **EXP-099 — ADOPTED.** deploy is a first-class `deploying` state owned by cicd (governed state-graph amendment) so deploy time is attributable in GLT-by-owner; in force in state-graphs.json + wi-project + cicd.md.
  - Original row: | EXP-099 | v82 (2026-07-06) | process/machinery/state-graphs.json (v3) + work-items.py + cicd.md + loop-run.md + process-current.md §11b | **Problem:** the use-case state graph folded deploy into the engineer's `built_green` (building→validating) transition, so `cicd`'s deploy time had no state of its own and was invisible in the gross-lead-time by-owner breakdown — a whole part of the process (deploy) could not be attributed or seen as a time-thief; it silently inflated the engineer's `building` span. **Solution:** a governed graph amendment (per state-graphs.json's own rule that a wanted-but-absent transition must be proposed as an EXP) — insert a `deploying` state owned by `cicd` (`built_green`→`deploying`, new `deployed`(cicd)→`validating`), with `state_owners.deploying=cicd` and `queue_map.deploying=wip`; `cicd` fires `deployed` via `wi-append` after each per-UC deploy. | gross lead time (attribution granularity) — makes `cicd`'s deploy contribution a named, measurable % of GLT so the retro can target it as a distinct constraint | over the next 2 projects with REAL deploy events (OagEventSource's `deployed` events are migration-synthesised): deploy time appears as its own `cicd`-owned span in `views/stats.md` by_owner, distinct from build; if deploy stays invisible or folded into building, FAILED | 2 projects (or 2 retros) | active (0/2) | applies-to: any use-case whose route deploys. Founding: the v82 Phase-A build flagged that `cicd`'s only legal use-case transition was `build_failed`, so deploy latency was absorbed into `building` — the "every part understands its contribution to gross lead time" goal (v82) required deploy to be a first-class owned stage. First observation: OAG migration shows `deploying` at ~20% GLT (synthetic) attributed to `cicd`. |

### KILLED — no measurable effect / never validated (superseded mechanism / under-question-negative / really-a-fix)

- **EXP-022 — KILLED — no measurable effect / never validated.** the uniform-queue policy.csv min_items/wip_limit buffer model was SUPERSEDED by the v82 event-sourced model (queues derived from state via queue_map, not policy.csv buffers; cf. EXP-098 pruned at v82). Mechanism retired.
  - Original row: | EXP-022 | v40 (2026-06-08) | process §F2 + queues/policy.csv + flow-manager.md + dora.py | UNIFORM queue model: every queue min_items + wip_limit, retro-tuned; same four metrics per queue | gross lead time (dwell) + throughput | dwell falls without starvation; retro tunes each queue | 2 buffer-tunes | active (1/2) | floor-breach detection works (fired on observatory intake). **First real buffer TUNE landed at v80-retro (EXP-098): ready min_items 2→3 driven by 41 loop_wake starvation events in the SLC-039/014/025 burst — the retro-tuned-per-queue model exercised end-to-end (evidence→tune→scored experiment). 1 more tune to validate.** |
- **EXP-040 — KILLED — no measurable effect / never validated.** the staging.csv visible-buffer + CSV ledger-row self-recording mechanism was SUPERSEDED by the v82 per-item event logs; the no-invisible-work principle now holds by construction (state=fold(events)). Mechanism retired.
  - Original row: | EXP-040 | DEFECT-012 (2026-06-11) | product.md + flow-manager.md + queues/staging.csv + engineer practice | handoffs are visible buffers + agents self-record their own open/close ledger rows at actual start/finish (orchestrator records only flow events it owns): product appends decomposed items to staging.csv before task_end; flow-manager drains at every sweep; board renders the staging queue | CFR (data-trust: invisible/phantom work on the board) + WIP/queue truthfulness | no decomposed item is ever invisible (staging depth>0 during the handoff window); no orphan task_start / late task_end rows from proxy bookkeeping (DEFECT-012's root cause: orchestrator recorded product's start 4min early once and its end 26min late once, plus 2 orphan starts in one session) | 2 decompose handoffs | active | applies-to: every product decompose/replenish + every agent dispatch. Founding data: DEFECT-012 (fix bc2ff5f). The DEF-012 engineer self-recorded its rows same-session — early compliance signal. **s014 run scoring (1/2): partial pass** — every agent self-recorded across the 3-day run (engineers/testers/ui-designer/flow-manager, including through session-limit cuts where rows survived the dead reporter); ONE miss class: a tester keyed its rows by slice slug → DEFECT-013/EXP-041 sharpened the rule to work-item ids. No decompose handoff occurred yet (staging path unexercised — carry) |
- **EXP-054 — KILLED — no measurable effect / never validated.** really a harness/config fix, not a falsifiable experiment (under-question, NEGATIVE on its only score). Root cause (a stale single-quoted shell snapshot + inherited-PATH dependency) was found and fixed; the durable fix (a committed Make target exporting the toolchain bin internally) stands as project practice.
  - Original row: | EXP-054 | v56 (2026-06-18) | .claude/settings.json `env.PATH` = fully-STATIC absolute PATH (nvm node bin + pyenv/homebrew/coreutils), NO `${PATH}` token | give the agent shell node WITHOUT an inline `PATH=…` prefix, robustly | gross lead time (PATH-prefix tax) + shell integrity | plain `npm`/`node`/`git`/`cat` resolve first try in a FRESH session; 0 inline-PATH prefixes | first node command of next fresh session | under-question (0/1, negative) | applies-to: every node-using dispatch. **Supersedes failed EXP-050 (deleted v56).** EXP-050 used `…/bin:${PATH}`, but the harness does NOT expand `${PATH}` → the literal token clobbered coreutils (git/cat vanished) AND its env was read only at session start (mid-session edit never reached subagents). Fix applied 2026-06-18 (static value). **Leg (b) still open → cicd:** a committed Make/wrapper target that exports the node bin INTERNALLY so resolution never depends on inherited shell env (covers MID-session). Until validated in a fresh session, node commands resolve via the committed Make/wrapper, not a hand-typed inline `PATH=…` prefix (§15). **SLC-004 retro (0/1, carry): UNVALIDATABLE YET** — this retro session still required the human-supplied inline `export PATH=…` prefix (the session pre-dated the static-PATH edit; §16.5 session-start resolution means a mid-session edit can't rescue the running session). The static value IS committed in settings.json (verified — no `${PATH}` token, coreutils present). Scores on the FIRST fresh-session node command, not here. Leg-b Make/wrapper still open → cicd. **SCORED v60 (2026-06-21) — NEGATIVE → under-question.** This was a FRESH session (post-/clear), the committed `env.PATH` is correct (verified: `/usr/bin` + `/bin` present, no `${PATH}` token), yet `ls`/`cat`/`head` were `command not found` from the first command and EVERY command needed a manual `export PATH=…` prefix. So the settings.json `env.PATH` (leg-a) did NOT reach this session's Bash tool environment — the §16.5 "env read only at session start" concern realised as the targeted failure RECURRING despite a correct value. Leg-a is not delivering; **leg-b (a committed Make/wrapper that exports the toolchain bin INTERNALLY, never depending on inherited shell env) is now the binding fix** (also OagEventSource OI-008). If leg-b also fails to remove the manual-prefix tax across 2 fresh sessions, retire EXP-054 and route the PATH problem as a harness/defect issue. **UPDATE 2026-06-22 (OI-008 done) — ROOT CAUSE FOUND + FIXED.** The dead-`${PATH}` token was traced to a SINGLE stale shell snapshot `~/.claude/shell-snapshots/snapshot-zsh-1781762602826-t4u4za.sh` (18 Jun, pre-EXP-054) containing `export PATH='…/v24.12.0/bin:${PATH}'` single-quoted — re-sourced on EVERY Bash call (it was never regenerated; settings.json env.PATH is clean/static). Two fixes shipped: (a) **leg-b** — OagEventSource Makefile top-level `export PATH` (globbed node + coreutils), proven to bootstrap node/ls/git under a deliberately broken PATH (`make toolcheck` PASS); (b) **leg-a root cause** — removed the stale snapshot so the harness regenerates clean from the static settings.json. **Scores next FRESH session:** if plain `ls`/`node`/`git` resolve with no manual `export PATH`, leg-a is validated → move to `validated`; if the snapshot regenerates dirty again, escalate as a harness defect (settings.json env single-quoted into the snapshot). |
- **EXP-077 — KILLED — no measurable effect / never validated.** metric DEGRADED, not moved — impacted-tests-sees-the-sub-repo went under-question NEGATIVE (specced as IMP-007, never built, recurred at full cost); SUPERSEDED/rescued by the live EXP-104 which lands the actual nested-`.git` fix.
  - Original row: | EXP-077 | v63 (2026-06-24) → IMP-007 | Makefile impacted-tests + IMP-007 slice | impacted-tests sees the project sub-repo: when the parent-repo diff is empty (parent .gitignores /work/*/), `make impacted-tests` ALSO diffs the project sub-repo so a class-deps/dep-model change inside work/<project> is reflected in the impacted set | CFR | the next dep-model change inside a project sub-repo appears in the impacted set (not "no changed nodes") AND a sibling-path change is covered by the selected suite; if a sub-repo dep change is again invisible to test selection, FAILED | 2 sub-repo dep-model changes (or 3 retros) | under-question (v83: NEGATIVE) | v83 retro: metric did NOT move — it DEGRADED. `make impacted-tests` with a PROJECT SHA failed `fatal: bad revision` on FOUR OFS slices (UC-A3/A4/A5/E3) because the tool resolves the git root to the parent/integration repo, not the project's nested repo (a stronger failure than the founding "empty diff"). The fix was specced (IMP-007) but never BUILT, so it recurred at full cost. RESCUE: re-scoped into EXP-104 — cicd lands the tester's proven nested-`.git` resolution fix (14/14 self-tests). Resolve when EXP-104's build lands and a project SHA returns a non-empty impacted set with zero `fatal: bad revision`. applies-to: any test-impact selection on a project that is its own sub-repo. Founding: 2026-06-24 principle-failure — DEFECT-025's class-deps.mmd change (project sub-repo) returned "no changed nodes" from the parent's perspective; tester fell back to the code-diff rule. Extends IMP-007 (impacted-tests-from-change-map). |

## AdixOut retro (REQ-003, 2026-07-16) — 6 rows retired

Applied §25a scoring at the REQ-003 (AIDX ELDT/EIBT/TKO conformance-completeness) close. Registry cut 13 → 8 active: EXP-100/104/110/111 ADOPTED, EXP-102 physically moved (already ADOPTED 2026-07-13), old-EXP-105 KILLED (0/3, never a scoring opportunity across 3 retros). EXP-104 was reconciled to ADOPTED on the merge of main v92 (its nested-repo git-root fix is in force; the residual false-clean is a separate unmarked-node gap, now covered by the engineer mark-changed-node practice). Each entry carries its disposition + one-line outcome and the FULL original row text verbatim.

### ADOPTED — behaviour sound + load-bearing + in force; row pruned, behaviour kept as plain agent/process practice

- **EXP-100 — ADOPTED (3/3).** Walk the full Theory-of-Constraints loop (identify→exploit→subordinate→elevate + ≥3-level why-chain + recurring→principle-failures + constraint-gate) at every retro/parts-check. 3rd positive application at REQ-003: walked the loop, recorded the why-chain, gated the change-set on the constraint, and the in-system constraint's share FELL (engineer 19.4%→17.1%, rework→0.58%). Metric moved (engineer GLT share fell across retros). Behaviour now lives in process-current.md §5b + retro command as plain practice; EXP-100 citation scaffolding stripped.
  - Original row: | EXP-100 | v82 (2026-07-06) | process-current.md §5b (five-step ToC loop) + retro.md (walk exploit/subordinate/elevate + ≥3-level why-chain + recurring→principle-failures + constraint-gate) + loop-run.md (per-close parts-check) | **Problem:** the process only "IDENTIFIED the constraint" (§5 named the top GLT-share owner) but never ran the rest of the Theory-of-Constraints loop — no explicit EXPLOIT (remove the constraint's waste/rework first), SUBORDINATE (cap upstream `wip_limit` so non-constraints stop piling inventory on it), or ELEVATE (add N / a stronger tier only last). A constraint could be named retro after retro while the change-set spent its budget on non-constraint work, so gross lead time did not fall even though the bottleneck was known. There was also no cheap after-every-piece review of the parts — the only review was the full §F8 retro. **Solution:** §5b writes the full five steps against `stats.*` `by_owner`; retro.md WALKS exploit→subordinate→elevate (not just records the constraint), requires a ≥3-level root-cause why-chain for the constraint's dominant GLT contribution, opens a `principle-failures/` entry for a RECURRING root cause even when nothing "failed", and GATES the change-set (a change not targeting the constraint must justify itself as a subordinate/exploit/safety move or be deferred); loop-run.md adds a cheap per-close parts-check (read `by_owner`, log "constraint = X; shifted? y/n", no version bump) that escalates to a full retro only when the constraint SHIFTS or the §F8 threshold fires. | gross lead time (the constraint's GLT share falls because the change-set is forced onto it via exploit→subordinate→elevate, not spent elsewhere) | over the next 3 retros: each retro's change-set either targets the identified constraint (an exploit/subordinate/elevate move) or is a justified safety/subordinate exception, AND the constraint's GLT share in `stats.*` `by_owner` falls or the constraint SHIFTS to a new owner (evidence the prior one was relieved); if a retro names a constraint but routes its change-set away from it with no justification, or the same owner stays the top GLT share across 3 retros with no exploit/subordinate/elevate move landed against it, FAILED | 3 retros | active (2/3) | v83 retro (1st opp): APPLIED — walked identify→exploit→subordinate→elevate; honestly separated the queue-wait ARTIFACT (66% = front-loaded batch registration + one overnight session gap) from the real WORKING constraint (engineer/building 31.6%); recorded a ≥3-level why-chain; opened 2 principle-failures for recurring root causes; gated the change-set on the constraint (every routed change is an exploit/subordinate/safety move). Constraint not yet shifted — the top exploit lever (token visibility, EXP-103) had to be built first, and evidence is a single-project thin sample. Positive. applies-to: every retro + every per-close parts-check. Extends §5's identify-only wait-taxonomy to the full ToC loop. Guard: a subordinate `wip_limit` cap must not starve a non-constraint stage into a NEW bottleneck (score throughput alongside — an elevate/subordinate that just moves the constraint without lowering total GLT is not a win). ‖ ROC retro (2nd opp): APPLIED — walked the full loop against `stats.md` `by_owner`; separated the registered/queue ARTIFACT (74.44% = front-loaded batch UC-registration at decomposition + multi-day human-session cadence on a weekend project) from the squeezable in-system WORKING constraint (engineer/building 25.56%). EXPLOIT enabler landed: EXP-103 fired on ROC (token coverage 18%→50%, `by_owner` build tokens now visible = 1.396M). Change-set gated on the constraint: the one behavioural change (IMP-011 CORE-done invariant) is a justified SAFETY move (defect-preventing) under the gate. Constraint still `queue` (not shifted) but honestly artifact-dominated on a thin single-project sample. Positive. ‖ AdixOut retro (3rd opp, 2026-07-16): ADOPTED — walked identify→exploit→subordinate→elevate on REQ-003; recorded the ≥3-level why-chain; gated the change-set (two deterministic fixes routed as plain practice, no rows); the in-system WORKING constraint's share FELL (engineer 19.4%→17.1%, rework→0.58%, lead-time median 1284→1053s). Metric moved across retros. |
- **EXP-102 — ADOPTED (3/3, moved 2026-07-16).** Defect-vs-rework fork + findings→requirement loop. Scored ADOPTED 2026-07-13; physically moved from active this retro. Both fork limbs demonstrated across 3 opportunities; the classification is woven into process §3 + tester.md as plain practice (no scaffolding to remove — the behaviour already lives in the agent/process).
  - Original row: | EXP-102 | v82 (2026-07-06) | process-current.md §10/§F9 + product.md (findings→requirement loop) + tester.md + §3 (defect-vs-rework fork) | **Problem:** two feedback loops were ABSENT/unstated. (V5a) When an agent surfaced a FINDING that was genuinely new customer value or newly-discovered scope, there was no path to turn it into a costed, prioritised work item — it was either lost or dumped into `open-items.md` (process residue), so real value never entered the queue and could not be pulled. (V5b) The fork between a deploy-failure (the UC under validation fails → rework, no DEF-) and a regression in already-shipped work (→ a DEF- defect) was unstated, so testers conflated them — inflating CFR with rework, or filing defects for UCs that never shipped. **Solution:** (V5a) product frames a value/scope finding as a JTBD and REGISTERS it as a requirement/chunk/UC via intake (`registered`) so it enters costing+prioritisation (§10); a finding needing a human value-judgement routes through `/intake`. (V5b) the tester classifies on failure: failing behaviour in the UC currently under validation → `rejected` (rework, a deploy-failure, no DEF-); a failure in previously `validated`/`done` behaviour → a `DEF-` via `/intake` (`reported`), a defect against the standing system (§3). | CFR (rework and standing-system regressions are no longer conflated — CFR counts deploy-failures only, §3) + gross lead time (value findings enter the queue and flow, instead of being lost to open-items residue) | over the next 3 slices: (a) ≥1 value/scope finding is registered as a costed work item via intake (not parked in open-items) and appears in the queue; AND (b) every tester failure is classified as EITHER a `rejected` deploy-failure (UC-under-validation) OR a `DEF-` regression (shipped work) with 0 misclassifications (no DEF- raised for a never-shipped UC; no shipped-work regression left as bare rework); if a value finding is lost to open-items instead of registered, or a tester failure is misclassified, FAILED | 3 slices (or 3 retros) | validated (3/3, ADOPTED 2026-07-13) | ADOPTED — both fork limbs now demonstrated across 3 opportunities; the classification is woven into process §3 + tester.md as plain practice, so this row is retired from active (no scaffolding to remove — the behaviour already lives in the agent/process). AdixOut retro (3rd opp, 2026-07-13): the DEF- limb HELD — DEF-ADIX-001 (Dependabot dep vulns, a security issue in the SHIPPED system) was correctly registered as a `DEF-` defect (not rework on an in-flight UC), routed through defect intake, and counts in CFR/MTTR as a defect not a deploy-failure; 0 misclassifications. AdixOut retro (2nd opp, 2026-07-12): fork (b) HELD AGAIN — UC-ADIX-006's concurrency failure was classified `rejected` (rework on the UC under validation, no DEF-), 0 misclassifications; →2/3. v83 retro (1st opp): fork (b) HELD — the UC-E3 stale-model failure was correctly classified as a `rejected` REWORK (deploy-failure of the UC under validation, no DEF- raised), so CFR (11.1% = 1/9 deploys) counts the deploy-failure only and MTTR got its first data point (652s); 0 misclassifications. Loop (a) had no opportunity (no new value/scope finding surfaced — the acceptance tightening was rework, not new scope). Positive on (b). applies-to: every slice (the defect/rework fork fires on any tester failure; the findings loop fires whenever an agent surfaces new value/scope). Two loops registered as one row because they share the "route the signal to the right register" mechanism. Sibling of §3 (CFR convention) and §F5 (defect intake pre-empts). |
- **EXP-110 — ADOPTED (3/3).** An unrun test tier is a test FAILED — start the dependency (Docker/emulator) and run it; every green report carries per-tier counts. 3rd positive at REQ-003: every engineer/tester/cicd report on UC-011/012/013 carried full per-tier counts incl. Docker local (started + run), 0 silent skips. Behaviour lives in engineer.md/tester.md/cicd.md as plain practice; EXP-110 citation scaffolding stripped.
  - Original row: | EXP-110 | v86 (2026-07-12) | engineer.md + tester.md + cicd.md (a test you did not run is a test FAILED — start Docker/emulator and run all tiers) | **Problem:** engineers/testers repeatedly SKIPPED the local Docker/DynamoDB-Local tier ("requires Docker, not executed here") across UC-ADIX-001/003/005 and still reported the item green. As a direct result a stale `transactionIdentifier` assertion in `skeleton.local.test.ts` HID for three use-cases — the local tier that would have caught it was never run. User escalation: "needing docker is not a reason to not do something… Not running tests is the same thing as a test failure." A whole tier of the suite was dead weight and real drift survived validation — a change-failure waiting to surface. **Solution:** every test-running agent (engineer, tester, cicd) treats an unrun tier as a FAILURE — if a runtime dependency is down, START it (`make -C <proj> local-up`; start the Docker daemon itself) and RUN the tier; `built_green`/`validated` may NOT be reported with any tier unrun; a genuinely un-startable dependency is an explicit BLOCKER, never a silent skip. Every green report carries the per-tier counts. | CFR (a skipped tier is an unvalidated tier → escaped defect / stale assertion survives to a green report) | over the next 3 use-cases: every `built_green`/`validated` report carries ALL-tier counts (unit + local/integration [+ synth-pin]) with 0 silently-skipped tiers, and no stale-assertion/skipped-tier-class escape recurs; if an item is reported green with any tier unrun (absent a justified BLOCKER), FAILED | 3 use-cases (or 3 retros) | active (2/3) | AdixOut retro (2nd opp, 2026-07-13): POSITIVE — every UC-007/008 + DEF-ADIX-001 report carried full per-tier counts (unit + local Docker + synth-pin, e.g. 152/152), 0 silently-skipped tiers; the DEF-ADIX-001 vitest-3.x toolchain bump was proven green across all tiers rather than assumed. applies-to: every test-running agent on every build/validation. Founding: `2026-07-12-adixout-skipped-local-tests-needing-docker.md` (stale assertion hid across 3 UCs). First positive data point THIS cycle: UC-ADIX-005 and UC-ADIX-006 both reported full-tier counts (116 unit + 6 local Docker + 20 synth-pin), engineer+cicd both ran `local-up`→test-local→`local-down`; the stale assertion was caught and fixed once the tier actually ran. Sibling of EXP-092/EXP-109 (completeness-of-what-you-exercise family). ‖ AdixOut retro (3rd opp, 2026-07-16): ADOPTED — every engineer/tester/cicd report on UC-011/012/013 carried full per-tier counts including the Docker local tier (started + run), 0 silent skips. |
- **EXP-111 — ADOPTED (3/3).** Truthful build identity: `assert-clean-tree` deploy gate + `safe-deploy.sh` stream-drain across the code swap + verify GitCommit==HEAD. 3rd positive at REQ-003: 3 deploys (0.11.0/0.12.0/0.13.0) all had GitCommit==exact HEAD off a clean tree, safe-deploy drained the stream ESMs across every swap, 0 mis-identified records. Behaviour lives in cicd.md as plain practice; EXP-111 scaffolding stripped.
  - Original row: | EXP-111 | v86 (2026-07-12) | Makefile `assert-clean-tree` (deploy prerequisite) + `safe-deploy.sh` (stream-drain across code swap) + cicd.md (truthful build identity) | **Problem:** build identity LIED twice on AdixOut. (1) Stream-race: SST/Pulumi updated the Lambda's config+tags BEFORE the code zip finished swapping, so stream-triggered invocations ran STALE code under the NEW build's `gitSha` — 3 conformance-violating >32-char `@TransactionIdentifier` records became resident in the Outbox tagged with the new build (UC-ADIX-003). (2) Dirty-tree deploy: cicd deployed UC-ADIX-005's uncommitted C10/C11 code, so the pipeline stamped `GitCommit=f010289` (the last commit, which has none of that code) — the deployed resource claimed a sha it was not running. Both violate ADR-0006 (build-once, deploy a COMMITTED artifact by digest; the tag names the bits actually serving) and the runbook's first diagnostic ("read build identity, compare to expected"). **Solution:** `safe-deploy.sh` disables the event-source-mappings across the code swap (trap-EXIT re-enable) so no stale-code invocation runs under the new identity; a Makefile `assert-clean-tree` gate (deploy prerequisite) REFUSES to deploy on a dirty working tree (`git status --porcelain` non-empty) unless explicit `ALLOW_DIRTY=1`, so `GitCommit` is always == HEAD. | CFR (a deploy that mis-identifies its code, or emits mis-identified/bad records at the cutover, is a change-failure) | over the next 3 deploys: every deployed resource's `GitCommit` tag == the exact 40-char HEAD sha AND 0 dirty-tree deploys occur AND 0 mis-identified records are produced at the deploy cutover; if a deploy stamps a `GitCommit` != HEAD, or a stream-race tags stale code with a new identity, FAILED | 3 deploys (or 3 retros) | active (2/3) | AdixOut retro (2nd opp, 2026-07-13): POSITIVE — UC-ADIX-007 (0.7.0) and UC-ADIX-008 (0.8.0) both deployed off a clean tree with `assert-clean-tree` passing, each verified `GitCommit`==exact HEAD via `list-tags`; 0 dirty-tree deploys, 0 mis-identified builds. applies-to: every SST/stream-triggered deploy. Founding: `2026-07-12-adixout-build-identity-claimed-before-code-live.md` (two manifestations). First positive data point THIS cycle: UC-ADIX-006 deployed twice — 0.6.0 (`GitCommit=cd5f440`) and 0.6.1 (`GitCommit=27fb4d7`), each verified == HEAD via `list-tags`, both off a clean tree (the guard printed `assert-clean-tree OK`), no cutover records mis-tagged. Sibling of EXP-096 (deploy-role policy freshness) — both keep the deployed artifact honest-to-source. ‖ AdixOut retro (3rd opp, 2026-07-16): ADOPTED — 3 deploys (0.11.0/0.12.0/0.13.0) all had GitCommit==exact HEAD off a clean tree, safe-deploy.sh drained the stream ESMs across every swap, 0 mis-identified records. |
- **EXP-104 — ADOPTED (reconciled on main-v92 merge).** Nested-`.git` git-root resolution for `make impacted-tests` (`resolveDiffRoot`). The fix shipped with 22 self-tests and is in force — a project SHA now resolves against its owning nested repo with zero `fatal: bad revision`. Outcome: adopted (tool fix in force). The residual UC-ADIX-013 false-clean was NOT the tool but an unmarked changed `.mmd` node, now covered by the engineer mark-changed-node plain-practice added this retro. Behaviour lives in impacted-tests.js + cicd.md/engineer.md; row pruned from active.
  - Original row: | EXP-104 | v83 (2026-07-11) → IMP-007 | .claude/tools/impacted-tests.js (nested-`.git` git-root resolution) + Makefile | **Problem:** `make impacted-tests` with a PROJECT SHA fails `fatal: bad revision` because it resolves the git root to `process.cwd()` (the parent/integration repo) while the project is its own nested repo under `work/<project>/` (v50 topology, v0a worktree). Recurred on FOUR OFS slices (UC-A3/A4/A5/E3), losing mechanical change-impact coverage each time. The fix was specced (IMP-007) but never built, so it recurred at full cost (see EXP-077, now under-question). **Solution:** cicd lands the tester's proven fix — detect the nested `.git` under `work/<project>/` and diff FROM that repo (`git -C work/<project> diff <since>..HEAD -- architecture/dependencies/*.mmd`), interpreting SINCE against the repo that owns the SHA, falling back to the parent only when no nested repo exists; keep the 14 self-tests as protection. | tester lead time (reduce: no manual fallback) + CFR (restore change-impact coverage assurance) | on the next multi-UC slice, `make impacted-tests` with a project SHA returns a non-empty impacted set with ZERO `fatal: bad revision`, and the tester derives its plan from it (no manual fallback); if a project SHA is still invisible/errors, FAILED | 2 slices (or 3 retros) | active (0/2) | applies-to: any test-impact selection on a project that is its own nested repo. Supersedes/rescues EXP-077. Owner: cicd (§16.3). **Fix landed 2026-07-13** (5th recurrence prompted the build): `resolveDiffRoot()` in impacted-tests.js now asks each candidate repo whether it owns SHA (`shaExistsIn`, `git rev-parse --verify`), prefers the nested project repo, falls back to parent, raises an actionable error if neither owns it — never a raw `fatal: bad revision`. Proven end-to-end against real AdixOut shas (`5eacf14`, `98139c7`, and the parent-of-`c8b34a1` window which yields a genuine non-empty impacted set of 4 changed nodes) with zero bad-revision failures; self-tests 14→22 (8 new: 3 resolveDiffRoot unit + 1 end-to-end + 4 covering the new @covers/node-id convention check below). Retro should score against the stated measure (2 slices / 3 retros clean) starting from this landing, not from v83. ‖ AdixOut retro (REQ-003, 2026-07-16): MIXED. The git-root resolution WORKED (no `bad revision`), and UC-ADIX-012 correctly reported MAP IMPACTED — but UC-ADIX-013 gave a FALSE-CLEAN (empty impacted set) that forced a manual code-diff fallback — a NEGATIVE data point against the "non-empty set / no manual fallback" clause. Root cause was NOT the tool: the engineer did not mark the changed departure/MAP node `:::changed` in the feat commit (principle-failure 2026-07-16-uc-adix-013-changed-node-not-marked). The node-marking practice added to engineer.md THIS retro is the enabling remedy for EXP-104's measure. Stays active. |

- **EXP-108 — VALIDATED / ADOPTED (3/3, AdixOut v94 retro 2026-07-22).** `deploy_failed` event + CFR integrity (CFR counts deploy_failed). Horizon = 3 deploy failures across a window; it got 3 IN ONE CYCLE on REQ-005 Chunk A — UC-014 (iam:CreateRole least-priv gap), UC-016 (WAFv2 description-charset ValidationException), and the UC-016/DEF-ADIX-002 rework — each recorded a `deploy_failed` before fix-forward, so CFR read an HONEST 28% (12.5%→28%) instead of a false 0.0%, and MTTR tracked the rework recovery. Outcome: the "fire `deploy_failed` on ANY deploy-step failure, even fixed-forward" rule already lives as PLAIN PRACTICE in cicd.md (§ "Record every deploy failure as `deploy_failed`") and engineer.md (§ "Record deploy failures — even fixed-forward"), and CFR = (rejected + deploy_failed)/(validated + rejected + deploy_failed) is in wi-project; row pruned from active. Sibling of EXP-107, whose local-synth gate was shown this same cycle NOT to catch the real-control-plane failure class — EXP-108 (record the failure) + live post-deploy verification are what make CFR honest where the cheap pre-push gate cannot.
  - Original row: | EXP-108 | v87 (2026-07-12) | state-graphs.json v5 (deploy_failed event) + work-items.py (CFR counts deploy_failed) + cicd.md + engineer.md + §3 | **Problem:** CFR read a falsely-perfect 0.0% while a real deploy failure had occurred — UC-XA2's first push (ec56025) turned the infra CI (auto-deploy) red, but was fixed-forward under `built_green` leaving NO event the metric reads; and CFR only counted tester `rejected`. The old §3 convention even carved out "pipeline-failure before prod" as non-CFR, which let a failed DEPLOY step hide as a mere pipeline wait. So genuine change-failures were invisible (same "green/zero reflects unrecorded reality" family as v84 §12d and DEF-XA1). **Solution:** a `deploy_failed` event (deploying/prod-deploying → reworking) that cicd/engineer MUST fire on ANY deploy-step failure — including a CI auto-deploy going red — EVEN when fixed-forward, before the re-deploy; wi-project now computes CFR = (rejected + deploy_failed)/(validated + rejected + deploy_failed); §3 narrows the pipeline-wait carve-out to pre-DEPLOY CI only. | CFR (integrity — CFR reflects real deploy failures instead of reading a false 0%) | over the next 3 deploy failures (any project): each fires a `deploy_failed` event before fix-forward AND is reflected in the CFR numerator (CFR > 0 when a deploy_failed exists); if a deploy step fails with no `deploy_failed` recorded, or CFR stays 0 across a window containing a known deploy failure, FAILED | 3 deploy failures (or 3 retros) | active (0/3) | applies-to: every deploy (cicd/engineer). Founding incident + gap: principle-failures/2026-07-12-cfr-reads-zero-and-no-cancel-state; concretises the v86 EXP-107 note ("a fix-forward deploy-failure should leave a trace the counter can read"). The `cancelled` terminal (state-graphs v5) shipped alongside is a state-model correctness fix (CONTRACT + §12e), not a DORA experiment — no separate row. ‖ AdixOut retro (REQ-003, 2026-07-16): no opportunity — 0 deploy failures this cycle (all REQ-003 deploys green). |

### KILLED — never validated, no applicable slice across 3 retros

- **EXP-105 — KILLED (0/3).** Load/replace stale-prior-state acceptance case. Never validated and NO scoring opportunity ever existed — AdixOut/ROC/OFS were backend / non-load-replace work, so it was stranded across 3 retros. No behaviour folded (it was never demonstrated). May be re-opened if a UI load/replace surface arises.
  - Original row: | EXP-105 | v83 (2026-07-11) | product.md + tester.md (load/replace surfaces encode + adversarially exercise the stale-prior-state case) | **Problem:** UC-E3's acceptance under-encoded a load/replace surface — it required a bad entry not to load as a wrong model, but NOT that a failed load AFTER a prior GOOD load must CLEAR the earlier model from view. The tester's adversarial ordering caught the stale-model defect, costing a rework cycle (the run's ONLY CFR hit: 11.1%). **Solution:** product authors an explicit failed-load-after-good-load acceptance case for any load/replace UC (the earlier good model must be cleared, only the error shows); the tester exercises that ordering as standard. | CFR (fewer validation rejections on load/replace UCs — the stale-prior-state class is caught in dev, first time) | on the next load/replace UC, the acceptance includes the stale-prior-state case AND it passes dev-validation first time (no rework cycle for that class); if a load/replace UC is rejected for an un-encoded stale-prior-state failure again, FAILED | 3 load/replace UCs (or 3 retros) | active (0/3) | applies-to: any UC that loads/replaces the active model/view. Founding: OFS UC-E3 rework. |

- **EXP-103 — KILLED — target never reached (3-strikes).** orchestrator stamps `--tokens` from the dispatch-level `subagent_tokens`. Scored OFS v83 (1st, POSITIVE 18%→50%), AdixOut REQ-003 (MIXED, regressed 50%→43%), ROC v100 (NEGATIVE, regressed to 19.5% because centralised orchestrator appends landed TOKENS=0). Target (>80% coverage, non-zero plumbing share, cross-retro climb) never reached across 3 scoring opportunities and trending DOWN — the mechanism is sound but coverage depends on operator discipline the row cannot enforce. The stamping behaviour is KEPT (in orchestrator.md) and its no-regression requirement is now carried as **EXP-116 guard G5**; the standalone measurement row is retired.
  - Original row: | EXP-103 | v83 (2026-07-11) | orchestrator.md (stamp `--tokens` from the DISPATCH-LEVEL subagent_tokens, not the agent self-report) | **Problem:** every OFS stage event (engineer/tester/cicd) landed TOKENS=0 — token coverage 18.2% of 55 events, plumbing share 0.0% — because a subagent cannot introspect its own `subagent_tokens`, so waiting for the agent to self-report yields 0. This makes the §E plumbing-vs-delivery cost-split silently zero and the §26/EXP-055 token-efficiency review (and EXP-067) UNCOMPUTABLE — the exploit lever on the engineer constraint (find build-time re-read/redundant-dispatch waste) is blind. **Solution:** the orchestrator reads each dispatch's token usage from the Task/Agent RETURN and passes it on the SAME `wi-append` that records the state event; TOKENS=0 is reserved for genuinely token-free bookkeeping transitions. | proxy: token-coverage % of stage events (justified as the enabler of the GLT-per-token control loop that protects lead time + CFR against blind token cuts) — secondary: plumbing-share becomes non-zero and trends | over the next 2 slices token coverage on dispatch-produced stage events rises from ~18% toward >80% and the plumbing share is non-zero with a cross-retro trend; if coverage stays ~0 (events still TOKENS=0 on real dispatches) the mechanism did not fire, FAILED | 2 slices (or 3 retros) | active (1/2) | ROC retro (1st opp): POSITIVE — token coverage rose from ~18% (OFS founding) to **50%** of legal-walk events on ROC, and §E `by_owner` is now populated (engineer 1.396M, cicd 663k, tester 52k; delivery 2.111M total) — the cost-split is no longer silently zero and the EXP-055 token review is computable. Not yet >80% and plumbing-share still 0.0% (no plumbing transition carried `--tokens`), so 1/2 not adopted. applies-to: every dispatched stage event. Rescues EXP-067 (cost-split starved of data) and makes EXP-055's token review computable. Founding: OFS v83 retro — 100% of stage events TOKENS=0. ‖ AdixOut retro (REQ-003, 2026-07-16): MIXED — partially firing. Engineer state events carried real TOKENS this cycle, but several cicd/tester events stamped TOKENS=0 (self-report gap), so coverage REGRESSED ~50%→~43% and the plumbing-share stayed 0.0%. Remedy: the orchestrator must stamp `--tokens` from the dispatch RETURN for ALL dispatchers (engineer, tester, cicd), not only some. Stays active (1/2). ‖ AdixOut v94 retro (2026-07-22): no scoring opportunity this cycle. Stays active (1/2). |

- **EXP-114 — ADOPTED — validated 3/3 (physically pruned v100/ROC).** painted-pixel contrast verification + no permanent axe exclusions (tester.md + ui-designer.md). Scored 3/3 POSITIVE on OFS (UC-B2/C2/G2), ADOPTED at main v98 and already in force in tester.md + ui-designer.md; the row was stale-in-table (never physically moved) and is pruned here.
  - Original row: | EXP-114 | v94 (2026-07-16) | tester.md (painted-pixel contrast + no permanent axe exclusion) + ui-designer.md (contrast conditions painted-pixel-verifiable, verify new tokens vs new adjacencies) | **Problem:** two a11y contrast incidents in one OFS session, same root cause. (1) DEF-001 — a WCAG 2.2 AA miss (Reset button 4.41:1) SHIPPED in already-done UC-A3 and stayed invisible: the project had NO axe wiring until this session, and the token nominally passed (6.1:1) while a CSS `transition` painted a low-contrast mid-flip pixel; `getComputedStyle` FALSE-GREENS (returns the declared token, not the painted pixel), so a token-based check could never have caught it. (2) UC-B1 dev-reject — a chip border token blind-aliased to the panel border (1.26:1) because contrast was never checked against the NEW adjacencies the slice introduced. Both are "contrast asserted from the token, not verified at the rendered surface", and one shipped to a done item. **Solution:** contrast is verified at the PAINTED PIXEL (Playwright screenshot → RGBA sample, node `zlib`, no new dep), settled AND across transitions/states, for every contrast clause; the page-wide axe scan runs on every UI-bearing build with NO permanent `.exclude()` selectors (an exclusion hides a real violation; only a momentary in-fix scaffold); if a project has no axe wiring, the first UI slice adds `@axe-core/playwright` as a committed gate. The ui-designer authors contrast conditions as painted-pixel-verifiable and never blind-aliases a colour token without checking it against the actual adjacent surfaces this surface places it on. | CFR (an a11y/contrast violation is caught in-loop at dev-validation or earlier — never shipped to a done item; the false-green class of contrast check is eliminated) | over the next 3 UI-bearing slices: every contrast clause is verified at the painted pixel, the page-wide axe scan runs with zero permanent exclusions, and 0 a11y contrast defects reach a done/validated item; if a contrast violation ships to done, or a contrast clause is validated by token/getComputedStyle only, FAILED | 3 UI-bearing slices (or 3 retros) | validated (3/3, ADOPTED v98) | OFS retro 2026-07-21 (2 opps, UC-B2 + UC-C2): POSITIVE ×2 — UC-B2 (ageing colour) chose linear-sRGB interpolation so contrast stays ≥4.5:1 at the painted pixel across the WHOLE scale by construction (measured 6.37–10.85:1) with monotone-luminance as a colour-blind-safe redundant cue; UC-C2 (drill-down) verified painted-pixel contrast + a focus-ring pixel-delta on the new controls. BOTH ran the page-wide axe scan with NO permanent exclusions, and 0 a11y contrast defects reached a done item (the gate caught the UC-B1 border miss at dev-validation the session it was founded). The false-green class (token/getComputedStyle) was eliminated on both surfaces. ‖ applies-to: any UI-bearing slice/defect. Founding: DEF-001 (2026-07-16, gap-closing retro) + UC-B1 border reject. Renumbered from a locally-authored EXP-106 that collided with main's EXP-106 (CORE-job done gate). Registry note: registry is over the nominal v88 cap-8 (~13 active from concurrent multi-project retros) — a dedicated prune pass is owed, tracked separately; this mandatory gap-closing row is not deferred for it. Sibling of EXP-088/EXP-087/EXP-112 — makes "did you actually check the rendered result?" an executable gate, not a nominal assertion. ‖ OFS v98 retro (3rd opp, UC-G2): POSITIVE → ADOPTED. UC-G2 (distribution chart) applied it — painted-pixel contrast on the μ/σ + no-fit text, bars-vs-curve distinguished by shape/line-style (greyscale-safe), page-wide axe clean with no permanent exclusions; 0 a11y contrast defects shipped. 3/3 all positive → behaviour already in force in tester.md + ui-designer.md (v93/v94); row retired (adopted). Keeps the registry at the cap (9→8 active). |


- **EXP-109 — ADOPTED — validated 2/2.** solution-architect authors concurrency/ordering/idempotency acceptance conditions upfront for any concurrent/event-driven surface + tester runs the concurrency/batch-durability probe as standing practice. Scored AdixOut UC-008 (1st, POSITIVE — no repeat of the UC-006 last-writer-wins race) and ROC C3 (2nd, POSITIVE — the timer-sweep vs clear-handler boundary race was authored upfront in delta 007 §2 / AC-049-5 / AC-050-6 and probed live; resolved to a single event-time-arbitrated terminal, 0 concurrency rework). In force in solution-architect.md + tester.md; row retired.
  - Original row: | EXP-109 | v86 (2026-07-12) | solution-architect.md (step 3: concurrency/ordering/idempotency acceptance conditions for concurrent surfaces) + tester.md (standing concurrency/batch-durability probe) | **Problem:** UC-ADIX-006 (SQS-triggered subscribe gap-heal) shipped a last-writer-wins data race to the 0.6.0 deploy — under concurrent/batched delivery a stale in-memory snapshot's unconditional `store.save()` regressed `lastAppliedPosition` backward (27→26, 50→49 on 2/6 real streams) and SILENTLY, permanently lost push-only content (breach of REQ-001 J3). The engineer's build was fully green because the acceptance conditions specified only a single happy-path gap-heal (+ the unfillable→DLQ negative), NOT durability under the concurrency the SQS transport inherently produces. The defect was caught ONLY because the tester improvised a batched-simultaneous-injection stressor at the deployed surface — a POST-deploy catch (CFR 25%, rework 33% for the slice) that luck, not process, provided. **Solution:** the solution-architect MUST author explicit concurrency/ordering/idempotency-under-parallelism acceptance conditions for any concurrent/parallel-invocation or event-driven-delivery component (SQS/stream/EventBridge-triggered) — name the failure modes (last-writer-wins, out-of-order/duplicate delivery, stale-snapshot clobber, state regression) and the observable that must hold under batched load; the tester runs a concurrency/batch-durability probe (simultaneous batch at the same record → consistent-read → assert no regression across EVERY affected record) as STANDING practice for such surfaces, not by instinct. Cross-agent (architect acceptance authoring + tester probe) ⇒ version bump. | CFR + MTTR (the race becomes a first-time TDD target for the engineer instead of a post-deploy tester catch → fewer rejects, no rework-and-redeploy recovery) | over the next 2 concurrent-surface use-cases (SQS/stream/EventBridge-triggered or any >1-instance-over-shared-state): acceptance.md carries ≥1 concurrency/idempotency condition AND the engineer's suite includes a concurrent-interleaving test that is green BEFORE tester validation AND no concurrency-class regression is FIRST-found by the tester post-deploy; if a concurrent surface ships with only happy-path acceptance and a concurrency/ordering/idempotency regression is again first-caught post-deploy (or in prod), FAILED | 2 concurrent-surface use-cases (or 3 retros) | active (1/2) | AdixOut retro (1st opp, 2026-07-13): POSITIVE — SLC-ADIX-004's UC-ADIX-008 (the resync THROTTLE) is a concurrent surface; the architect PROACTIVELY authored concurrency/idempotency acceptance conditions and the tester ran the concurrency-durability probe (150-request concurrent burst, aggregate byte-identical), and there was ZERO concurrency rework — no repeat of the UC-006 last-writer-wins class. The exploit move caught the class at design time on its first real test. applies-to: any use-case whose component runs >1 concurrent invocation over shared state (event-source-mapping-triggered Lambdas, parallel workers, shared-aggregate folds). Founding: UC-ADIX-006 (2026-07-12), principle-failure `2026-07-12-adixout-concurrency-race-shipped-acceptance-omitted-durability.md`. Sibling of EXP-092 (real-volume window) and EXP-110 (unrun-tier) — all three say a green build is only as complete as what its acceptance+tests exercise. EXPLOIT move on the AdixOut constraint (engineer rework 19.4% active GLT, 33% rework) — removes the rework at source. ‖ AdixOut retro (REQ-003, 2026-07-16): no opportunity — REQ-003 was pure AIDX mapping with no concurrent/parallel-invocation surface. Still 1/2 positive. Stays active. ‖ AdixOut v94 retro (2026-07-22): no scoring opportunity this cycle. Still 1/2 positive. Stays active. ‖ AdixOut v99 retro (2026-07-22): no direct concurrency-surface score this cycle, but the v99 edge-protection-real-payload fold (tester.md + solution-architect.md) is an explicit SIBLING of this family — a green build is only as complete as what its acceptance actually exercises. Still 1/2 positive. Stays active. ‖ AdixOut v101 retro (2026-07-23, REQ-005 Chunk B UC-ADIX-019): HEAVY WORKOUT / holds. The dynamic per-customer auth surface exercised the concurrency/idempotency conditions this row authors and the CONCURRENCY handling HELD (no last-writer-wins / regression class). What surfaced instead is a NEWLY-identified SIBLING gap — resource-SET completeness + migration/self-heal (a fingerprint idempotency short-circuit skipped ensuring the full per-customer resource set for pre-existing rows), caught across 3 dev-validation cycles. NOT a failure of this row (single-resource idempotency held); the sibling gap is now covered by the v101 plain-practice fold (solution-architect.md enumerate-the-per-customer-resource-set + self-heal/migration acceptance; tester.md re-apply-heals-a-pre-existing-customer) — NO new experiment row forced. Still 1/2 positive. Stays active. |



- **EXP-116 — ADOPTED — validated 2/2 (guards); lead-time claim under-exercised.** Guarded lean orchestration: for small, well-understood work INSIDE an already-signed-off slice the orchestrator MAY author obvious decomposition-gap UCs (inheriting the parent's signed-off persona/job, no new scope) and centralise stage bookkeeping, under five hard guards (G1 persona/job carried; G2 no product/arch decision; G3 never hand-crank a code fix; G4 mandatory live-stack tester E2E per UI/pipeline slice; G5 centralised appends carry TOKENS). Scored POSITIVE twice (ROC v103 + v109): the literal pass condition — lead-time median does NOT rise AND all five guards hold with 0 breaches — held both cycles (CFR/rework 0 harm). HONEST LIMIT: the named lead-time-REDUCTION metric stayed under-exercised because the lean-authoring trigger did not recur (new-scope requirements correctly went through the full discovery+product gate); what is validated is the SAFETY property — the leaner loop holds every guard with zero DORA harm. INTEGRATED into orchestrator.md as plain operating practice ("Lean orchestration (guarded — plain practice)"), EXP/trial scaffolding removed, guards intact. Registry 8→7.
  - Original row: | EXP-116 | v100 (2026-07-23, ROC) | orchestrator.md (lean-orchestration trial, guarded) | **Problem:** on ROC the orchestrator ran a LEANER loop than the letter of §F — it authored small decomposition-gap use-cases directly (UC-ROC-024/025/026/036) rather than a full `product` dispatch per UC, and centralised all stage-event `wi-append`s. The user authorised validating this AS AN EXPERIMENT ("you can be pragmatic if it's an experiment and we're validating if it's better or not"). Open question: does lean orchestration cut registration/coordination latency (the #1 GLT contributor — `registered`/`queue` = 65% this cycle) WITHOUT losing the guarantees the specialist roles provide? An early NEGATIVE signal already surfaced: centralised appends REGRESSED token-coverage 50%→19.5% (the killed EXP-103) because appends landed `TOKENS=0`. **Solution:** for small, well-understood work INSIDE an already-signed-off slice the orchestrator MAY author decomposition-gap UCs (inheriting the parent's signed-off persona/job, introducing NO new scope) and centralise bookkeeping — under FIVE HARD GUARDS; any breach = revert that class to full role dispatch: **(G1)** every orchestrator-authored UC carries persona/job from the signed-off dossier; **(G2)** NO product/architecture DECISION (new scope/persona/tech choice) is taken by the orchestrator — those still dispatch product/solution-architect; **(G3)** NO code fix is EVER hand-cranked by the orchestrator — every bug is a `/defect` built by engineer + validated by tester (the v98 rule + principle-failure `2026-07-22-orchestrator-hand-cranked-fix` STAND, explicitly NOT relaxed by this trial); **(G4)** every UI/pipeline slice still gets a real live-stack tester E2E (stand up the FE + push data through the running pipeline, EXP-115 family); **(G5)** every centralised stage append carries the dispatch-return `TOKENS` (absorbs EXP-103 — no coverage regression). | gross lead time (registration/coordination latency on the `registered`/`queue` constraint FALLS) | over the next 3 slices: lead-time median does not rise (target: falls) AND all five guards hold with 0 breaches — 0 orchestrator-authored UCs missing persona/job, 0 orchestrator-taken product/arch decisions, 0 hand-cranked code fixes, every UI/pipeline slice live-E2E'd, token-coverage does NOT regress vs this cycle; if lead time does not improve OR any guard breaks, FAILED (revert to full role dispatch) | 3 slices (or 3 retros) | active (1/3) | applies-to: ROC (and any project trialing lean orchestration). ‖ ROC v103 retro (1st opp): POSITIVE — 2 slices delivered; guards held (G4 strong: every UC live-validated, UC-050 drove the real poller process; G5 IMPROVED: orchestrator-fires-all-stage-appends-with-tokens fixed the UC-046 self-append TOKENS=0 gap; G3 held: 0 hand-cranked fixes; G1/G2: lean-authoring fired once cleanly on UC-046 and correctly NOT on C3 new-chunk). HONEST LIMIT: primary lead-time-reduction claim under-exercised (C3 bulk correctly bypassed the lean path); what's validated is the guards hold with no DORA harm (CFR/rework 0%). Founding: user authorisation 2026-07-23 + honest self-assessment that the ROC loop ran leaner than §F. FENCES OFF the known-forbidden hand-crank pattern (G3) — this trial is ONLY about authoring obvious decomposition-gap UCs + centralised bookkeeping, never skipping engineer/tester or making product/arch calls. Absorbs killed EXP-103's token-stamping as G5. |


- **EXP-117 — ADOPTED — validated 3/3.** Board-push cadence: push the board at MEANINGFUL transitions only (pulled / blocked-unblocked / terminal validated-done-rejected-resolved) + the periodic step-5b full-sweep as the reconciling backstop; skip the intermediate built_green/deployed/dev-validating per-item pushes. Scored POSITIVE 3× (ROC v109 1/3, v111 2/3, v112 3/3): terminal-only pushes + sweeps held board fidelity across the entire config-authoring effort (In-Progress lane honest every cycle, incl. after the v111 blocked-mapping machinery fix), plumbing board-token cost per delivered UC materially below the ~4-pushes-per-UC C3 baseline. INTEGRATED as standing practice in .claude/commands/loop-run.md (step 4/5b) + orchestrator.md; experiment scaffolding removed. Registry 7→6.
  - Original row: | EXP-117 | v103 (2026-07-23, ROC) | .claude/commands/loop-run.md (board-push cadence) + orchestrator.md | **Problem:** the mandated per-item board push (loop step 4, after ANY state change) was the single largest PLUMBING token cost of the C3 session — ~18 `linear` projection dispatches at ~15–20k tokens each (~290k total) mirroring registered→building→dev-validating→done for each UC, most of it re-rendering the same issue through transient intermediate states (built_green/deployed/dev-validating) that a human watching the board barely distinguishes from "In Progress". Delivery agents (engineer/tester) are the right place to spend tokens; board mirroring is pure plumbing (§24/EXP-055 — cut plumbing tokens per delivered UC WITHOUT losing board fidelity). **Solution:** push the board at MEANINGFUL transitions only — `pulled` (work started), `blocked`/`unblocked`, and any TERMINAL state (`validated`/`done`/`rejected`/`resolved`) — plus the periodic step-5b full-sweep as the reconciling backstop; SKIP the intermediate `built_green`/`deployed`/`dev-validating` per-item pushes (they collapse to the same "In Progress" band). | plumbing token-share per delivered UC (a §24 DORA-cost-per-token reduction) — GUARD: board fidelity (a TERMINAL/blocked state must never lag its item-file state by >1 cycle) | over the next 3 slices: plumbing board-push token cost per delivered UC falls materially vs this session's ~4-pushes-per-UC baseline, AND 0 incidents where a TERMINAL (done/validated/rejected/resolved) or blocked state is wrong/stale on the board for >1 cycle (the step-5b sweep catches drift); if board fidelity regresses OR the token cost does not fall, FAILED | 3 slices (or 3 retros) | active (2/3) | applies-to: every loop board push. Founding: v103 ROC token review — board projection was the single largest plumbing cost of the C3 session. Opened cap-neutral against EXP-109's adoption this retro (retire-one-open-one, cap-8). The step-5b full-sweep already exists as the structural backstop, so skipping intermediate pushes cannot silently lose terminal fidelity. ‖ ROC v109 retro (1st opp): POSITIVE — SLC-ROC-013 close mirrored the board with per-item terminal pushes plus ONE full-sweep dispatch (86 items, ~19.5k tokens total) vs the ~290k/4-pushes-per-UC C3 baseline; terminal fidelity intact (SLC-013 + UC-054/055 all Done on Linear, 0 stale-terminal incidents). Token cost per delivered UC fell materially; guard held. ‖ ROC v111 retro (2nd opp): POSITIVE — SLC-ROC-014 close used terminal-only per-item pushes + 2 full-sweep reconciles (incl. the blocked-mapping fix); In-Progress lane stayed truthful (2 items, no stale terminal/blocked), plumbing board tokens low. 2/3. |

- **EXP-106 — RETIRED as SUPERSEDED — the INTENT is inherited, the MECHANISM is falsified (1/3 across 3 projects, 4 recurrences of the class it exists to prevent).** CORE-job done-gate: a CORE-`job` aggregate is "done-in-fact" only when acceptance is validated against that job's success measure for the named persona(s), and a deliberately-partial CORE slice must register its undelivered remainder before closing. **Why retired rather than continued:** the hypothesis was right and the mechanism cannot work, for a structural reason this retro finally named. The check runs **at slice close** — the moment when a just-shipped capability has had NO opportunity to occur — so the discriminating evidence (a real event of that type in the store) does not yet exist. It is therefore blind to the dominant form of the failure by construction, which is exactly what the record shows: it scored NEGATIVE at its 1st opportunity (ROC `CHK-ROC-001` folded `done` on a fake-Jira child), was twice recorded as "no opportunity" (AdixOut REQ-003, v94), and then at OAG v125 the class recurred a **fourth** time in its most extreme form — **five** capabilities reading `done`/`validated` while never once working on real data (`OagFlightCancelled` 0/10,519,584 events; `departure.scheduledTimeUtc` 78% null and never read; `irregularOperationType='Recovery'` 0 captures; `OagFlightDiverted` 0/5,300,655; `deriveAirports()` structurally unable to route a diversion airport), all green on an 1,804-case suite, with CFR and rework reading clean throughout — the precise blindness EXP-106 was opened to remove. Two further diagnoses from the same retro: its enforcement half (**IMP-011**'s `wi-validate` I5) never landed, so for four cycles the gate was operator memory; and validating "against the job's success measure" is itself a PROSE obligation, indistinguishable when unmet (process v125 §17c Layer 2). **Disposition:** intent + CFR metric inherited by **EXP-122** (observed-or-not-established), which moves the check from the close BOUNDARY to a CADENCE over real data and replaces the success-measure narrative with an observation pointer to a real record the system did not author, plus the `awaiting_observation` state for the honest interim. §12d is retained and AMENDED with that third obligation (it is now a standing rule, not a hypothesis); IMP-011's I5 remains owed and is folded into EXP-122's machinery item. Retired at OAG v125 to open EXP-122 under the hard cap-8 (retire-one-open-one), metric inherited — not a cap-game. Registry 8→7 then 7→8.
  - Original row: | EXP-106 | v84 (2026-07-11) | process-current.md §12d + product.md + tester.md + documenter.md | **Problem:** an aggregate's `done` folds structurally (all children `done`), with NO linkage to the job's success measure or persona — so a slice can be `done` while its CORE job is undelivered, and the undelivered remainder can fall off the backlog silently. SLC-030 (`job: J0` CORE, "live delivery to consumers") was marked `done` having built the bus SAME-ACCOUNT only; the cross-account CORE remainder was registered nowhere, `items/active/` read empty ("CORE-DELIVERED"), and the pull-vs-push delivery-model inversion then propagated into the consumer-facing skill. CFR read 0.0% and rework 0.0% the whole time — the metrics were blind to an escaped requirement-level defect. **Solution:** §12d — a CORE-`job` aggregate is "done-in-fact" only when acceptance is validated against that job's success measure for the named persona(s) (product anchors it, tester validates against it); and a deliberately-partial CORE slice MUST register its undelivered remainder as a tracked item before it closes (flow-manager confirms). Plus: consumer-facing docs/skills state their "primary path" by tracing to the authoritative delivery model, never a peer/derived doc (documenter). | CFR + MTTR | 3 CORE-job slices (or 3 retros) | active (1/3) at retirement | ROC retro (1st opp): NEGATIVE — the gap recurred on a third project; root cause = the gate is TEXT enforced by operator memory, not mechanical; queued IMP-011 (wi-validate I5). ‖ AdixOut REQ-003 + v94: no scoring opportunity (no CORE-partial slice). ‖ OAG v125: 4th recurrence of the class, mechanism falsified as close-time-blind → superseded by EXP-122. |

- **EXP-115 — ADOPTED — validated (5 positive opportunities across 3 projects), with its boundary stated.** Whole-journey / real-shipped-artifact validation: any data artifact the project ships to be loaded (sample/demo/seed/fixture) is a VALIDATED artifact with a committed test that loads THAT FILE through the public surface and drives the primary journey to a real terminal outcome; "verified/done" means the whole journey was executed+observed at the public surface, driving the REAL human entry point (demo.sh/run script/URL) derived the way it does, not a harness copy. Scored POSITIVE at OFS v98 (1/3, recurrence at the entry-point seam → strengthened), AdixOut v106 (3 latent defects found on the account migration before any prod escape), AdixOut v109 (orphaned-integration + 2 EventBridge→SQS delivery bugs a full synthetic suite passed), AdixOut v111 (an ENTIRE orphaned integration: OAG actually fans Aerobus→a shared consumer bus with a different source/detail-type/envelope), AdixOut v118 (the DEF-AIDX-008 real-scale egress outage + the AIDX UFI OriginDate drift + the prod-fixture leak). BOUNDARY, recorded at OAG v123 and NOT counted positive: it validates that the JOURNEY works with real artifacts — it does not ask whether every output a change can emit ACTUALLY OCCURS over real traffic, so a never-firing branch is indistinguishable from a quiet day (DEFECT-OAG-041/042). That limb is EXP-120's (output liveness), which is where the class continues to be measured. INTEGRATED in tester.md as standing practice; trial scaffolding removed. Retired at OAG v124 to open EXP-121 under the hard cap-8 (retire-one-open-one). Registry 8→7 then 7→8.


## OAG retro v126 (2026-08-01) — 3 rows retired (1 adopted, 2 killed)

_Registry 8 (AT cap) → 5, then → 6 with EXP-123 opened. The cap was the forcing function; the scoring was done against `views/stats.md`, not against how old the rows were._

### ADOPTED — behaviour sound + load-bearing + in force; row pruned, behaviour kept as plain agent/process practice

- **EXP-101 — ADOPTED — the metric moved, and it had been moving for 26 days while the row sat unscored at `0/2`.** Dev-then-prod validation: `deploying`(deploy-to-dev) → `dev-validating` --`dev_validated`(tester)--> `prod-deploying` --`promoted`(cicd)--> `prod-validating` --`validated`(tester)--> `done`, where `dev_validated` triggers the prod deploy automatically (no human gate, an automated promotion assurance like §F5a). **Evidence it worked:** the path FIRED — `prod-deploying` n=11 and `prod-validating` n=11 in `views/stats.md` §C. And the hypothesis' discriminating comparison came out exactly as predicted: `dev-validating` failure rate **1.1% (2/175)** against `prod-validating` **0.0% (0/11)**. Two validation failures were caught as contained dev-stage catches; **zero** reached prod. That is the CFR claim, satisfied on real transitions. **Why it read `0/2`:** nobody ever scored it. The row was carried as unvalidated for 26 days across multiple retros while its mechanism was quietly doing its job — which is itself the cycle's headline failure (an obligation with no mechanism is not performed; scoring the registry was documented, `retro-debt` was mechanised, and only the mechanised one happened). **Disposition:** the behaviour is already load-bearing in process §11b/§F5a, `tester.md` and `cicd.md`; strip the vNN/EXP trial scaffolding and keep it as plain operating practice. Retired to bring the registry under the hard cap-8. Registry 8→7.
  - Original row: | EXP-101 | v82 (2026-07-06) | process-current.md §11b + §F5a + tester.md (dev-then-prod validation) + cicd.md (deploy-to-dev then deploy-to-prod) | **Problem:** §F5a referenced a "dev acceptance stage" that was a PHANTOM — no such state existed in the use-case graph; the actual path (`building → deploying → validating → done`) validated ONLY in prod, so a broken change was first observed by real customers in production instead of being caught in dev first. This violates the vital "dev-first, acceptance before prod" principle: there was no de-risking dev validation before the prod deploy, so every validation failure was a prod CFR hit rather than a contained dev-stage catch. **Solution:** the locked dev→prod path `deploying(deploy-to-dev) → dev-validating --dev_validated(tester)--> prod-deploying --promoted(cicd)--> prod-validating --validated(tester)--> done` — cicd deploys to DEV (`deployed`), the tester dev-validates against the ORIGINAL FROZEN acceptance.md (the dev-validation oracle) and fires `dev_validated`, which AUTOMATICALLY (no human gate — an automated promotion assurance like §F5a infra auto-approve) triggers cicd's prod deploy (`promoted`), then the tester prod-validates (`validated`). Local-only projects collapse dev==prod (validate straight from `dev-validating`). | CFR (a defect is caught in the dev-validation stage BEFORE it reaches prod, so it is a contained dev catch, not a prod deploy-failure) + MTTR (a dev-stage catch has no prod-blast recovery) | over the next 2 cloud/hosted slices, every UC is dev-validated against the frozen acceptance.md BEFORE the prod deploy AND ≥1 validation failure is caught at `dev-validating` (a `rejected` from dev, no prod impact) with 0 rejected-in-prod that a dev-validation would have caught; if a UC reaches prod-validating without a passing dev-validation, or a prod deploy-failure occurs that the frozen dev-oracle would have caught, FAILED | 2 cloud/hosted slices (or 3 retros) | active (0/2) | applies-to: any cloud/hosted (non-local-only) slice whose UCs deploy. Depends on the state-graph amendment (dev-validating/prod-deploying/prod-validating states, owned by a parallel agent). Honours the user's "dev-first, acceptance before prod" principle. Distinct from EXP-091 (tester-validates-in-prod safety net still stands — this ADDS the dev-first validation ahead of it) and EXP-099 (deploy as a first-class cicd-owned stage — this splits it into dev+prod legs). ‖ AdixOut retro (REQ-003, 2026-07-16): still 0 real opportunity — REQ-003 was AdixOut sandbox-only (no cloud dev→prod promotion). Kept — first real opportunity expected on the dev-shared/prod-shared bus ingestion thread (imminent), which WILL be its first real cloud dev→prod opportunity. ‖ AdixOut v94 retro (2026-07-22): PARTIAL POSITIVE / FIRST real opportunity — REQ-005 Chunk A deployed to a REAL hosted dev env (dev-shared) and was dev-validated THERE, the first non-local dev validation (the local-only collapse no longer applies). BUT prod is DEFERRED for REQ-005, so the dev→prod PROMOTION leg did not run — `validated` fired directly from `dev-validating` (deferred-prod collapse). Dev-first validation on real infra is now DEMONSTRATED; the full dev→prod promotion still awaits a prod-in-scope slice. Stays active. ‖ AdixOut v99 retro (2026-07-22): POSITIVE data point — the WAF XML-body false-positive (UC-ADIX-017) was CAUGHT IN DEV validation and fixed in rework before any prod exposure, exactly the dev-first containment this experiment is for. Prod promotion leg still deferred for REQ-005, so no dev→prod promotion to score; stays active (0/2). ‖ AdixOut v105 retro (2026-07-24, REQ-005 COMPLETE): STRONG POSITIVE data, but the measure does NOT yet permit adoption. REQ-005's full external AIDX egress (13 UCs + DEF-ADIX-003) was validated on the REAL hosted dev env (dev-shared) end-to-end, and the adversarial live/JTBD validation CAUGHT REAL DEFECTS IN DEV before any prod exposure (the DEF-ADIX-003 bad-state chain, the WAF XML false-positive, the UC-019 dynamic-key rework) — this is exactly the dev-first containment the experiment is for, now heavily demonstrated. BUT prod stays deferred for REQ-005, so the dev→prod PROMOTION leg the measure requires still has not run (`validated` fires from `dev-validating` under the deferred-prod collapse). NOT an adopt yet — the measure explicitly needs the promotion leg over 2 cloud/hosted slices. Kept active (0/2) with the strong dev-first positive; first real dev→prod promotion still awaits a prod-in-scope slice. |

### KILLED — no measurable effect / never validated (mechanism falsified by direct observation)

- **EXP-107 — KILLED — never validated in 20 days, and its target metric is the WORST in the system.** Infra-bearing push gate: the push-on-green done-condition for `sst.config.ts`/`infra/`/IaC/deploy-role changes should include the infra checks, so deploy-failures are caught pre-push rather than becoming CI deploy-failures. **Why killed:** unscored at `0/3` since 2026-07-12, and the evidence points the wrong way. `views/stats.md` §C puts `deploying`/cicd at a **6.7% failure rate (13/195)** — the highest failure rate of ANY stage — while `deploying` accounts for **18.16% of gross lead time**. Thirteen deploy failures reached the deploy stage over the row's life; the pre-push catch was never demonstrated once. A hypothesis whose target metric is both unmoved and the system's worst, after 20 days and three scoring opportunities, is killed under the 3-strikes rule rather than carried. **Explicitly NOT re-registered.** Re-registering the same idea a third time is the prose-remedy failure §17c exists to prevent — the honest move is to treat a 6.7% deploy-failure rate as work needing a root-cause pass, not as another hypothesis. Routed to an improvement slice. Registry 7→6.
  - Original row: | EXP-107 | v86 (2026-07-12) | process-current.md §14 + engineer.md (infra-bearing push gate) | **Problem:** the push-on-green done-condition was unit + lint only. For infra-bearing changes (`sst.config.ts`/`infra/`/IaC/deploy-role policy) CI auto-deploys, so green-locally-on-unit-lint is NOT the green CI enforces — an infra statement that passed offline shape-tests was rejected at the AWS API on deploy (EventBridge `PutPermission` invalid-principal), turning the shared infra CI pipeline red (UC-XA2 first push ec56025), fixed forward at 76a7e58. Same "green in the cheap check ≠ correct where it matters" family as v84 / the 2026-06-23 false-green class. **Solution:** infra-bearing push gate — a change touching sst.config.ts/infra/IaC/deploy-role policy is not push-green on unit+lint alone; the pre-push done-condition MUST include `make deploy-sst` (or `sst diff`/synth) passing locally before push. Routed to process-current.md §14 (cross-agent) + engineer.md (woven into push-when-done). | CFR (infra deploy-failures caught pre-push instead of turning CI red post-push) | over the next 3 infra-bearing pushes: each runs the synth/deploy gate BEFORE push and 0 turn the infra CI pipeline red for a cause the local synth/deploy would have surfaced; if an infra push goes red in CI for a synth/deploy-detectable cause, FAILED | 3 infra-bearing pushes (or 3 retros) | active (0/3) | applies-to: any push touching sst.config.ts/infra/IaC/deploy-role policy. Founding incident: principle-failures/2026-07-12-infra-pushed-green-locally-red-in-ci. Sibling of EXP-101 (dev-first validation) — this is the pre-PUSH analog (catch the deploy failure before it reaches CI at all). ‖ AdixOut retro (REQ-003, 2026-07-16): no opportunity — REQ-003 was domain-mapping only, no IaC/sst.config.ts/deploy-role touched, so no infra-bearing push occurred. Stays active (0/3). ‖ AdixOut v94 retro (2026-07-22): NEGATIVE/BOUNDARY — REQ-005 Chunk A had 3 infra-bearing changes and the local synth/`sst diff` gate did NOT catch any of the 3 real deploy failures (UC-014 iam:CreateRole least-priv gap; UC-016 WAFv2 description-charset ValidationException; UC-016/DEF-ADIX-002 SST `$transform` no-op on the ApiGatewayV1 Stage — where `sst diff` even FALSELY showed the tag applying). HONEST BOUNDARY recorded: local synth catches SHAPE errors only, NOT real-control-plane failures (least-priv IAM, service-API validation, child-construction-order) — those need the real CI apply + live post-deploy verification (fold 1a). NOT killed (running `sst diff` cheaply is still worth it), but its measure must NOT claim to catch the real-control-plane class; its value is now SUBORDINATE to EXP-108 (record the failure) + the live-verify practice (tester fold 1a). Stays active (0/3). |

- **EXP-112 — KILLED — the mechanism was falsified by direct observation during this very cycle.** Dependency-vulnerability audit gate: a high/critical advisory should be caught at the next push via a `make audit` target the cicd agent reads. **Why killed:** unscored at `0/3` since 2026-07-13, and this cycle produced a clean negative. A push to `origin/main` returned, in its own output, *"GitHub found 11 vulnerabilities on AeroCloudSystems/FlightEventSource-OAG's default branch (11 high)"* — and `OI-DEPENDABOT-HIGH-SEV` is still sitting open in the intake queue. The advisories are live, the pushes happened, and nothing was caught. That is not "no opportunity to score"; it is a measurement that came back NEGATIVE. Per §25a a row whose measurement has already returned negative must not continue as `active`. **Disposition:** the exposure is real and stays tracked as the open-item — it is a piece of work, not a hypothesis, and should never have carried a registry row (EXP-063 validity bar: a fix is not an experiment). Registry 6→5.
  - Original row: | EXP-112 | v91 (2026-07-13) | cicd.md (dependency-vulnerability audit gate) + per-project `make audit` target | **Problem:** DEF-ADIX-001 — vulnerable dev/build/test dependencies accumulated SILENTLY across the whole first requirement with NO signal any agent reads: a **CRITICAL** vitest UI-server arbitrary-file-read/exec advisory, a HIGH vite `server.fs.deny` bypass, and 3 MEDIUMs sat open in BOTH lockfiles (`package-lock.json` + `src/app/package-lock.json`) until the human pointed at the GitHub Dependabot banner. Nothing in the loop ever ran an audit, so supply-chain risk grew unbounded between deploys — a change-failure waiting to surface, invisible to CFR. **Solution:** a standing, committed dependency-audit gate — a `make audit` target running `npm audit --audit-level=high` in EVERY manifest (root + each sub-package), wired into the build/push gate cicd owns (alongside lint/test); a high/critical advisory is a gate failure and becomes a `DEF-` through intake (dev-only advisories still fixed but flagged no-prod-exposure for correct prioritisation); a toolchain bump to remediate is verified green across all tiers (EXP-110) before push, never pinned back. | CFR (a high/critical advisory is caught at the next push instead of accumulating unaddressed across a whole requirement) + MTTR (a caught-early vuln is a cheap bump, not a critical-severity scramble) | over the next 3 cycles with an npm project: `make audit` runs as part of the build/push gate AND 0 high/critical advisories accumulate UNADDRESSED across a cycle boundary (each is either fixed or registered as a triaged DEF- within the cycle it appears); if a high/critical advisory sits open across a full cycle with no audit-gate signal, FAILED | 3 cycles (or 3 retros) | active (0/3) | applies-to: any npm/dependency-bearing project (extends naturally to other ecosystems' audit tools). Founding: DEF-ADIX-001 (2026-07-13), principle-failure `2026-07-13-adixout-dependency-vulns-accumulated-no-audit-gate.md`. Opened cap-neutral against EXP-102's adoption this retro (retire-one-open-one, v88 WIP cap). Sibling of EXP-088 (render-diagrams gate) / EXP-087 — all make "did you actually check?" an executable committed gate, not a banner someone must notice. ‖ AdixOut retro (REQ-003, 2026-07-16): light / no-opportunity — npm project but no new dependencies added this cycle, so no new advisory surfaced; `make audit` remains wired into the build/push gate. Stays active (0/3). ‖ AdixOut v94 retro (2026-07-22): LIGHT POSITIVE — `make audit` ran in CI on every push this cycle and reported 0 high/critical vulns; the gate is executing as intended (no advisory accumulated unaddressed). Not yet a full horizon hit (no advisory to catch), so not adopted. Stays active (0/3). |

- **EXP-118 — ADOPTED — the metric moved POSITIVE twice and the behaviour is now plain ui-designer practice.** Data-visualization faithfulness as a TESTABLE condition: every visual mark must quantitatively represent its datum with no silent clamp, truncation or renormalization. **Why adopted:** scored POSITIVE x2 at the OFS v115 retro (UC-I2 bin slider — the fitted curve stays a faithful density across every bin count as maxCount varies, mu/sigma invariant, no flat-clip, asserted by sampling the rendered path; UC-J2 Animate — occupancy/ageing/distribution stay faithful frame-by-frame from 2/s to 1e6/s). Both validated the depicted RELATIONSHIP at the painted surface rather than mere presence, which is the whole claim. **Integration:** already woven into `ui-designer.md` §3b and `tester.md` (validate the depicted relationship) as core operating practice; this retro removed the remaining experiment scaffolding (the `(v114, DEF-004)` citation) so the rule reads as practice, not as a trial. Founding defect DEF-004 (2026-07-28): a fitted log-normal overlay was scaled to the tallest histogram bar and clamped at the plot top, rendering a FLAT TOP, and was signed off as "by-design (hump preserved)" because no acceptance clause required the curve to be a faithful density. Retired to free the cap-8 slot for EXP-128.
  - Original row: | EXP-118 | v114 (2026-07-28) | ui-designer.md §3b (data-visualization faithfulness — TESTABLE) + tester.md (validate the depicted relationship) | **Problem:** DEF-004 — the fitted log-normal overlay on the distribution chart rendered a FLAT TOP: `buildCurvePath` scaled the curve to the tallest histogram bar (`maxCount`) and CLAMPED any point above the plot to `PLOT.top`, so whenever the true peak exceeded the bars the curve clipped flat — MISREPRESENTING the fit P1 must judge by eye (J12). It shipped because the chart's acceptance (G3) asserted geometry + a11y + "a hump is present" but NO clause required the curve to be a FAITHFUL density; the clamp was even rationalized as "by-design". A viz that "fits in the box" by distorting the data passed as done. **Solution:** ui-designer §3b — for any chart, emit a checkable FAITHFULNESS condition per encoded quantity (marks quantitatively represent their datum with no silent clamp/truncation/renormalization that changes the reading; series share an honest common scale sized to hold all series; a computed overlay depicts the source data's true shape — single real peak, correct tail — asserted by sampling rendered marks, e.g. one interior maximum / no ≥3-point top plateau). tester validates the DEPICTED RELATIONSHIP at the painted surface, not just that the chart renders. | CFR (a data-viz distortion defect is caught at acceptance/dev-validation, never shipped to a done item) | over the next 3 chart-bearing slices/defects: each chart's acceptance carries a faithfulness condition per encoded quantity AND 0 viz-distortion defects (clamp/renormalization/truncation that changes the reading) reach a done item; if a chart ships with a mark that distorts its datum, or a chart acceptance asserts only geometry/a11y/presence, FAILED | 3 chart-bearing slices (or 3 retros) | active (2/3) | OFS v115 retro (2 opps, UC-I2 + UC-J2): POSITIVE ×2 — UC-I2 (bin slider) keeps the fitted curve a faithful density (μ/σ invariant; no flat-clip; asserted by sampling the rendered path) across every bin count as maxCount varies; UC-J2 (Animate) keeps occupancy/ageing/distribution faithful frame-by-frame at 2/s→1e6/s. Both validated the depicted relationship at the painted surface, not just presence. ‖ applies-to: any chart/plot/graph surface. Founding: DEF-004 (2026-07-28, gap-closing retro). Sibling of §3a (visual-structural geometry) — 3a says "shape that carries meaning is asserted as geometry"; 3b says "quantity that carries meaning is asserted as faithful magnitude". Extends the EXP-114 painted-pixel + EXP-115 real-journey family from "is it rendered/accessible?" to "does it tell the truth about the data?". |

- **EXP-124 — ADOPTED — the metric moved, and it moved by the only route the founding ruling allows.** The AC-traceability / authored-precondition gate: a test that names no acceptance criterion is either waste or an undiscovered acceptance criterion. **Why adopted:** shipped as a committed RATCHET on 2026-08-02 with a measured baseline of limb1 = 1,801 untagged of 2,728 cases and limb2 = 22 authored preconditions. On 2026-08-06 the floor was re-baselined DOWN to **limb1 = 1,773 / limb2 = 15** — DEFECT-OAG-063's work lowered untagged by 22 while adding ZERO, and UC-OB1's earlier work lowered it by 12, both measured by reverting the author's own files. Critically it moved by **work that named its criteria**, not by mass-tagging, which the row itself declared would count as FAILED. It was then observed holding twice more in the same session: an engineer adding **82 new cases, all AC-tagged**, left both limbs exactly at floor. **Integration:** the behaviour is enforced by MACHINERY rather than by agent prose — `.claude/tools/test-requirement-gate.js`, `make test-requirement-gate`, `loop-gate` check 6, and the committed per-project baseline that `make test-requirement-gate-baseline` refuses to raise. That is the strongest form of integration available: no agent has to remember it, and the floor can only shrink. Proof-of-fire was recorded at landing (limb 2 observed RED against the real pre-fix UC-HF041 shape and CLEAN against the corrected one — the gate distinguishes the fix from the bug) and re-observed since. Founding: the human ruling of 2026-08-02, *"the ONLY thing tests should be validating is the requirements."* Retired to hold the registry at the hard cap of 8 while EXP-129 opens.
  - Original row: | EXP-124 | v127 (2026-08-02, OagEventSource) | process-current.md §17d + the AC-traceability / authored-precondition gate (committed, scheduled lane) + engineer.md + tester.md | **Problem:** tests are written to agree with the code, so a green suite is evidence about the tests and not about the system. Human ruling, verbatim: *"A test was written to match the code. I do not care AT ALL about code coverage. The ONLY thing tests should be validating is the requirements. If we are making up tests for coverage that do not map onto requirements then either (a) we are wasting time, or (b) we have identified a new acceptance criteria and we need to retro as to why it wasn't discovered earlier."* THREE independent instances in ONE session, which is what makes it a pattern rather than an incident: (1) `uc-hf041-cancellation-recovery.test.ts` built its "pre-fix" prior by re-ingesting a REAL capture **with `statusDetails[].state` deleted** — precisely the leaf whose presence breaks the heal — with three siblings hand-setting `{state:'Cancelled'}` and asserting suppression; **2,171 tests green while nine real cancellations sat unhealed in prod** on the passenger-facing feed, including the flight the customer reported; (2) the `awaiting_observation` probe test stubbed `subprocess.run` and so, in the engineer's own words, "only proved the mapping agreed with itself" — against a real `make` every probe read BROKEN; (3) the provenance ledger's `read` dispositions were DECLARED and, when tested differentially against `normalise()`, **8 were false**. This is §17c Layer 1 turned on the suite itself: a test whose INPUT we authored cannot come back negative about reality. **Solution:** §17d, made executable by a gate with two limbs — **limb 1** every test declares the `AC-<ID>.<n>` it validates, and an untagged test is forced to a binary choice (delete as waste, or register as a discovered acceptance criterion which also opens a discovery retro); **limb 2** a precondition may not be AUTHORED — a test that builds its prior by mutating a real capture (deleting a field, spreading an override, hand-setting a folded value) is flagged, and the prior must instead be folded from events or harvested. Plus: never stub the seam under test (that is instance 2), and coverage is explicitly not a goal and never a justification. | CFR (a defect of the "green suite, broken on real data" class is caught by the gate instead of by a customer or a prod census) | over the next 3 slices/defects: limb 2 is observed RED at least once against a real authored-precondition test, the untagged-test baseline RATCHETS DOWN and never up, and 0 defects of this class reach a customer or a `done` item; if a defect of this class escapes again with the gate in place, or the baseline is satisfied by mass-tagging rather than by deletion/AC-registration, FAILED | 3 slices/defects (or 3 retros) | active (0/3) | applies-to: every project with a test suite. Founding: the human ruling above plus the three instances. Deliberately falsifiable in the direction that matters — mass-tagging to clear the baseline counts as FAILED, not as success, because it would reproduce the coverage-theatre the ruling rejects. Note the gate must ship in a workflow that ACTUALLY RUNS: `make render-diagrams` has been red on trunk ~20 days over 3 committed `.mmd` files precisely because it runs in no workflow, and a noisy gate gets ignored, so limb 2 favours precision over recall. Opened with the registry at 6 of the hard cap-8. **MECHANISM LANDED 2026-08-02** (`.claude/tools/test-requirement-gate.js`, `make test-requirement-gate`, `loop-gate` check 6, config+allowlist+baseline in `.claude/config/test-requirement-gate/<P>.json`). **SCORING BASELINE for OagEventSource, measured not estimated:** limb 1 = **1,801 untagged of 2,728 test cases (66%)**, of which **606** carry only a file-level `@covers AC-x`; limb 2 = **22** authored preconditions across **9** files (`spread-override` 8, `mutate-real-capture` 5, `authored-derived-prior` 4, `delete-on-real-capture` 4, `exec-boundary-stubbed` 1); allowlist = **1 entry** (machinery invariant tests) suppressing 47. Shipped RATCHET, not enforce, because turning trunk red for 1,801 untriaged cases would teach everyone to ignore it. PROOF OF FIRE (§17c) recorded: limb 2 observed RED against the ACTUAL pre-fix UC-HF041 shape recovered from git (4 hits) and against the `subprocess.run` stub (1 hit), and observed CLEAN against the CORRECTED folded-from-events version of the same file — the gate distinguishes the fix from the bug; limb 1 observed RED against one genuinely untagged case and green when the same case named its AC. Both fixtures are committed under `.claude/tools/fixtures/test-requirement-gate/`. Three false positives were found and REMOVED at the first real run (a runaway destructuring regex, an `expect(...)` argument, a fixture embedded in a template literal); the 22 remaining were each read individually. Scoring is now mechanical: the two integers in the committed baseline may only fall. |

- **EXP-120 — KILLED — its own stated re-scoring condition was checked today and failed.** Wire-value provenance: a claim about a field on a wire we do not own must be confirmed by a real capture or declared `unverified`. **Why killed:** at v125 this row recorded its own adherence gap — *"this row's routed mechanism DOES NOT EXIST"* — and set the condition for its next scoring in terms: **the target must EXIST and have been observed going RED; intent no longer counts as adherence.** Checked 2026-08-07: `make wire-provenance` is **still absent from the project Makefile** (`grep -c '^wire-provenance:'` → **0**), two extensions and three scoring opportunities after the row opened. Under §25a's three-strikes rule that is a kill, and under the row's own condition it is not even close. **What it got right, and what survives it:** the underlying discipline was sound and is repeatedly vindicated — DEFECT-OAG-041 (`Canceled` vs `Cancelled`), DEFECT-OAG-042 (`scheduledTimeUtc` present in all 109 real captures and read by nothing), and this week DEFECT-OAG-070, whose engineer **harvested** an AWS-shaped fixture from the live stream rather than authoring one, precisely because *"a mutated capture masquerades as real"*. That behaviour is now plain engineering practice in this repo, enforced where it counts by the §17d test-requirement gate and by real-capture pins, not by this row. **The lesson worth keeping is about the registry, not the discipline:** a row whose mechanism is prose survives on the strength of the idea while delivering nothing, and extending it twice was how it lasted three retros. A routed mechanism that does not exist after one retro should be re-routed or killed, not extended. Retired to hold the registry at the hard cap of 8 while EXP-130 opens.
  - Original row: | EXP-120 | v123 (2026-07-30) | engineer.md (wire-contract provenance) + tester.md (zero-occurrences is a defect signal) + solution-architect.md (seam names its wire-contract source of truth, handed over executable) + cicd.md (`make wire-provenance` in the gate + committed corpus-refresh/live-probe targets) | **Problem:** DEFECT-OAG-041 + DEFECT-OAG-042 are ONE failure mode: **we compared against a hand-typed guess at an external wire value, and being wrong produced SILENCE rather than an error.** 041 — OAG sends coarse state `Canceled` (US); the handler tested `=== 'Cancelled'` (UK) ⇒ **0 `OagFlightCancelled` in 5,308,984 dev + 5,210,600 prod events**, the event type had NEVER fired once; the handler's own docstring claimed the value was "corpus-confirmed" (it was not), and the only 4 occurrences of the UK spelling in the whole repo were **our own test expectations**, so it passed its 1,525-test suite forever while never working on real data. 042 — `departure.scheduledTimeUtc` was never read from a FlightStatus body at all (`buildGenesisDelta` read `times.estimated.*`/`times.actual.*`, never `times.scheduled`) though all 109 captured real bodies carry it ⇒ 78% of flights had no departure time, entire airlines invisible on departures boards. Two more of the class are known+unexploded: `irregularOperationType === 'Recovery'` is in ZERO captures (so `recovery` has silently always been `false`) and `diversionType` is unvalidated. Why nothing caught it: EVERY gate consulted OUR DECLARATION of the wire contract, never the wire — TDD red→green proves the code agrees with the guess (a self-consistent pair of wrong things is green); the tester's live journey validation asks "does the journey work?", never "did every type we can emit actually emit?" (0 occurrences ≡ a quiet day); the architect reviews the seam's shape, not the vocabulary crossing it. Note the process ALREADY BELIEVED the right thing — solution-architect.md's "verify external-interface facts at the authoritative SOURCE" (v64/EXP-078) — but as PROSE it is unfalsifiable: a docstring claiming verification is indistinguishable from a real one, so the belief never became a gate. **Solution:** generalise the two ledgers the engineer built during the fixes (`tests/defect-oag-04{1,2}-wire-*-provenance.test.ts`) into a standing EXECUTABLE gate with three invariants over a provably-real capture corpus — (P1) LITERAL provenance: every literal compared against inbound external data is declared `confirmed` (present as a value for that key in ≥1 real capture) or `unverified` (still ABSENT from every capture, with the consequence if wrong stated); (P2) FIELD/leaf coverage: every canonical output leaf declares its external source path(s), each of which must be present in a capture AND actually POPULATE that leaf when the capture is fed through the real read path (this invariant would have failed 042 on day one); (P3) OUTPUT LIVENESS: every type/branch the system can emit has occurrence count > 0 over real traffic, or is explicitly declared not-yet-observed with a reason + a live probe. Plus the three holes in the seed that the gate must close: **completeness** (fail on a MISSING declaration — the seed's hand-maintained inline arrays only catch an entry that became false, so a new comparison with no entry is invisible), **corpus soundness** (only provably-real captures may confirm; the seed's 041 sweep indexes derived/synthetic fixtures too), and **refresh** (a committed corpus-refresh + live-probe `make` target — the seed's corpus is grown by a self-described throwaway script + manual curation, so it silently ages). | CFR (a wire-contract mismatch — wrong literal, unread field, never-emitted type — is caught by the build/dev-validation gate instead of reaching a done item and surviving millions of events) + secondarily MTTR (a red ledger names the literal/leaf; the alternative was 5.3M-row archaeology) | over the next 3 slices/defects touching an external-data contract: the ledger covers 100% of literals compared against inbound external data + 100% of canonical leaves, RUNS in the build/push gate, output-liveness is asserted over real traffic, AND ≥1 of the two known-unexploded holes (`irregularOperationType='Recovery'`, `diversionType`) is either corpus-confirmed or converted to a declared+tracked hole with a live probe. **FAILED if** any of: a new wire comparison or canonical leaf ships with no provenance declaration; another zero-occurrence / never-read wire defect is found by anything OTHER than the ledger; the ledger passes green while an emitted type or canonical leaf is 0-populated over real traffic; or a literal is "confirmed" only by a fixture we authored | 3 external-contract slices/defects (or 3 retros) | active (0/3) | **applies-to:** any project that consumes or emits data across a wire it does NOT own (third-party feed, partner API, another team's bus/event contract) — i.e. any code comparing a literal against, or reading a field path out of, a payload whose shape is defined outside the repo. NOT applicable to purely-internal contracts where both sides live in-repo and are refactored together. Where captures cannot be committed (PII/volume), the predicate is satisfied by a committed live-probe target against the real source instead of an offline corpus. Founding: DEFECT-OAG-041 + DEFECT-OAG-042 (2026-07-30), principle-failure `2026-07-30-oag-wire-value-guessed-and-being-wrong-was-silent.md`. Sibling of EXP-115 (whole-journey/live validation) — 115 asks "does the journey work with the real artifacts?", 119 asks "are the external VALUES and FIELDS we assert real, and does every output we claim actually occur?"; and the executable successor to EXP-078's prose "verify at the authoritative source". Opened at cap: registry goes 7 → 8 active. ‖ **OAG v124 gap-closing retro (2026-07-31, 1st opp): POSITIVE 1/3, and EXTENDED.** Positive on two limbs, one day after opening: (a) its **output-liveness** invariant found the **4th** instance of the never-fired-capability class — `OagFlightDiverted` = **0 of 5,300,655** prod events (after `OagFlightCancelled` 0/10.5M, `departure.scheduledTimeUtc` never read, `irregularOperationType='Recovery'` zero captures); (b) its **only-real-captures-confirm** limb shaped the DEFECT-OAG-043 fix, which pinned the out-of-scope airports against a committed REAL prod capture (`fixtures/prod-capture/defect-oag-043-out-of-scope-genesis.json`, AF5913 DTW→RSW) instead of a hand-authored value. **EXTENSION (rather than a new row — the gap is inside 120's own scope):** 120 makes NEW claims executable but says nothing about **already-recorded** unverified assumptions. Architecture delta `029-slc028-…` (2026-06-26) had ALREADY recorded the doubt that the coded diversion wire shape (`body.diversion.airport`, nested) may not match OAG's documented shape (root `irregularOperationType` + flat `diversionAirport`) and closed with "re-verify when a real diversion is first captured" — never actioned, because **prose in a delta has no mechanism to become work** and "re-verify when X happens" is a trigger nobody watches. Added limb (solution-architect.md): an `unverified` mark or a "re-verify when…" note must, in the same act, EITHER become an executable provenance entry whose `unverified` limb goes RED the day the wire sends the value, OR be registered as an item/open-item whose acceptance IS the verification, with a machine-checkable liveness predicate — never both-neither; and at every slice gate, sweep the deltas you build on for outstanding marks and close or restate them. ‖ **OAG v125 test-process retro (2026-07-31): HELD at 1/3, EXTENDED AGAIN, and an ADHERENCE GAP recorded that is the more important finding.** Not re-scored — instances 1–4 were already this row's 1st opportunity yesterday, and the 5th (`deriveAirports`) is a specification failure no wire-provenance gate catches. **THE GAP: this row's routed mechanism DOES NOT EXIST.** `make wire-provenance` is absent from the project entirely (it lives only as an aspiration in `open-items.md` + `cicd.md`); the 041 literal ledger's corpus sweep is an **undiscriminated recursive walk of all 132 `.json` under `fixtures/`** — including 4 hand-authored `synthetic/`, 11 derived `oag-version-coverage/`, 2 vendor doc samples and a config dump — so its own docstring's claim to sweep the "**real**" corpus is false and a value we typed can still confirm a production literal; `_capture`/`_provenance` markers exist on **115** files and **NO GATE READS THEM**; the directory filter that fixes exactly this already exists in the sibling 042 field ledger (`if (!dir.startsWith('oag-raw')) continue`) and was **never back-ported**; and `diversionType` still carries **no declaration at all** — neither confirmed nor declared-unverified — which literally satisfies this row's own FAILED criterion "a new wire comparison ships with no provenance declaration". This is process v125 §17c Layer 2 applied to this very row: the remedy was written as prose and therefore reproduced the defect. **Consequently the next scoring of this row must verify the target EXISTS and has been observed going RED (proof-of-fire, EXP-122 limb ii) — intent no longer counts as adherence.** **THREE ADDED LIMBS:** (1) **the INVERSE sweep, D2 data→code** (engineer.md) — steps 1–6 all start from what OUR code does; the complementary invariant starts from REALITY: every key present in ≥X% of real captures must be READ by some code path or carry an explicit `ignored: <reason>`. This is the limb that would have failed DEFECT-OAG-042 on day one (`times.scheduled` was in **all 109** real captures and read by nothing) and the one that forces an undeclared field like `diversionAirport` to become a question somebody answers. (2) **statistical liveness** — P3's binary `count > 0` is insufficient for rare branches (diversion is a fraction of a percent, `Recovery` rarer): declare an expected base rate with a SOURCED denominator and go RED when `P(0 given p,N) < α`, with `not-yet-observed` as a first-class declared outcome carrying a live probe and a tracked item. (3) **the gate must READ the provenance marker** — corpus soundness was already named as a hole to close and is now measured as unclosed; only `source ∈ {prod-store, dev-store, oag-rest}` WITH a resolvable id may confirm, and synthetic fixtures move to a directory the resolver cannot read. Mechanism for all three: **IMP-028** Phases 0–1. |

- **EXP-122 — ADOPTED — the mechanism exists, runs every cycle, and was observed firing three times in one gate invocation.** DONE is conditional on a real-world OBSERVATION, and every gate must prove it can fail. **Why adopted:** both limbs are now machinery, not prose. Limb (i) — the `awaiting_observation` state is live in `state-graphs.json` and `make loop-gate` check 5 **RE-RUNS each parked item's observation predicate on every invocation**, so an observation that has landed BLOCKS for a tester dispatch and a predicate that cannot be evaluated blocks too. Observed on the real project 2026-08-07T13:02Z: three items parked and re-checked in a single run — `DEFECT-OAG-063` (`make:probe-withdrawal-observed`, 5.0h), `UC-ML1` (`make:probe-genesis-phase-observed`, 4.7d), `UC-OB1` (`make:probe-oag-alerts-observed`, 7.0h) — each reporting NOT YET OBSERVED rather than being quietly folded into a `done` aggregate. That is precisely the "shipped and green but UNPROVEN is not done" property the row was opened for, and it is now the default behaviour of the loop rather than something an agent must remember. Limb (ii) — "every gate proves it can fail" is now standing practice: this retro's own new gate (EXP-131) was accepted only after the control was DISABLED and 3 of its 6 tests were observed failing with one witness each, and the same non-vacuity discipline was applied by the engineer at `66d24be` (4 of 9 cases failed with the controls off). **Integration:** enforced by machinery — `awaiting_observation` in the state graph, `loop-gate` check 5's per-cycle predicate re-run, the `OBSERVE=` append parameter, and `wi-validate` I6 rejecting a park with no predicate. No agent has to remember it. **Honest limit recorded at adoption:** the row's founding sibling concern — a rare branch whose non-occurrence is statistically uninformative (§12d.3) — is served today only by the human-readable "if the wait is unbounded, arm it, force the trigger, or judge it statistically" advisory text, NOT by a computed base-rate test. `UC-ML1` has now sat 4.7d unobserved, which is the case that will eventually test whether the advisory is enough. Retired to hold the registry at the hard cap of 8 active while EXP-131 opens.
  - Original row: | EXP-122 | v125 (2026-07-31, OagEventSource) | process-current.md §17c + §12d obligation 3 + tester.md (observation pointer; injected input is a diagnostic) + cicd.md (proof-of-fire; scheduled census lane) + engineer.md (test built FROM the real record) | **Problem:** FIVE capabilities read `done`/`validated` while never once working on real data — `OagFlightCancelled` **0 of 10,519,584** events; `departure.scheduledTimeUtc` **78% null**, its source path never read though **all 109** real captures carry it; `irregularOperationType='Recovery'` 0 captures (so `recovery` has always been `false`); `OagFlightDiverted` **0 of 5,300,655**; and `deriveAirports()` deriving `metadata.airports` from departure+arrival ONLY, so a diversion airport can never enter the routing key every consumer fan-out rule filters on. All five passed an 1,804-case green suite; CFR and rework read clean throughout. NONE was found by a test — four by ad-hoc prod queries run ONLY because a human challenged a reported figure ~3x reality, the fifth by reading code. Two layers (§17c): **L1** every test is `code → expectation` over inputs WE authored — an EXISTENCE proof — while every failure is a UNIVERSAL property over inputs reality authors and outputs we declare, so the three inverse questions (D1 code→data liveness, D2 data→code coverage, D3 gate→artifact identity) were never asked. This is NOT a data-access problem: we hold ~10.5M prod events and 109 real captures and `times.scheduled` was in every one — reality was in the repo, unexamined. **L2, which explains the recurrence:** the load-bearing claim lives in PROSE, where it cannot be false — a docstring claiming a literal was "corpus-confirmed" (it was not); a provenance ledger whose docstring says it sweeps the "real" corpus while walking hand-authored `synthetic/`, derived fixtures, vendor doc samples and a config dump; a prod smoke whose "no real consumer is fanned out to" comment was false AND had ROTTED (two runs reached an external consumer's live prod DLQ); a scope declaration citing a **1,160,377-row prod scan that exists only as a docstring with no committed script**; and decisively, v123's own remedy — **`make wire-provenance` DOES NOT EXIST**, `_capture`/`_provenance` markers sit on 115 files NO GATE READS, and the fix for the sweep already existed in a sibling ledger and was never back-ported. A remedy written as prose reproduces the defect it was written for. **Solution — one hypothesis, two limbs, both aimed at L2:** (i) **observation-gated done** — a capability is not `done` until OBSERVED working on data the system did not author: `validated` carries an observation pointer (real stream+event id, or a provenance-stamped capture), an INJECTED input is a diagnostic never a validation, and where the capability has not been observed the item enters **`awaiting_observation`** (non-terminal, machine-checkable liveness predicate, re-checked every cycle like `blocked`), with rare branches judged by expected-rate × exposure (`P(0 given p,N) < α`) rather than a binary; (ii) **proof-of-fire** — a gate is not a gate until it has been OBSERVED going RED: every new gate/control ships with a deliberately seeded violation, demonstrated once and recorded on the item. Supersedes EXP-106, whose intent was identical but whose mechanism ran at slice CLOSE — before the capability could possibly have occurred — and was therefore structurally blind (4 recurrences, 1/3). | CFR (inherited from EXP-106: a never-fired capability, an unread real field or a dead gate is caught by a standing lane instead of surviving millions of events into `done`) + MTTR (the census NAMES the type/leaf; the alternative was 5.3M-row archaeology after a human challenged a number) | over the next 3 slices/defects delivering a boundary-crossing output: 100% of `validated` events on such items carry an observation pointer to a real non-authored record; ≥1 item honestly occupies `awaiting_observation` instead of being passed as `done`; 100% of NEW gates ship with a demonstrated proof-of-fire; and ≥1 never-fired capability or dead gate is caught BY these mechanisms rather than by a human challenging a number. **FAILED if** any of: an item reaches `done` on an injected/self-authored input; a new gate is added with no observed red; another never-working capability is found by anything other than the census/observation gate; `awaiting_observation` is never used (proving it is decoration and everything is still being passed as done); or the census/proof-of-fire targets are described in prose but do not EXIST at the next retro — the L2 failure mode, and the explicit adherence check for this row | 3 slices/defects (or 3 retros) | active (0/3) | **applies-to:** any project shipping a capability whose output crosses a boundary (an event type, a published field, a routing/fan-out decision) and any project adding a gate. The mechanism is IMP-028 (`make conformance-census`, real-store D1/D2 over a diffable snapshot); the machinery half (state-graph `awaiting_observation` + the owed IMP-011 `wi-validate` I5) is registered as an item — the orchestrator does not write machinery code. Founding: process v125 §17c, the five OAG instances + DEFECT-OAG-044's synthetic-input prod smoke, `process/principle-failures/2026-07-31-green-test-process-proved-nothing-about-real-data.md`. Distinct from EXP-120 (facts on a wire we do NOT own — 120 makes the wire CLAIM executable; 122 makes DONE conditional on an observation, and makes every GATE prove it can fail) and from EXP-121 (an internal control omitted in one of N compositions). **Opened cap-neutral: EXP-106 RETIRED as superseded with its CFR metric inherited; registry 8 → 7 → 8, AT cap.** |

- **EXP-121 — ADOPTED — its mechanism is compile-time enforced, and its pattern was independently re-derived by a different requirement this session.** A guarded primitive with more than one composition/caller must have the guard REQUIRED of every caller, not remembered by each. **Why adopted:** the founding fix (DEFECT-OAG-043) made `inScope` **required by type** on the second ingest lane — `IngestHandlerDeps.inScope` in `src/app/src/lambda/oag-ingest.ts`, with the fail-safe code constant when the config store is absent so the lane is *never* unfiltered. That is the strongest available integration: the compiler enforces it and no agent has to remember it. **The independent re-derivation is the stronger evidence.** On 2026-08-07 the `REQ-OAG-DELIVERY-INTEGRITY` discovery — which had never read this row — arrived at exactly its pattern from the opposite direction: OAG's Event Hub guarantees per-flight ordering *per consumer*, and the system runs **two live consumers** (Fargate on `consumergroup-2`, the still-enabled Lambda rollback lane on `consumergroup-3`) writing to one event store. The measured consequence is on the record: **98 anomalous streams, 77 with the fold order itself inverted**, declared permanent and unrepairable. The owner then ruled J31 = **retire the Lambda lane outright** and replace the rollback path, i.e. remove the second composition rather than keep guarding it. A row whose pattern a separate investigation reaches unaided, and whose remedy an owner then chooses structurally, has demonstrated its value. **Integration:** enforced by types (`IngestHandlerDeps.inScope`), by the 6-module ingest lane-coverage gate, and by the real-prod-capture pin (`fixtures/prod-capture/defect-oag-043-out-of-scope-genesis.json`). **Honest limit recorded at adoption:** requiring the guard did NOT make the guard CORRECT — DEFECT-OAG-046 is the proof, where `airportInScope` is faithfully applied by both lanes and is itself the wrong predicate (stateless, per-message, no notion of genesis), discarding ~150–285 messages/day. "Every caller is guarded" and "the guard is right" are different properties, and this row only ever bought the first. Retired to hold the registry at the hard cap of 8 active while EXP-132 opens.
  - Original row: | EXP-121 | v124 (2026-07-31, OagEventSource) | engineer.md (a control that is OPTIONAL on a shared primitive is a control some lane omits) | **Problem:** DEFECT-OAG-043 — prod ran **TWO** live ingest lanes into ONE event store and only one was scope-gated. `IngestHandlerDeps` had **no `inScope` field at all**, and the shared `consume-and-ingest` primitive documented an absent predicate as *"no filtering (pre-OB6 behaviour preserved)"*. UC-OB6 wired the gate into the ECS composition; **nothing forced the Lambda composition to declare a decision**, and its EventBridge rule (`oag-ingest-1min`) sat ENABLED on a 1-minute schedule while being *documented* as the dormant "ECS-rollback asset". Result over 4 days: **27,537 events / 2,144 streams** appended, ~25,100 outside the declared airport scope, **all republished to live external consumers** (FIDS + AdixOut — the event store IS the transactional outbox, so there is no second gate downstream); it also invalidated the volume/cost figures (~3x the physical TPA rate). The un-gated lane wrote precisely what the gated lane refused. **Why nothing caught it:** the omission was SILENT AND GREEN — a back-compat-permissive fallback converts a *missing control* into *normal operation*, so there is no error to observe, no red test to write, and TDD cannot help because you never write a test for a field you did not know a caller must supply. The engineer's own error-class statement: *a control declared as an OPTIONAL dep on a shared primitive is a control that some lane will omit.* **Solution:** for any primitive that appends/publishes/persists/spends behind a control (scope, tenancy, entitlement, redaction, rate/cost bound): (1) the control is a **REQUIRED** field of the deps type so the compiler fails on an omitted control at the point a new lane is composed (never `control?:`, never a default meaning "off"); (2) **no permissive fallback** — absent configuration enforces the code-constant fail-safe and logs it, never "no filtering"; back-compat is an explicit, named, tested per-lane decision, never a consequence of `undefined`; (3) a committed **lane-coverage completeness gate** enumerates every module reaching the primitive and **fails the build on an undeclared lane**, each lane declaring its control decision AND trigger, with no continuously-triggered lane unfiltered and every inert lane ASSERTING why it is inert; (4) a rollback/standby asset is a LIVE lane until proven inert — the deployed trigger state is the fact, not the documentation; (5) both lanes derive the control from ONE store, one-directionally (EXP-047). | CFR (an omitted control fails at compile/build time instead of after tens of thousands of un-gated records) + MTTR | over the next 3 slices/defects that add or touch a lane reaching a control-bearing primitive: every such primitive's control is a REQUIRED dep with a closed fail-safe, a lane-coverage gate enumerates all reaching modules and is RED on an undeclared lane, and **0** lanes reach a guarded primitive in any environment without a declared control decision; positive requires ≥1 omitted/undeclared control actually CAUGHT by the compiler or the gate (proving it can fire). If a lane ships un-gated again, or a control is added as optional / with a permissive fallback, or the gate is decoration that never goes red, FAILED | 3 slices/defects (or 3 retros) | active (0/3) | **applies-to:** any project where a guarded primitive has more than one composition/caller — i.e. essentially every multi-lane ingest/publish/persist path, and any "rollback"/standby duplicate of a live lane. Distinct from EXP-120 (which is about facts on a wire you do NOT own; this is about an INTERNAL control omitted in one of N of your own compositions) and from EXP-107 (infra-bearing push gate). Founding: DEFECT-OAG-043 (2026-07-31) — required `inScope` + the 6-module ingest lane-coverage gate + the real-prod-capture pin, `work/OagEventSource/items/done/DEFECT-OAG-043.md`. Cost to adopt is near-zero: both mechanisms were BUILT in the founding fix, this row generalises them. **Opened cap-neutral: EXP-115 ADOPTED+archived this retro to free the slot (retire-one-open-one, hard cap-8); registry stays at 8 active.** |

- **EXP-125 — ADOPTED — the metric moved, and it moved three times in one session without anyone consulting the row.** A red gate is a DEFECT, not context; a gate that cries wolf gets FIXED, never softened. Human ruling, verbatim: *"a gate that cries wolf gets ignored … no, a gate that cries wolf needs to be FIXED."* **Why adopted:** on 2026-08-07 the prescribed behaviour fired three independent times, in each case as reflex rather than as compliance. (1) `make ci-watch` reported a **FALSE RED with an empty diagnostic** on a job that was still `in_progress` and concluded `success` — and its message tells the engineer *"this is a DEFECT — close the local coverage gap"*, i.e. it directs a defect against innocent work. The engineer **declined to soften or delete it** and it was registered as `DEFECT-OAG-072` with the fix shape specified. (2) `make test-fids-integration` had been timing out in its own 300s hook: the fix was to make the setup cost **bounded** (`bootstrap()` gained `fromCursor`), **explicitly not to raise the timeout**, which the engineer named as the softening this row forbids — 600.68s/exit-1 → 7.45s/16-of-16, with assertions *strengthened* (`AC-2.2` from a 5-sample to every entry; `AC-2.3` now fails on an empty corpus instead of silently returning). (3) `make impacted-tests` was found to be **structurally unable to fire** across all of `component-map.mmd` (no node carries a `%% @alias`); the engineer reconciled only the node in its scope and the rest was registered as `OI-COMPONENT-MAP-NO-ALIAS` rather than left as folklore. **Integration:** the behaviour is now plain practice across `engineer.md` and the orchestrator's own conduct — a red or unfireable gate becomes a registered item with a specified fix, and "a noisy gate gets ignored" is no longer accepted as an argument for softening one. The strongest evidence it is integrated is that in all three instances the agents did it unprompted and the row was never cited. **Honest limits recorded at adoption:** none of the three fixes has yet been *validated in prod*, so what is proven is the RESPONSE to a bad gate, not that the gates are now good. And the row never addressed the harder sibling problem this session surfaced twice — a gate that is green *because* it cannot fail (`probe-ingest-scope.sh` check B reads out-of-scope drops as proof the gate works; the position-baseline gate was once unconditionally red). That case is EXP-122's non-vacuity limb, not this row's. Retired to hold the registry at the hard cap of 8 active while EXP-133 opens.
  - Original row: | EXP-125 | v128 (2026-08-03, OagEventSource) | process-current.md §17d + `render-diagrams` fixed and wired BLOCKING into ci.yml (+ `architecture/**` path trigger) + cicd.md + orchestrator.md | **Problem:** a red gate is treated as context rather than as a defect, and "a noisy gate gets ignored" is used to justify SOFTENING gates instead of repairing them. Human ruling, verbatim: *"a gate that cries wolf gets ignored … no, a gate that cries wolf needs to be FIXED."* Evidence: the orchestrator used that phrase **six times in one session**, every time to justify a softening — report-only for the test-requirement gate, "an architect follow-up" for a stale threshold, and `make render-diagrams` as a cautionary anecdote. `render-diagrams` had been **RED for ~20 days** over three `.mmd` files, ran in **no workflow at all**, and was quoted inside `security-audit.yml`'s own header comment as the reason THAT workflow needed a `schedule:` trigger — a defect turned into a rhetorical device. Worse, **v125 had already routed the correct rule** ("every committed gate is green on trunk or DELETED") naming this exact target, and it stayed red for three further days: the SIXTH prose-only remedy in this repo's recent history. And the whole repair was **four semicolons and one pair of quotes** — `;` is a mermaid statement separator, so a `;` inside `Note over X:` or a message label silently truncates the statement and the parser then fails on the NEXT line, which is why the reported line numbers never pointed at the real defect and nobody looked twice. **Solution:** §17e, five limbs — a red gate is a defect with an owner, fixed or deleted, with no "known red" state; softening (report-only / ratchet / raised threshold / exclusion / skip) is permitted ONLY with an owner, a date, and a registered item whose acceptance is the gate BLOCKING again; a threshold that fires on legitimate volume is BROKEN not noisy and is re-baselined from the measured population before shipping; wired-but-non-blocking is not wired; and **never cite a red gate as evidence in an argument** — if you know enough to cite it you know enough to fix or open it. Applied immediately rather than described: `render-diagrams` is green (31/31, was 28/3) and now runs BLOCKING in `ci.yml` with `architecture/**` added to the path trigger so a diagram broken in isolation cannot skip CI; and the two softenings I created today were registered with owners and expiry dates (`OI-GATE-SOFTENINGS-WITH-EXPIRY`). | CFR (a defect the gate exists to catch is caught, because the gate is green and blocking rather than red and quoted) + lead time (a permanently-red gate is re-diagnosed from scratch by every agent that meets it) | over the next 3 cycles: **zero committed gates are red on trunk** at any cycle boundary, every softening in force carries an owner + expiry + a blocking-acceptance item, and **no agent report cites a red gate as supporting evidence**; if a gate is found red at a cycle boundary with no owner, or a softening is extended without one, FAILED | 3 cycles (or 3 retros) | active (0/3) | applies-to: every project with committed gates. Founding: the human ruling plus the render-diagrams instance. Deliberately falsifiable in the direction that matters — the measurement is "were any gates red and unowned", which CAN come back negative and is cheap to check (`make render-diagrams`, `make test-requirement-gate`, `make board-audit`, `make loop-gate` all report). Note the honest risk: this rule makes gates more expensive to add, which is the intended trade — a gate you must keep green is a gate you think twice about shipping. Opened with the registry at 7 of the hard cap-8, taking it to 8. |


## EXP-123 — mechanise the loop's preconditions as a non-zero exit (`make loop-gate`)

**Routed** v126 (2026-08-01, OagEventSource). **Retired** v140 (2026-08-13, OagEventSource). **Disposition: ADOPTED.**

**Outcome.** The hypothesis was *documented obligations are skipped, mechanised ones are honoured* — and it held. Observed firing at its own retirement retro: `loop-gate` BLOCKED the pull with 4 violated preconditions and named the remedy for each. Two were stalled validations (`DEFECT-OAG-104` 6.5h, `DEFECT-OAG-107` 6.2h) where the work was DONE, the `ref:` was green on origin trunk, and only a tester dispatch was missing — the 35.5h founding case, now impossible to leave unnoticed. The other two were wip depth 10 against a cap of 8, and retro debt. **All four were cleared by acting on the gate's remedy text, none by softening the gate** (§17e).

**Honest caveat carried into the archive.** The row's own falsification condition was that the `queue` + `dev-validating` GLT shares must FALL. They did not: `queue` is **63.0%** at retirement against a 37.02% baseline. That is NOT scored as a failure, because the v132 retro established the baseline was 44.98% interpolation and the later figures are measured — the two numbers are not comparable and the row was never re-baselined. **What is adopted is the MECHANISM (a precondition that exits non-zero is obeyed), which is directly observed; the GLT claim is unproven and is explicitly not adopted with it.** The constraint remains `queue`/`open` and is now owned by EXP-131.

**Folded into:** `/loop-run` step 0b, `orchestrator.md`, and `make loop-gate` in the work-items machinery.

**Original row:**

| EXP-123 | v126 (2026-08-01, OagEventSource) | `make loop-gate` (work-items machinery + root Makefile target) + `.claude/commands/loop-run.md` step 0b + orchestrator.md | **Problem:** the loop's preconditions are documented in STAGE F as orchestrator judgement, and they are reliably skipped — while the ONE precondition that is mechanised is reliably obeyed. Measured this cycle: `make retro-debt` (exit 2) fired and FORCED a retro that would otherwise not have happened; meanwhile every judgement-shaped obligation failed simultaneously — DEFECT-OAG-045 dwelt **127,636s (35.5h)** and DEFECT-OAG-048 **98,224s** in `validating`, both already pushed AND deployed and awaiting only a tester dispatch nobody made; Ready sat at 1 against `min_items` 3; and intake sat at **14 against a `wip_limit` of 10**, i.e. the cap is not enforced anywhere at all. This is the SECOND consecutive retro to name this constraint: v124 diagnosed it as orchestrator over-gating (holding pushes) and prescribed "push on green". That prescription was FOLLOWED — all three fix commits (`5095849`, `78bfd55`, `265bea2`) are ancestors of `origin/main`, pushed same-day — and the dwell happened anyway, because the mechanism was misdiagnosed. The metrics confirm it did not work: `dev-validating` 22.17%→21.15%, `tester` 22.60%→22.28%, `queue` 36.78%→**37.02% (worse)**. The real mechanism is not a withheld push; it is a **missing dispatch after the push**. **Solution:** mechanise the remaining preconditions in `retro-debt`'s proven shape — one committed `make loop-gate PROJECT=<p>`, exit 0 = may pull / exit 2 = blocked, reporting EVERY violation with the ids and the remedy, checking (1) stalled validation (any item in `validating`/`dev-validating`/`prod-validating` dwelling past `--stale-hours` whose latest `fixed`/`built_green`/`deployed` event carries a `ref:` — work done, dispatch missing), (2) Ready below `min_items`, (3) any queue over `wip_limit`, (4) retro-debt due (delegating to the existing logic, not reimplemented). Push/deploy state is derived from the structured `ref:` plus `git merge-base --is-ancestor` in the project repo — NEVER from event-note prose, which is what produced a confident precisely-quantified wrong conclusion this cycle (a note reading "NOT pushed" was ~35h stale while the commit had been on `origin/main` the whole time). The loop runs it before every pull. | gross lead time — specifically the `queue` (37.02%) + `tester`-attributed `dev-validating` (21.15%) shares, which together are ~58% of GLT | over the next 3 loop cycles: no item dwells >4h in a validation state with a green `ref:` and no dispatch (the 35.5h case becomes impossible to leave unnoticed), Ready-below-floor and queue-over-cap are surfaced at the moment they occur rather than at a retro, and the `queue`+`dev-validating` GLT shares FALL vs this cycle's 37.02%/21.15% baseline; if the gate is added and those shares do not fall, the diagnosis is wrong again and this is FAILED — mechanisation is not the lever and the constraint is elsewhere | 3 loop cycles (or 3 retros) | adopted (0/3) | applies-to: every project running `/loop-run`. Founding evidence: this retro's why-chain — `retro-debt` is obeyed *because* it exits non-zero, and it is the only loop obligation that does. Deliberately falsifiable in the direction that matters: if mechanising the checks does not move the two dominant GLT shares, the pattern "documented obligations are skipped, mechanised ones are honoured" is not the root cause and must be abandoned rather than re-prescribed a third time. Note the 4h `--stale-hours` default is a guess and is itself the first thing to tune. Opened under the hard cap-8 after retiring EXP-101 (adopted), EXP-107 and EXP-112 (both killed): registry 8→5→6. |

---

## EXP-133 — KILLED (negative criterion fired), 2026-08-19, v143 retro

**Outcome: NEGATIVE — killed on its OWN stated kill condition, not on a judgement call.**

The row said: *"NEGATIVE - kill the row - if any of: a container-contention failure still
occurs with `ddb-local-mine` available (then the container was not the mechanism and
EXP-127's load-average reading was right after all)"*.

That fired on 2026-08-19. Measured: **load average 18.93 with `ddb-local-mine` in use and
exactly ONE DynamoDB Local container running, no leaks** — versus the 19.85 of the
thirteen-container incident. Two contention-caused false test results the same day: an
adapter non-vacuity guard went RED because a race did not collide under load (passed 2/2 in
isolation, tier re-ran 260/260), and two concurrently-run suites produced four phantom
timeout failures (clean re-run 4681/4681).

**So the container was NOT the mechanism, and EXP-127's load-average reading was right after
all.** The row's author wrote the condition that would refute it and it was honoured.

**A second, independent reason the row could not be scored positive:** `ddb-local-mine`
shipped WITHOUT a matching teardown. `ddb-local-down` had no `DISPATCH` handling at all — it
tore down the DEFAULT container and exited 0 — so every dispatch that followed the documented
teardown leaked its container for the twelve days the experiment ran. The row's baseline was
about avoiding contention; its own mechanism was quietly manufacturing it. Registered and
fixed as `DEFECT-OAG-132` (differentially proven: old path exits 0 with the container still
running).

**THE MECHANISM IS KEPT AS PLAIN PRACTICE, per §25a — killing a row is not removing
behaviour.** `ddb-local-mine DISPATCH=<id>` is load-bearing and was demonstrated sound at
landing (three coexisting containers, non-vacuity proven in both directions), and now has a
symmetric teardown deriving the same identity by the same rule. It stays, with no registry
row. What dies is the CLAIM that per-agent containers address the failure class — they
address collisions, not saturation.

**Where the real lever went:** `OI-DISPATCH-HAS-NO-LOAD-PRECONDITION` (registered
2026-08-11, six agent deaths in two days, every one first misattributed to the agent rather
than the host). Today's measurement was appended to it, converting its argument from
agent-deaths to **measurement-validity** — the dangerous direction being the GREEN one,
because nobody re-runs a pass. Recommended bringing its 2026-09-05 defer forward.

**Original row:**

| EXP-133 | v137 (2026-08-07, OagEventSource) | `work/<p>/Makefile` (`OAG_DDB_NAME` + `OAG_DDB_PROJECT` + `ddb-local-mine` + a tightened `ddb-local-assert-ours`) + `src/app/local/docker-compose.yml` (parameterised `container_name`) | **Problem:** owner-raised 2026-08-07 - *we should consider using dynamodb container per engineer*. Investigated and the exposure was NOT where it looked. Test DATA was already safe: the adapter suite namespaces its tables per run (`OagFeed-EventStore-PortContract-<runid>-N`; measured 27 tables across 3 run ids on the live container) so two concurrent engineers could not corrupt each other's rows. THE CONTAINER ITSELF was the exposure and in two distinct ways. (1) `container_name` was HARDCODED in docker-compose.yml so `OAG_DDB_PORT` moved the port while the name stayed fixed - a container per engineer was therefore IMPOSSIBLE and a second `docker compose up -d` RECREATES the one container on the new port yanking the endpoint out from under an in-flight suite whose tests then fail with a connection error INDISTINGUISHABLE FROM A CODE FAILURE. (2) `ddb-local-assert-ours` could only ask is this container OAG's - never is it MINE - so it green-lit engineer B onto engineer A's database. Context: two engineers were building concurrently at the time and BOTH had independently discovered they must hand-override `OAG_DDB_PORT=8010` because port 8000 is held by a sibling project. **Solution:** container identity is DERIVED not hardcoded. `OAG_DDB_NAME` (default = the LEGACY name so every existing invocation is byte-identical) plus a per-name compose project so two stacks are separate objects to compose rather than one being reconfigured under the other; `ddb-local-assert-ours` compares the publisher against `$(OAG_DDB_NAME)`; and `make ddb-local-mine DISPATCH=<id>` derives BOTH port and name from one dispatch id (port 8100-8999 clear of 8000 and 8010) so different dispatches cannot collide and a re-dispatch idempotently reuses its own container. Derived rather than hand-picked for the same reason a threshold is: a hand-picked port looks fine and collides silently. | CFR (a phantom connection failure that reads as a code failure is a false defect signal) - secondarily lead time via removed re-dispatches | over the next 3 sessions: **POSITIVE** if zero agent deaths or suite failures are attributable to DynamoDB Local contention AND no engineer hand-picks a port. **NEGATIVE - kill the row - if any of:** a container-contention failure still occurs with `ddb-local-mine` available (then the container was not the mechanism and EXP-127's load-average reading was right after all); OR the derived-port scheme itself collides; OR engineers keep hand-overriding `OAG_DDB_PORT` because `ddb-local-mine` is more friction than the workaround it replaces - that last one is the realistic failure mode and is why the legacy default was preserved rather than forced. Baseline: 1 watchdog stall this session at load 7.57 and 2 of 2 engineers hand-overriding the port. | 3 sessions | active (0/3) | applies-to: any project whose agents run a tier against a shared local container (DynamoDB Local / Azurite / an emulator) - ROC and AdixOut both do. VERIFIED AT LANDING not asserted: `ddb-local-mine DISPATCH=retro-probe` brought up `oag-dynamodb-local-retro-probe` on derived port 8603 ALONGSIDE the live shared container on 8010 and a sibling project's on 8000 - three coexisting - without disturbing the two engineers mid-suite. NON-VACUITY PROVEN in both directions: the guard PASSES on a correct port/name pairing and FAILS loudly on my-name-vs-another-engineer's-port, which is the case the OLD guard would have PASSED (port 8010 IS `oag-dynamodb-local`) - that is the exact hole. Backward compatibility verified: the legacy invocation the two live engineers were using still passes. Opened cap-neutral: EXP-125 ADOPTED and archived this retro; registry stays at **8 active AT cap**. |

---

## RETIRED at v145 (2026-08-20, ROC) — the six row-less ROC `##` sections + the 2026-08-14 id re-allocation note

**Why they are here.** These were never rows in the registry table, so the hard WIP cap never
governed them, no retro ever scored them, and `process/experiments.md` was carrying two different
genres in one id space: capped falsifiable hypotheses, and long-form findings awaiting a decision.
That is what let two experiments both be minted `EXP-142` (main's test-requirement-gate ratchet and
ROC's screen-viewport hypothesis) — the same defect class as `DEF-ROC-077` the same day: a GLOBAL
registry read against PER-PROJECT reality, with no uniqueness check anywhere.

**Dispositions** (the full text of each section follows, verbatim):

- `EXP-136` + `EXP-139` + `EXP-140` and its ten-instance continuation → **PROMOTED** to a real row,
  **`EXP-ROC-002`**, carrying the mechanism the sections had already converged on: derive firing
  rights from the item's DECLARED OWNER; let the state graph constrain transition SHAPE only.
- `EXP-137` (atomic pathspec commits) → **ADOPTED**, live in `CLAUDE.md` with its four known limits.
- `EXP-138` (`prod-deploying` needs a `blocked` exit) → **DEFERRED**, `open-items.md`.
- `EXP-141` ("dep satisfied" is undefined) → **DEFERRED**, `open-items.md`.
- `EXP-142` (a screen is evaluated as a SCREEN) → **KEPT and RENAMED `EXP-ROC-001`**, with a row.
  `main` holds the authoritative `EXP-142`; the ROC section was the colliding half, and v144 was
  right that it had no standing to relabel `main`'s. Per-project namespacing removes the choice.

Ids are per-project namespaced from v145 (`EXP-<PROJ>-<nnn>`) and the bare-numeric space is FROZEN,
enforced by `make process-lint`. Citations of `EXP-136`..`EXP-141` in v141/v144 records still resolve
here; nothing has been silently relabelled.
## ID RE-ALLOCATION — 2026-08-14 (ROC), read before citing any ID below

The sections that follow were originally numbered **EXP-119 … EXP-124** by the ROC instance while it
was **22 process versions stale** (v118 against main's v140) and 15 days un-reconciled. Those six IDs
were **already allocated on `main` to OagEventSource experiments**, since adopted/retired at OAG v125.
They have been **renumbered to EXP-136 … EXP-141**, above main's high-water mark of EXP-135. Content is
unchanged; only the identifiers moved. Any earlier ROC commit message citing EXP-119..124 refers to
these sections under their old numbers.

**Two structural problems this exposed, neither of which is fixed by renumbering:**

1. **There is no cross-instance ID allocator.** Each instance mints the next ID from whatever registry
   its own branch happens to hold, so any instance running on a stale base will collide. Renumbering is
   a repair, not a fix — the next divergence reproduces it.
2. **The hard cap of 8 `active` rows is a GLOBAL budget, but rows are allocated PER-INSTANCE.** Main
   arrived at exactly 8. ROC's genuine experiments below would take it past the cap — and ROC cannot
   honestly score OagEventSource's rows to make room, because it has neither the evidence nor the
   standing to adopt-or-kill another project's work. **So the cap is breached, deliberately and
   visibly, rather than resolved by killing rows I cannot fairly judge.** Recorded here instead of
   silently exceeded. The cap needs either a per-instance allowance or a cross-instance scoring owner.

Also note a **format divergence**: main's registry is a table (`| EXP-NNN | … |`); these ROC sections
are `##` prose blocks. They merged without conflict precisely because they do not overlap textually —
which means the merge cleanliness was luck, not compatibility.
## EXP-136 — a `documenter` can advance its own docs-only work item
**Registered:** 2026-07-31 (ROC) · **Status:** OPEN · **Applies-to:** any project with a
docs-only or runbook-bearing use-case.

**The gap.** `documenter` appeared **zero times** in
`process/machinery/state-graphs.json`. `built_green` was restricted to `engineer`, so a
docs-only use-case could not be advanced by the agent that actually built it. On ROC's
`UC-ROC-082` (the SSO-outage runbook, a tracked acceptance condition of a signed-off
requirement) the documenter had to append under the `engineer` slot and record the
attribution truth in the note — an honest workaround, but the event log now misattributes
who did the work, which is exactly the property the event-sourced model exists to get right.
It then stranded the item in `deploying`, whose only forward event is `deployed (cicd)` —
a dead end for an item with no runtime artifact to deploy.

**Amendment made.** `documenter` added to the `agents` list of the `use-case` transitions
`built_green` (building → deploying) and `deployed` (deploying → dev-validating). Minimal
and additive: no new state, no new event, no change to any existing agent's rights.

**Why not a separate docs-only path?** A new state or a `deploying`-skipping edge is the
tempting design, but docs DO ship — via commit and push to trunk — so `deployed` is
semantically honest for them rather than a fiction, and it keeps one graph instead of two.
Revisit only if a docs item genuinely needs a different validation shape from a code item.

**Target metric:** gross lead time — specifically the queue/blocked component. The failure
mode this removes is an item sitting in a dead-end state until a human notices and
hand-resolves it, which is pure blocked time attributable to the machinery rather than the
work.

**Anticipated effect:** docs-bearing use-cases flow to `dev-validating` without
orchestrator intervention, and the event log attributes doc work to `documenter` instead of
to `engineer`. Also expected: honest `time_by_owner` for documenters, which today reads as
zero because they cannot own a transition.

**Scoring horizon:** the next three docs-bearing use-cases across any project. Score as
positive if each advanced without an orchestrator unblock and without an agent appending
under a role it does not hold.

**How it could be wrong.** If `deployed` for a docs item turns out to mislead a reader into
thinking a runtime deployment occurred, the fix is a distinct docs path, not reverting this.
Watch for that in the retro rather than assuming the semantic holds.

### Follow-up 2026-07-31 — the documenter's own assessment, and a correction to it

The documenter that hit this gap was asked to judge whether `deployed` would mislead a
future reader. Its answer was better than the question: the risk is **not in prose** — a
human reading the item cannot be misled, because the note's first clause states there is no
runtime artifact — but in **derived metrics and incident correlation**. It also argued,
correctly, that `deploying` was the genuinely dishonest state (a no-op) while `deployed` is
substantively true for a runbook, since the artifact reaches its reader the instant it is on
trunk. It recommended keeping the graph as amended and guarding the derivation instead,
noting that the amendment made this possible by keeping the `agent` field an honest
discriminator (`documenter` vs `cicd`) rather than filing doc work under `cicd`.

**Correction — its proposed guard rested on a premise that does not hold.** It proposed
excluding `deployed` events whose agent is `documenter` from the deployment-frequency and
change-failure-rate derivations. I checked `_compute_dora` in
`.claude/skills/work-items/scripts/work-items.py` before implementing that, and
**deployment frequency does not count `deployed` events at all**: it counts each item's
TERMINAL event (`validated` / `closed` / `deploy`, falling back to
`not_reproduced` / `declined`) per active day (`work-items.py:1180-1188`). So filtering on
the `deployed` agent would have changed nothing, and implementing it would have added dead
code plus a false sense that a risk had been closed.

**What survives the correction:**
- **Concern 1 (frequency inflation) partially stands, by a different route.** A docs-only
  item still reaches `validated`/`done` and so still increments the terminal count. Whether
  that is *wrong* is genuinely arguable — a delivered runbook IS delivered work — so this is
  a question for the retro, not a bug to patch. What is NOT true is that it inflates via a
  `deployed`-event count.
- **Concern 2 (incident correlation) stands unchanged and needs no code.** Someone asking
  "what deployed just before this incident?" can find a docs `deployed` event in the window
  and waste time on a markdown commit. The mitigation is exactly the honest `agent` field
  this amendment preserved — a one-predicate filter for whoever writes that query.

**Watch in the retro:** whether docs-only items materially move the terminal-event count,
and whether anyone doing incident correlation is actually misled in practice. Do NOT
pre-emptively filter the derivation on the strength of the original reasoning — the
mechanism was misidentified, and the honest `agent` discriminator is already in place for
whoever needs it.

## EXP-137 — atomic pathspec commits in a shared working tree
**Registered:** 2026-07-31 (ROC) · **Status:** OPEN · **Applies-to:** any project where
more than one agent works concurrently in ONE working tree (i.e. the normal case today —
agents share a worktree; only *projects* get separate worktrees).

**The gap.** Every agent was instructed to commit as
`git -C work/<p> add <paths> && git -C work/<p> commit -m "…"`. The git **index is shared**
across concurrent agents in one working tree, so a co-worker's `git add` landing between
your `add` and your `commit` sweeps their staged work into your commit. Observed **twice on
2026-07-31**: the cicd agent recording deploy events accidentally committed 25 files of
another engineer's in-flight UC-ROC-084 work (`91f0404`), and on the retry the same
engineer's own commit (`c67e588`) picked up cicd's three item files. Nothing was lost either
time — both agents noticed and repaired non-destructively — but attribution is now wrong in
the history, and it cost two agents real time to detect and unwind.

**Change made.** `CLAUDE.md` now instructs `git -C work/<p> commit -m "…" -- <paths>`
(atomic, pathspec form) and explains why: the pathspec form takes content from the WORKING
TREE and never consults the shared index, so the race window does not exist. It also records
the non-destructive repair (`reset --soft HEAD~1` → `reset HEAD -- .` → re-add own paths)
and the rule not to rewrite a commit another agent has built on.

**Why not a lock.** A mutex around git writes was the other candidate. Rejected as the
first move: it adds a coordination mechanism (and a deadlock/staleness failure mode) to
solve a problem that a different command form eliminates outright. Revisit only if the
pathspec form proves insufficient — e.g. if agents need multi-step staging that genuinely
cannot be expressed as one pathspec commit.

**Note this was ALREADY the emergent practice.** Several engineers independently arrived at
"explicit pathspec, never `git add` sweep" and said so in their reports; one even recommended
this exact form in a commit message. The gap was that the instruction told them otherwise, so
the safe behaviour depended on individual diligence rather than the documented default.

**Target metric:** change failure rate, and gross lead time's rework component —
mis-attributed commits produce false blame during later diagnosis and cost detection time.

**Anticipated effect:** zero further cross-agent index sweeps. Watch for the opposite
failure too: an agent that needed staged-but-uncommitted state and finds the pathspec form
awkward.

**Scoring horizon:** the next multi-agent cycle with 4+ concurrent agents in one tree. Score
positive if no sweep occurs and no agent reports the form as blocking.

### Third occurrence, found after registering this — and it BROKE CI

A third sweep surfaced the same day, and unlike the first two it was not merely an
attribution problem: commit **`f624dff`** — a `UC-ROC-082` *item/docs* commit — swept an
unrelated **in-flight file move** (`src/app/local/evaluateApi.ts` →
`src/app/src/api/evaluateApi.ts`) onto trunk **ahead of its importer updates**. The runner
then failed with `ENOENT` on the old path, turning CI red over `9928840`. It self-cleared on
the next run (`25123f9`, success) once the importers landed.

**This is the cost case the first two occurrences did not demonstrate.** A swept *content*
change is bad attribution; a swept *refactor mid-flight* is a broken build on trunk. And it
landed via a **docs-only commit**, from an agent that touched no source at all — so no
amount of care about one's own files prevents it. That is what makes the two-step form
unsafe rather than merely untidy: the hazard is not proportional to what you are committing.

Note also how it was diagnosed: the engineer who hit the red CI stood up a **clean detached
worktree at HEAD without their change** and reproduced the failure there, rather than
assuming it was or was not theirs. Worth reinforcing — that technique separated three
distinct pre-existing failures from their own work in one pass.

**Deliberately NOT registered as a defect.** The root cause is this experiment's subject and
is already fixed in `CLAUDE.md`; the symptom self-resolved on the following run; and the
broken state no longer exists on trunk. A defect record would inflate the count without
adding a fix or a fact. Recorded here as evidence instead — if a fourth sweep occurs AFTER
the atomic-pathspec instruction is in place, that is a different finding and does warrant its
own defect, because it would mean the instruction is not being followed or is insufficient.

### FOURTH occurrence — and it proves this amendment is INSUFFICIENT, not unfollowed

A fourth sweep happened on 2026-08-03, **with both agents correctly using the atomic
pathspec form**. `DEF-ROC-016`'s commit `883ebd8` swept ~112 lines of `DEF-ROC-019`'s
then-uncommitted edits to `architecture/dependencies/class-deps.mmd`. Nothing was lost — all
the DEF-ROC-019 model nodes/edges are on trunk — but they are attributed to the wrong commit,
and only one line rode in the correct one.

**So my framing above was wrong, and this correction matters more than the original
experiment.** I wrote that the pathspec form "takes its content from the working tree and
never consults the shared index, so the window does not exist." The first half is true and
the second half does **not** follow. Taking content from the working tree is *precisely* the
problem for a **CO-OWNED file**: if another agent has uncommitted hunks in a path you name in
your pathspec, you commit THEIR hunks along with yours, deterministically and with no race
window at all. The atomic form eliminates the *index* race for disjoint files; it gives no
protection whatsoever where two agents legitimately edit the same file.

`class-deps.mmd` is exactly that file — the change-impact model every engineer is required to
update in the same commit as their code. So the process actively directs concurrent agents
into a shared path and then offers them a rule that does not cover it.

**Candidate fixes, none yet chosen** (deliberately not decided unilaterally — this needs a
judgement about how much machinery is warranted):
1. **Split the diagram** so each item's claims live in a separate file that is later composed.
   Removes co-ownership at the cost of a build/compose step.
2. **Per-hunk staging** (`git hash-object -w` + `git update-index --cacheinfo`, which one
   engineer already used successfully). Precise, but needs allowlist additions and is easy to
   get wrong under time pressure.
3. **Serialise model updates** — the model edit becomes a follow-up commit, accepting that it
   is briefly out of step with the code it describes. Cheapest, and weakens the
   same-commit guarantee the model relies on.
4. **Accept mis-attribution on co-owned files** and note it in commit messages. Honest, zero
   machinery, loses per-item traceability of model changes.

Note one engineer already worked around this correctly and unprompted (building a mine-only
blob and `update-index`-ing it), and another **waited** for the co-owner to commit before
landing its own hunk. Both are evidence the problem is real and that agents can handle it —
but both were individual diligence, which is what this experiment set out to replace with a
documented default.

**Revised scoring:** score EXP-137 positive for disjoint-file commits only. Co-owned-file
mis-attribution is a SEPARATE open problem and should not be counted against or in favour of
the atomic-pathspec rule.

## EXP-138 — `prod-deploying` needs a `blocked` exit for single-environment projects
**Registered:** 2026-07-31 (ROC) · **Status:** OPEN · **Applies-to:** any project that has no
production environment yet, or whose prod promotion is externally blocked.

**The gap.** `prod-deploying` was the ONLY wip state in the `use-case` graph with **no
`blocked` exit** — `ready`, `building`, `deploying`, `dev-validating` and `prod-validating`
all had one. Its only exits were `promoted` (cicd → prod-validating) and `deploy_failed`
(cicd → reworking). That reads as an oversight rather than a design choice.

**How it bit.** ROC has **no production environment**: its Terraform has never been applied,
`deploy-ROC.yml` deploys only to `aas-test`, and no prod Function App exists. A tester
validating `UC-ROC-084` fired `dev_validated` (dev-validating → prod-deploying) — a
perfectly legal event — and the item stranded. Both remaining exits would have required
asserting something false: `promoted` claims a prod deploy that cannot have happened, and
`deploy_failed` claims a failure when nothing failed. The honest state ("waiting on a prod
environment that does not exist") was inexpressible.

Note the trap is not the tester's error. `dev-validating` offers BOTH `validated` (→ done,
the correct path for a single-environment project) and `dev_validated` (→ prod-deploying).
Nothing in the graph signals which applies, and the more specific-sounding name is the wrong
one here. Every earlier ROC use-case happened to take `validated`.

**Amendment made.** Added `{"from": "prod-deploying", "to": "blocked", "event": "blocked",
"agents": ["flow-manager", "orchestrator"]}` — consistent with the five states that already
have it. Minimal and additive: no new state, no new event name, no existing agent's rights
changed, and it makes the honest state expressible.

**Why not remove `dev_validated`, or auto-route single-env projects to `done`?** Both were
tempting and both are wrong for now. Removing it breaks projects that genuinely promote to
prod. Auto-routing would need the machinery to know whether a project has a prod
environment, which it currently has no way to know and which would be a much larger change
than the problem justifies. A `blocked` exit costs one line and keeps the fact visible in the
queue rather than hiding it.

**Guidance that goes with it (the recurrence fix):** in a project with no prod environment,
the tester's terminal event from `dev-validating` is **`validated`**, not `dev_validated`.
`dev_validated` is only correct when a prod promotion will actually follow.

**Target metric:** gross lead time — the blocked component. A stranded item accrues wip time
invisibly and needs a human to notice, which is precisely the failure EXP-136 addressed in a
different corner of the same graph.

**Anticipated effect:** no item strands in `prod-deploying`; a genuinely unavailable prod
environment shows up in the `waiting` queue where the flow view can see it.

**Scoring horizon:** the next three use-cases reaching `dev-validating` on a project without
prod. Score positive if none strand and none are advanced by an event that asserts something
untrue.

**How it could be wrong.** If items start routinely sitting `blocked` in `prod-deploying`
rather than being closed via `validated`, the guidance is not landing and the real fix is
making the graph itself aware of whether a prod environment exists. Watch for that.

## EXP-139 — a `cicd` agent can advance an infra-owned defect
**Registered:** 2026-08-04 (ROC) · **Status:** OPEN · **Applies-to:** any defect whose fix is
infrastructure, pipeline or deploy-configuration rather than application code.

**The gap.** The `defect` graph restricted `confirmed` (reproducing → fixing) to
`orchestrator`/`engineer` and `fixed` (fixing → validating) to `engineer` alone. But defects
are not all code: `DEF-ROC-020` was a **shared-ownership infrastructure** defect — two
uncoordinated writers to the same Azure Function App's `app_settings`, where a platform-infra
service principal's apply erased the `BUILD_SHA` our pipeline stamps. It was dispatched to
`cicd` deliberately, because the remedy is Terraform / workflow / a drift check / a cross-team
ask, not application code. The machinery then refused every transition it needed, so it fired
them as `AGENT=engineer` and said so in its report.

That is the second instance of this exact shape (see **EXP-136**, where `documenter` appeared
zero times in the graph and a docs-only use-case could not be advanced by its actual builder).
Both times the agent behaved correctly — attributed honestly in the note and escalated rather
than hand-editing state — and both times the event log ended up naming the wrong role, which
is precisely the property an event-sourced model exists to get right. It also skews
`time_by_owner`: infra work is billed to `engineer` and `cicd` reads as idle.

**Amendment made.** `cicd` added to the `agents` list of the `defect` transitions `confirmed`
and `fixed`. Minimal and additive — no new state, no new event, no existing agent's rights
changed.

**Why only those two.** `validated` stays tester-only: cicd fixing its own defect and then
validating it would collapse the gate that caught `DEF-ROC-013`'s misdiagnosis. And
`not_reproduced` stays orchestrator-only, since declining a defect is a judgement call about
scope rather than a technical step.

**The pattern worth noticing, and the reason this is registered rather than just fixed:** the
graph was written assuming defects are code and use-cases are built by engineers. Two agent
roles have now hit that assumption from different directions within a week. The next one is
probably `solution-architect` (an architecture-delta defect) or `product` (a
requirement-framing defect). Rather than wait for a third instance, the retro should ask
whether the per-transition agent allowlists are the right mechanism at all, or whether
"who may fire this" should derive from the item's own declared owner.

**Target metric:** gross lead time's blocked component, plus the integrity of
`time_by_owner`. The concrete failure removed is an agent stalling on a legal-looking
transition it may not fire, or firing it under a role it does not hold.

**Anticipated effect:** infra-owned defects flow without an orchestrator unblock, and
`time_by_owner` starts attributing infra work to `cicd`.

**Scoring horizon:** the next three defects dispatched to a non-engineer role. Score positive
if each advanced without an orchestrator intervention and without any agent appending under a
role it does not hold.

## EXP-140 — an aggregate can read `done` while signed-off scope was never registered
**Registered:** 2026-08-05 (ROC) · **Status:** OPEN · **Applies-to:** every project using the
event-sourced work-item model with aggregate types (requirement / chunk / slice).

**The gap, found by accident.** `REQ-ROC-002` and `CHK-ROC-004` had bubbled to **`done`** — and
were wrong. `SLC-ROC-006` was the only child ever turned into a work item, so when it finished,
the aggregates folded to `done` by construction. But the signed-off dossier for that requirement
contained further scope (the J20 pace-control / named-scenario replay work) that was
**explicitly deferred, not descoped** — and because deferred-but-agreed scope was never
registered as children, the model had no way to know it existed. The requirement therefore
reported complete while part of what a human had signed off was untracked.

Product found this only while looking for decomposable work behind a *different* closing chunk.
Nothing surfaced it: `wi-validate` passes (I1–I4 all hold — the fold is internally consistent),
the derived views are correct, and the tree looks healthy. **The invariant that is missing is
not about consistency of the fold; it is about coverage of the dossier.**

**Why this matters more than one requirement.** `done` on an aggregate is read by humans and by
the metrics as "this value was delivered". If registration is the only thing that makes scope
visible, then any scope agreed at sign-off but deferred to later is invisible the moment its
registered siblings complete — and the gap grows silently with every deferral. This project has
deferred scope at sign-off repeatedly and deliberately (connectivity statuses in REQ-ROC-006,
multi-role RBAC in REQ-ROC-005, template creation in CHK-ROC-009), all of them legitimate
decisions. Each is a candidate instance of the same trap.

**Not yet fixed — the right mechanism needs a judgement I should not make alone.** Candidates:
1. **A dossier-coverage check**: require every signed-off dossier to enumerate its scope items,
   and refuse to let an aggregate fold to `done` while any enumerated item lacks a registered
   child or an explicit descope record. Strongest, and the most machinery.
2. **Register deferred scope immediately as `blocked`/`open-item` children** rather than leaving
   it in prose. Cheap, keeps the tree honest, but inflates `waiting` with things nobody intends
   to pull soon — and this project already has 14 items in `waiting`.
3. **A `deferred_scope:` frontmatter field on aggregates**, checked at fold time, so `done`
   requires it to be empty or explicitly waived.
4. Accept it and rely on the retro to re-read dossiers before closing a requirement — no
   machinery, relies on diligence, which is what failed here.

**Target metric:** none of the four DORA metrics directly — this is a **truthfulness** defect in
the delivery record, which corrupts every metric derived from it. The observable proxy: the
number of aggregates that transition out of `done` after being found incomplete (this instance
is one; `CHK-ROC-004`/`REQ-ROC-002` were flipped back to `in_progress` when the missing scope was
registered as `SLC-ROC-025`).

**Anticipated effect:** an aggregate reading `done` means the signed-off scope was delivered or
explicitly descoped — not merely that its registered children finished.

**Scoring horizon:** the next three requirements to close. Score positive if each was checked
against its dossier before closing, and negative if any is later found to have had untracked
signed-off scope.

**How this could be wrong.** Option 1 could make deferral so expensive that agents stop
recording it in the dossier at all, which would be worse — the prose record is currently the
only reason this was findable. Whatever is chosen must keep deferral cheap to *state*.

### Third instance — 2026-08-14, a verification-only use-case

`UC-ROC-089` is a live-verification use-case whose entire deliverable is a probe script plus its
execution. A **tester** authored the script, committed it, and ran it green — then could not fire
`pulled` (orchestrator/flow-manager only) and, later in the chain, would face `built_green`
(engineer-only) for a script it wrote itself. It reported the block rather than spoofing a role,
which is the right behaviour and the same disposition the documenter and cicd agents showed.

So the mismatch has now appeared **three times in three different shapes**: a docs-only item built
by a documenter (EXP-136), an infra-owned defect fixed by cicd (EXP-139), and now a
verification-only item whose deliverable is authored by a tester. Each was patched by widening one
transition's agent list. That is three patches to the same underlying assumption — that item TYPE
predicts which ROLE does the work — and the assumption is simply false for any item whose value is
not application code.

**This strengthens the open question already recorded above** rather than adding a new one: the
retro should decide whether per-transition agent allowlists are the right mechanism at all, or
whether "who may fire this" should derive from the item's own declared owner. A fourth patch would
be evidence the mechanism is wrong, not that the list needs extending again.

---

## EXP-141 — "dep satisfied" is undefined, so two flow-managers read it oppositely

**Registered** 2026-08-14 (ROC). **Class:** flow rule with no written definition.

### What happened

Two `flow-manager` instances, **in the same session**, reached opposite conclusions about the same
promotion rule — and both were acting in good faith.

- The **earlier** instance established and applied a convention *three times*: a dependency counts
  as satisfied once it is `built_green` **on trunk**, without waiting for its own
  deploy/validate cycle. `UC-ROC-078`'s `made_ready` note states it explicitly — *"dep satisfied =
  green on trunk not full deploy+validate"* — and cites `UC-ROC-076`/`079`, which in turn cite the
  same basis against `UC-ROC-075`.
- The **later** instance searched for that convention, **found no precedent**, and declined to
  promote `UC-ROC-092` because its deps sat in `dev-validating` rather than `done`.

Both were right about what they could see. The convention exists **only in event-log notes**, never
in a rule; the later instance searched item *states*, where it is invisible by construction.

### Why it was invisible — the actual gap

Three places could have carried the definition and none does:

| Where | What it says about dep-satisfaction for promotion |
|---|---|
| `process/process-current.md` §F2/F3/F6/F7 | describes DAG / independent-set mechanics — never defines what makes a dep *satisfied* |
| `process/machinery/CONTRACT.md` | same: edges and folding, no promotion predicate |
| the machinery (`wi-append` edge-check, `wi-validate` I1–I4) | **no invariant on dep state at `made_ready`** |

So promotion is pure flow-manager judgement, unconstrained by machinery and undocumented in
process. That is why it diverged silently rather than failing loudly.

### The experiment

Pick ONE and write it down where a flow-manager will find it (§F6 + `CONTRACT.md`):

1. **Codify the convention** — `built_green` on trunk satisfies a peer dep for promotion purposes.
   Faster flow; accepts the risk that a dep is later rejected and its dependents rework.
2. **Reject it** — require `done`. Zero rework risk; Ready starves more often, which is exactly the
   floor breach observed this cycle (`ready=1` against `min_items=2`, with no legitimate promotion
   available because `UC-ROC-092`'s deps were merely `dev-validating`).

**Target metric:** rework count on promoted-early items, against Ready-floor breach frequency.
Those are the two costs the choice trades between, so measuring only one would bias the answer.

**Anticipated effect.** Option 1 raises throughput and should show near-zero rework *while the
suite is trustworthy* — but the trust precondition is doing real work in that sentence, and
`DEF-ROC-022` (an unexplained intermittent failure in the C1 walking-skeleton test) is live evidence
the precondition is not currently met. Option 2 costs flow but cannot produce this class of rework.

**Scoring horizon:** the next 10 promotions.

**Applies-to predicate:** any project whose items carry `deps` edges — i.e. all of them.

### How this could be wrong

The divergence may be *desirable* discretion rather than a defect: a flow-manager weighing a
specific dep's risk is doing judgement work a blanket rule would flatten. If so, the fix is not a
rule but a **required note** — force the promoting agent to state which basis it used and why, so
the next instance can find it in state rather than prose. That would keep the judgement and remove
the invisibility, which is the part that actually caused harm here.

**Do not score this as "the rule was written."** Score it as: did a later flow-manager, with no
access to this session's context, reach the same promotion decision as the earlier one?

---

## EXP-140 — FOURTH INSTANCE (2026-08-14): the trigger fired; the mechanism is wrong

EXP-140 recorded, verbatim: *"A fourth patch would be evidence the mechanism is wrong, not that the
list needs extending again."* **That fourth instance has now occurred. Recording the verdict rather
than patching a fourth transition.**

### The instance

`UC-ROC-080`'s **engineer** performed its own pull and could not fire `pulled` — the state graph
restricts that transition to `orchestrator`/`flow-manager`, while the engineer's own role brief
tells it to fire `pulled` if it performs the pull. `built_green` is unreachable from `ready`, so the
item was structurally stuck. The agent fired it as `AGENT=orchestrator` **with a note saying it had
done so and why** — the honest workaround, and the fourth agent in a row to report the block rather
than silently spoof a role.

### The four instances, and what they share

| # | Item shape | Role that did the work | Transition it could not fire |
|---|---|---|---|
| EXP-136 | docs-only | documenter | `built_green` / `deployed` |
| EXP-139 | infra-owned defect | cicd | `confirmed` / `fixed` |
| EXP-140 | verification-only | tester | `pulled`, then `built_green` |
| **this** | ordinary use-case | **engineer** | **`pulled`** |

The first three were explained away as "item TYPE does not predict the ROLE for unusual items." **This
fourth one kills that explanation**: the item is an entirely ordinary use-case and the role is the
canonical one for it. The mismatch is therefore **not** about unusual item types at all — it is that
**per-transition agent allowlists encode an assumption that a transition has one rightful owner**,
and that assumption is false wherever an agent legitimately does two jobs in sequence.

Note also the *direct contradiction* this instance exposes: the engineer's role brief instructs it to
fire `pulled`, and the graph forbids it. Two authored artefacts disagree — the same class as
`DEF-ROC-026`, where four documents asserted a partition key the code never set. A rule that cannot
be followed gets worked around, and every workaround costs a role-spoof or a note nobody reads.

### The experiment — replace the mechanism, do not extend the list

Derive "who may fire this" from the **item's own declared owner** rather than a per-transition
allowlist: the item records who is doing the work, the machinery checks the firing agent against
*that*, and the graph constrains only the **shape** of the transition (which states follow which).
This keeps the property the allowlists were protecting — an agent cannot rubber-stamp its own work
into a state it did not earn — while removing the false premise that a transition has exactly one
rightful role forever.

**Target metric:** count of role-spoofed or blocked transitions per 20 items (currently 4 known in
~2 weeks, all self-reported — the true rate is a floor, not a measurement, since a silent spoof
leaves no trace).

**Anticipated effect:** the class disappears rather than shrinking. If instances continue after the
change, the diagnosis here is wrong and the real problem is elsewhere.

**Scoring horizon:** the next 20 items.

**Applies-to predicate:** every project using the event-sourced work-item machinery.

### How this could be wrong

The allowlists may be catching real errors we never see, in which case removing them trades a visible
annoyance for an invisible integrity loss. Before adopting, check whether any allowlist rejection in
the history was a **genuine** mistake rather than a legitimate agent blocked by the mechanism. If
even one was, the answer is a narrower fix, not a replacement.

**Do not score this as "the four transitions now work."** Score it as: did an agent doing legitimate
work get blocked or have to spoof a role again?

### EXP-140 — instances five, six and seven, all on 2026-08-14: four different roles, one day

Recording these together because the pattern is now beyond argument. On a single day, **four
different roles** were blocked by or had to work around the per-transition agent allowlists:

| Role | Transition | What happened |
|---|---|---|
| **engineer** | `pulled` (use-case) | Its own role brief tells it to fire `pulled`; the graph forbids it. Fired as `orchestrator` with a disclosing note. |
| **tester** | `deployed` (use-case) | Item arrived in `deploying` with no cicd in the thread. Fired as `cicd`, transparently noted as a mechanical attestation. |
| **solution-architect** | `confirmed`/`fixed` (defect) | `DEF-ROC-026` resolved as a pure **design decision** — delta 016, no code. The role that did 100% of the work has **no path at all** in the defect graph. flow-manager correctly REFUSED to force it, on the grounds that borrowing an agent name would misattribute DORA time-by-owner and quality-by-stage. |
| **orchestrator** | `made_ready` (use-case) | **The flow owner cannot promote an item into Ready.** `made_ready` is `flow-manager`-only, while `pulled` is `["orchestrator","flow-manager"]` — so the orchestrator may PULL from Ready but not PUT into it. |

### Why the last one settles it

The orchestrator's own definition makes it the role that "owns sequencing, gates, DORA measurement
and Theory-of-Constraints optimisation of the whole pipeline." A rule that lets that role *consume*
from a queue but not *populate* it is not encoding a real safety property — nothing is protected by
forcing a second agent to be spawned purely to append one event. It is an artefact of enumerating
allowlists transition-by-transition without ever checking the set for coherence.

Note the cost shape: every one of these four cost either **a spoofed role with a disclaimer nobody
downstream will read**, or **an entire extra agent dispatch**. The first corrupts attribution; the
second corrupts lead time. There is no third option available to an agent that just wants to record
what it truthfully did.

### Two consequences for the retro, beyond the mechanism change

1. **The disclosing notes are load-bearing and invisible to the machinery.** `DEF-ROC-026`'s note
   literally has to say *"read the attribution here, not the agent field."* Any metric computed from
   the `agent` field is therefore already wrong wherever this has happened — and we know of at least
   four cases in one day, all self-reported, so the true count is a floor.
2. **A rule that cannot be followed gets worked around.** Three of the four agents did the honest
   thing and disclosed; one (flow-manager) refused outright and escalated. That disposition is
   admirable and should be praised — but relying on it is relying on every future agent choosing
   integrity over convenience, when the mechanism itself makes the dishonest path cheaper.

**Scoring, unchanged:** did an agent doing legitimate work get blocked or have to spoof a role again?
Four instances in one day is the pre-change baseline.

### EXP-140 — instance eight, and the sharpest: the role that OWNS the defect class cannot advance it

2026-08-17, ROC. `DEF-ROC-037` is **doc/test-pin drift** — a documented command diverging from the code that pins it. That class is owned by the **documenter** by design; the orchestrator dispatched it there for exactly that reason, and the documenter fixed it well (repointed the pin, added reverse and discovery arms, mutation-checked all five, and recorded the coupling in `DOCS-LAYOUT.md`).

**It then could not fire `confirmed` or `fixed`.** The defect flow reserves both to `orchestrator`/`engineer`/`cicd`. It appended them under `AGENT=engineer` with the substitution stated verbatim in each note, and flagged the gap rather than routing around it.

**Why this instance settles what the previous seven only suggested.** The earlier cases could each be read as an unusual item meeting an unusual role. This one is the opposite: **the item type and the owning role are perfectly matched — and the graph still says no.** A defect about documentation, dispatched to the documentation role, on the orchestrator's deliberate judgement, and the mechanism forbids the only role that should have it.

Tally so far, all self-reported (so a floor, not a measurement): documenter ×2 (`built_green`/`deployed`, now `confirmed`/`fixed`), tester (`deployed`), solution-architect (`confirmed`/`fixed` — no path at all), engineer (`pulled`), orchestrator (`made_ready`). **Five distinct roles, eight occasions.**

Every one cost either a **borrowed agent name with a disclaimer nobody downstream reads** — corrupting `time_by_owner` and quality-by-stage — or **an extra agent dispatch**, corrupting lead time. The mechanism makes the dishonest path the cheap one, and has relied entirely on agents choosing to disclose. They have, every time, which is creditable and is not a control.

**No ninth patch.** The replacement recorded above stands: derive firing rights from the item's declared owner; let the graph constrain transition SHAPE only.

### EXP-140 — instance nine, stated in one line

2026-08-17, ROC, `DEF-ROC-026`. The rejection's fix was an **architecture-only** change (a scoped supersession pointer in `architecture/deltas/002`). The **solution-architect** made it and was then refused, verbatim:

```
append REJECTED: DEF-ROC-026 is in state 'fixing'.
  event 'fixed' is legal here but not for agent 'solution-architect'.
  legal events from here: fixed (agents: engineer/cicd), ...
```

**In one line: an architecture-only defect fix cannot be reported as fixed by the agent that owns architecture.**

It substituted `amended` (a legal edge for its role), quoted the refusal in the note, and **left the item in `fixing`** rather than routing around it — so the item now needs an engineer or cicd to append a `fixed` event about work neither did. That is the second time in this defect's own life cycle that the same bookkeeping detour has been required.

Nine occasions, five roles. No tenth patch: the recorded replacement stands — derive firing rights from the item's declared owner, let the graph constrain transition SHAPE only.

### EXP-140 — instance TEN, on the very defect that closed EXP-142's gap

2026-08-18, ROC, `DEF-ROC-057` (the bad Config screen a human reported). The whole item is a **UI-designer** item by construction: layout allocation, table density, design-token contrast, plus the EXP-142 whole-screen sweep. It was dispatched to the **ui-designer**, which reproduced it, root-caused it to a missing house split-view template, fixed it across three surfaces, and proved the new gate fails 16/16 on the pre-fix build. Then, verbatim:

```
append REJECTED: DEF-ROC-057 is in state 'reproducing'.
  event 'confirmed' is legal here but not for agent 'ui-designer'.
  legal events from here: confirmed (agents: orchestrator/engineer/cicd), ...
```

**In one line: the role that owns UI quality cannot even record that it reproduced a UI defect** — `ui-designer` appears on NO edge of the defect graph at all, so unlike instances eight and nine it has no legal substitute edge to detour through; it cannot move the item one step in any direction.

Ten occasions, six roles, and this one arrives *inside the experiment built to stop UI defects escaping to humans* — the fix is verified and the item still cannot leave `reproducing` without an agent that did none of the work. The replacement already recorded (derive firing rights from the item's declared owner; let the graph constrain transition SHAPE only) would have made every one of the ten a non-event.

---

## EXP-132 — ADOPTED (v145, 2026-08-21, OagEventSource retro)

**Target metric:** lead time (orchestrator overhead / avoidable full-retro cost).
**Result: POSITIVE — the metric moved, measured on a single heavy session.**

`parts-check` fired **8 times** on 2026-08-20 and **drained 5 incidents**
(DEFECT-OAG-123, -130, -134, -136, -099) without paying full-retro overhead once — the constraint
was `queue` 64.0%/`open` 55.3% at every reading and genuinely never shifted, so every drain was
correct. Five avoided full retros in one day, on a day that resolved seven defects.

It also demonstrated the fail-safe direction: `retro-mark` warns when it cannot read the constraint
and the next `parts-check` then ESCALATES rather than assuming stability.

**Integration:** folded into `.claude/agents/orchestrator.md` (§Retro, opening paragraph) as plain
operating practice; EXP/vNN scaffolding stripped. Row pruned from the live registry.
**Residual noted, tracked separately:** every invocation writes a tracked marker file that dirties
the parent worktree and defers fold-forward — `OI-PARTS-CHECK-MARKER-DIRTIES-THE-TREE-AND-DEFERS-FOLD-FORWARD`,
ruled by `delta-075`, build scheduled. Adoption is of the *constraint read*, not of the marker store.

## EXP-142 — ADOPTED (v145, 2026-08-21, OagEventSource retro)

**Target metric:** change failure rate (the shrink-only test-requirement ratchet must not drift up).
**Result: POSITIVE — measured under the heaviest concurrency this project has run.**

Across **28 agent dispatches** in one session the limb-1 floor held at **1739** and limb-2 at **15**,
and **not one agent raised it** — several volunteered in their reports that they had *not*
re-baselined, and two traced a transient 1740 to another agent's file rather than moving the floor.
The auto-shrink also fired: the floor came down 1747 → 1739 and stayed.

It caught real defects rather than only counting: a **spread-override on a real capture** (an
engineer's first amendment case — "spreading an override over a real capture invents a record reality
never sent"), and untagged cases in three separate files. One engineer restructured its fixture to a
harvested prior *because* the gate refused it.

**Integration:** already live as plain practice in `process-current.md §17d.5` plus
`.claude/tools/test-requirement-gate.js`; row pruned, scaffolding needed no stripping.

## EXP-135 — ADOPTED (v145, 2026-08-21, OagEventSource retro)

**Target metric:** change failure rate (an exclusion must not read as a health verdict).
**Result: POSITIVE — the mechanism fired inside a real architecture ruling, not a test.**

`delta-072` (`DEFECT-OAG-136`, airport identity) invoked §17h **as its authority**: *"the old
`iata:string` required was an artefact of the EXCLUDED population that outlived the exclusion — the
exact §17h shape."* The excluded population was US general-aviation/unscheduled traffic, admitted by
the owner at 29.8%/31.2% of SRQ's real traffic — i.e. §17h is what let the architect see that a
passing type constraint was encoding a stale exclusion.

It fired a second time the same day: `S6` (FAA-only airfields, 5 of 965 nodes) was ruled a **COUNTED
finding with its measured size** and explicitly *"may never be described as benign"* — §17h limb 2
verbatim.

**Integration:** already live as plain practice in `process-current.md §17h`; row pruned.

## EXP-128 — ADOPTED at v147 (2026-08-21, OagEventSource retro)

**Outcome: ADOPTED. The metric moved, and the mechanism is load-bearing in the instrument the
retro itself reads.**

The row's problem was that *"the retro's own constraint instrument was 44.98% interpolation
presented as measurement"* — 138 of 282 flow items were migration backfill whose event timestamps
were synthesised by spreading a span evenly across transitions, so every state got an identical
duration, and that was pooled into the figure used to NAME the constraint.

**What it produced, now permanent in `views/stats.md`:** backfill is HELD APART and excluded from
every ranking figure; the excluded total and share are stated (`30463054 s across 136 interpolated
items, 15.02% of the naive total`); each state carries its own `backfill %` column; and the
denominator is declared as **measured dwell only**. §17f carries the reading rule it earned — *do
not name a constraint from a state whose backfill share is high*.

**Evidence it is load-bearing rather than decorative — from the retro that retired it:** v147
named `queue`/`open` as the constraint *because* those rows read **0.00% backfill**, and
deliberately did **not** name `dev-validating` (62.50% backfill), `deploying` (65.49%),
`building` (75.60%) or `ready` (81.41%) — every one of which would have out-ranked real states if
the interpolation were still pooled. Two consecutive retros (v146, v147) have made their
constraint call on the measured-only denominator, and v146's finding (that the denominator also
counted decided-and-parked inventory as waiting) was only *visible* because this row had already
separated measurement from interpolation.

**Integration:** the partition lives in the `wi-project` fold and in `stats.md`'s own header text,
not in an agent instruction — so there is no agent file to rewrite and no scaffolding to strip. The
reading rule is §17f. Row pruned at v147 to hold the registry at its 8-row cap.

**Original row text, verbatim:**

```
| EXP-128 | v132 (2026-08-06, OagEventSource) | `work-items.py` `_compute_glt` + `_is_interpolated` + the `loop-gate` backlog advisory (all LANDED this retro, 4 new tests, 209 green) + process-current.md §17f limbs 3-4 | **Problem:** the retro's own constraint instrument was **44.98% interpolation presented as measurement**. 138 of 282 flow items are migration backfill whose event timestamps were synthesised by spreading a span evenly across their transitions, so every state segment came out identical to the second (UC-14 and UC-16: five consecutive segments of exactly 304,800.0s, and byte-identical timestamps between two different items). Backfill lands only on the states migrated items walked, so pooling it biased the time-thief ranking toward exactly those stages: `deploying` read 12.30% against a measured 6.00% (73.15% backfill), `building` 10.12% against 2.03% (88.96%), `ready` 10.54% against 2.79% (85.42%). THREE consecutive retros named a delivery stage as the constraint and spent their change budget mechanising it; EXP-123 was opened to reduce `queue` and scored NEGATIVE against a share metric that cannot distinguish 'work waits longer' from 'there is more work'. v128 diagnosed that confound precisely and ROUTED the fix — *"stats.md must report median per-item dwell in registered/ready alongside the share"* — as prose in a version comment with no owner, no item and no test. It never landed: the **seventh** recorded prose-only remedy, and §17c.3 (a comment is not a control) was never applied to the retro's own output. **Solution:** segregate, never pool. `_is_interpolated` flags an item when >=3 non-zero state segments agree to within max(1s, 0.05%); its dwell is reported in its own `backfill_s` column and EXCLUDED from every measured figure, with the per-state backfill share printed beside it. Every by_state/by_owner row also carries `median_per_item_s` + `n_items` — count-independent, which is v128's owed fix implemented rather than re-routed. The `loop-gate` backlog advisory reports median in-queue AGE and names the oldest item, so a depth nobody can act on becomes an age somebody can. §17f gains limb 3 (no constraint may be named from a figure without stating its backfill share and its per-item median) and limb 4 (a metric fix a retro routes MUST land as code in that same retro). | lead time for changes (median, trailing 30d); secondary guard: CFR must not rise | Retros stop aiming at delivery stages inflated by migration and start aiming at the real constraint, so median lead time FALLS. The measured constraint at open is **`queue` 57.80%, median 166,319s/item** (within it `open` 39.73%, median 3.07d across 51 items) while every working owner is fast — cicd median **655s**, engineer **2,053s**, tester **3,723s**. Anticipated: trailing-30d median lead time below 12,430s, `queue` median/item falling, CFR not rising above 8.9%. | 3 retros | active | **Measurement:** trailing-30d median lead time is **12,430s** at open. Scored at each of the next 3 retros. **POSITIVE** only if median lead time falls AND the named constraint is attacked with an exploit/subordinate move that cites the median column, not the share. **NEGATIVE** if lead time is flat-or-worse, or if a retro again names a constraint off a high-backfill state, or if `queue`'s median/item rises. Deliberately falsifiable in the direction that matters: if fixing the instrument does not change WHERE retros aim or WHETHER lead time moves, the measurement was never the blocker and this row must be KILLED rather than re-prescribed — exactly the mistake made with v128's prose. applies-to: every project whose item substrate was migrated or backfilled, and every retro that reads `views/stats.md`. Founding: this retro's measurement + principle-failure `2026-08-06-retro-constraint-named-from-interpolated-dwell.md`. Opened cap-neutral: EXP-118 ADOPTED and archived this retro (POSITIVE x2, folded into ui-designer.md §3b as plain practice with its scaffolding stripped); registry stays at **8, AT cap**. |
```



## EXP-143 — RETIRED v148 (2026-08-24, ROC retro) — ADOPTED

**ADOPTED at v148 (ROC retro 2026-08-24) — the metric MOVED and the mechanism is causally traceable to it.** Target was `external` owner share of GLT + median time-in-`blocked`. Measured at open (v144, 2026-08-20 baseline carried in the v146/v147 parts-checks): **external 40.41% / blocked 39.11%**. Measured now: **external 35.67% / blocked 33.76%** (-4.74pp and -5.35pp). CAUSAL INSTANCES, not just a moved number: the probes' FIRST run cleared two false parks — `DEF-ROC-008` (the deployed host reported `jiraEgress.configured=keyvault`; the park's premise was gone) and **`UC-ROC-023`, blocked 27.3 days on two preconditions that were BOTH already satisfied** and now the highest-value READY item. Its own stated falsifier was: if probes come back `BLOCKER: standing` honestly and the external share does NOT fall, blockers genuinely stay closed, the ceremony is pure cost, and the row is KILLED. The share fell and the releases are attributable, so the falsifier did not fire. ADOPTION: the behaviour is already load-bearing in the machinery (`PROBE=` required on `EVENT=blocked`, `loop-gate` re-runs every park's probe each cycle, `wi-validate` I7 catches a hand-edited park) and is documented as plain practice in §17c limb 6 — no scaffolding to remove. **LIMIT FOUND AND ROUTED, recorded so adoption is not read as 'solved':** a probe re-checks a PARK; nothing re-checks a DIAGNOSIS. Two items (`DEF-ROC-053`, `DEF-ROC-081`) reached `fixing` on mechanisms that were false, which probes cannot see. That gap is now §17ab plus the 2026-08-24 principle-failure, with a candidate mechanism (an evidence `ref:` required on `confirmed`) deferred to a future scored row.

**Original row, verbatim:**

| EXP-143 | v144 (2026-08-20, ROC, gap-closing retro on DEF-ROC-073/074/075/076) | process-current.md §17c limb 6 + `.claude/skills/work-items/scripts/work-items.py` (`PROBE=` on `EVENT=blocked`, sentinels `BLOCKER: standing` / `BLOCKER: cleared`, reusing the `parse_observe_spec`/`_run_observation` machinery already proven for `OBSERVE=`) + `loop-gate` check + per-project `work/<p>/Makefile` probe targets | **Problem:** the machinery enumerates TWO park states (`_PARKED_STATES = {"blocked", "awaiting_observation"}`) and requires a machine-checkable predicate for only ONE. `blocked` takes a prose `note:` and nothing else, so a blocker is never re-tested and the park never ends on its own. `DEF-ROC-004` sat `blocked` **28.8 days after both blockers had gone** (its subscription was created the day it was raised); the blockage was falsified in ~5 minutes by trying. `external` is **46.3% of ROC GLT at median 19.3 d/item** — the largest single cost — with no detector but a human re-asking. §17c limb 3 already required a re-check and was PROSE, so Layer 2's own sentence came true against it. **Solution:** make `blocked` carry the same burden of proof as `awaiting_observation` — `append` REFUSES `EVENT=blocked` without `PROBE=make:<target>`; `loop-gate` BLOCKS on `BLOCKER: cleared` (unblock is actionable) and on a BROKEN probe, advisory on `BLOCKER: standing`. | **gross lead time** (`external` owner share + median time-in-`blocked`) | `external` share of GLT falls from **46.3%** and median time-in-`blocked` falls from **~19.3 d**, because a cleared blocker is found by a cadenced probe instead of by a human happening to re-ask. Secondary: fewer items parked on a premise that has already rotted. | 3 retros or 6 weeks, whichever is later (external waits are long; a shorter horizon cannot distinguish the effect from noise) | active (0/3) | **FALSIFICATION, stated up front:** if probes come back `BLOCKER: standing` honestly and the `external` share does NOT fall, then blockers genuinely stay closed, the probe ceremony is pure cost, and this row is **KILLED not re-tuned**. **Second, non-obvious failure mode to check FIRST at every scoring:** a probe that is cheap to write badly will be written badly — a target that always prints `standing` is a mechanism that cannot fail, i.e. this experiment reproducing the exact class it was opened against. So scoring MUST sample real probe targets for non-vacuity, not merely count that they exist (the EXP-124 coverage-theatre trap). **Applies-to predicate (§25a):** any item of any type entering `blocked` — i.e. every `EVENT=blocked` append. Does NOT apply to `awaiting_observation` (already covered by `OBSERVE=`) nor to items blocked on another tracked item (a `deps:` edge is already machine-checkable and needs no probe). **SCORED v145 (2026-08-20, ROC) — STRIKE 1, UNSCOREABLE BY CONSTRUCTION: the mechanism is NOT YET ENFORCED.** Limb 6 shipped saying `EVENT=blocked` "requires `PROBE=`" while nothing refuses; caught ~4h later by ROC itself, labelled NOT YET ENFORCED, and registered as `OI-ROC-005` (commit `912c5dc`) — §17c.5 working, but it means the row cannot move a metric yet. `blocked` is meanwhile STILL the top time thief (41.10% of GLT, median **21.7 days** across 10 items, 0.00% backfill) and its count-independent median has RISEN from 19.3d (v144) — so the target metric is going the WRONG way while the mechanism is unbuilt, which is the honest reading, not a strike against the hypothesis. `OI-ROC-005` is this cycle's exploit move and the resumed loop's FIRST pull; AC-005.6 (migrate the 12 currently-`blocked` items, not just future parks) is the limb that decides whether the 41% moves at all. Strike 2 is due at the next ROC retro whether or not the item has landed. **STRIKE 1 SUPERSEDED SAME DAY — the mechanism LANDED (v145, `OI-ROC-005`) and its first cycle recovered two items.** `wi-append` now refuses `EVENT=blocked` without `PROBE=`, `loop-gate` re-runs every blocked item's probe every invocation, `wi-validate` I7 catches a hand-edit, and all seven parked ROC items were migrated in the same change (AC-005.6, so the rule is not future-only). NON-VACUITY SATISFIED AGAINST REALITY, which was AC-005.5 and the row's own stated risk: **two of the seven were FALSIFIED on the probes' first run** — `DEF-ROC-008` (deployed `jiraEgress.configured=keyvault`; real tickets `ROC-14`/`ROC-15` had already been raised through that very pipeline) and `UC-ROC-023` (the DEV live-acceptance UC, blocked **27.3 days** on two preconditions that were both already satisfied) — while five reported `standing` against real queries (23.2d, 34.4d, 20.2d, 20.1d, 4.2h). Same family, both answers: not a constant. **The metric is NOT yet claimed.** Two recoveries do not move a median computed over 10 items and ~3 weeks each; the honest next scoring is the trailing-30d `blocked` median and the `external` owner share at the next ROC retro, and the kill condition is unchanged — if probes are written that only ever print `standing`, this row dies. WATCH FOR: a probe that measures OUR permission instead of the condition (the first `DEF-ROC-008` probe counted `az keyvault list` = 0, which is what our operator identity is allowed to SEE — DEF-ROC-046's error inside the probe built to prevent it). |


<!-- the ## section that accompanied EXP-143, moved here at v148 when the row was retired as ADOPTED. process-lint C2 caught it left behind in the live registry: a `## EXP-` section with no row is invisible to the per-project WIP cap it is supposed to be governed by. -->

## EXP-143 — `blocked` is the one park state nothing re-checks

**Opened** v144 (2026-08-20, ROC), gap-closing retro on `DEF-ROC-073`/`074`/`075`/`076`.

### The finding in one sentence

The process already had the right rule, already mechanised and already enforced — and had applied
it to **one of the two park states the machinery itself enumerates**.

### What makes this cheap and low-risk

Nothing here is invented. `work-items.py` already contains `parse_observe_spec`,
`observe_spec_in_effect`, `_run_observation`, a sentinel contract, a timeout, and the rule that
"anything else" is a BROKEN predicate which blocks. §17c.2 already treats an unrunnable probe as a
non-probe. This experiment **extends a proven mechanism to a second call site** — the same
justification EXP-135 used when it copied `test-requirement-gate`'s tag-or-justify shape.

### Why `blocked` is the right target and not a side-quest

`external` is **46.3%** of ROC's gross lead time at a **median 19.3 days per item** — the single
largest cost in the project, and `RESUME.md` names it as the constraint. Its only detector today is
a human deciding to re-ask. `DEF-ROC-004` is the proof of what that costs: **28.8 days parked after
both blockers had already gone.** Its `roc-test` subscription was created 2026-07-22 — *the same
day the defect was raised* — and the namespace IP firewall now allowlists our operator egress. The
blockage was falsified in about five minutes, by trying.

### The evidence that this is a rule and not advice

Three things, in ascending order of how uncomfortable they are:

1. **The machinery already knows both are parks.**
   `_PARKED_STATES = {"blocked", AWAITING_OBSERVATION}` — one line, both states, one predicate
   requirement between them.
2. **The stated reason for the existing requirement is not specific to observation.** `append`
   refuses `not_yet_observed` without `OBSERVE=` because *"a park whose reason is only a `note:`
   can never come back negative and therefore never ends"*. Substitute "blocked" and the sentence
   is unchanged and still true.
3. **§17c limb 3 already required the re-check, in prose.** It says an environmental premise ROTS
   and needs "a registered item that re-checks it when the environment changes". `DEF-ROC-004`
   **was** that registered item. Nothing re-checked it. So §17c Layer 2's own sentence — *"a remedy
   written as prose reproduces the defect it was written for"* — came true **against the rule that
   wrote it**. That is the strongest argument available for mechanising rather than restating.

### The generalising claim

**An externally-blocked item is not blocked; it is UNVERIFIED-blocked, until something that can
fail says so.** A blocker is a claim about the world, and the world changes without telling us. This
is `assert-real-state` (EXP-115) applied to the flow system rather than to the product — and note
that the defect this retro came from needed exactly that discipline twice: once for the diagnosis
(the reported symptom was real but its assumed cause was not) and once for the blocker.

### Honest risks, checked FIRST at every scoring

- **A probe that is cheap to write badly will be written badly.** A target that always prints
  `BLOCKER: standing` is a mechanism that cannot fail — this experiment reproducing the exact class
  it was opened against. Scoring must **sample real probe targets for non-vacuity**, never merely
  count that they exist. Relabelling every park with a hollow probe is FAILURE, not success.
- **It adds friction to the `blocked` transition**, which is a legitimate and often correct move.
  If it pushes agents to avoid `blocked` and mis-park work elsewhere, that is a KILL signal.
- **Cost is front-loaded** (writing probes) and the benefit is delayed (a blocker clearing), so an
  early negative score is expected and is NOT grounds to kill before the stated horizon.

### Falsification

If probes honestly report `BLOCKER: standing` and the `external` share of GLT does not fall, then
blockers genuinely stay closed, the ceremony is pure cost, and the row is **KILLED, not re-tuned**.

### Status at opening — NOT YET ENFORCED

The mechanism is **registered, not shipped**: `OI-ROC-005` (owner cicd + orchestrator), because
`work-items.py` is the one script every concurrent `wi-append` calls and three agents were live
mid-dispatch when this retro closed (§F2b). Registered rather than prosed per §17c.5 — **scoring
this row before `OI-ROC-005` lands would be scoring a rule nothing enforces.** First scoring is
therefore gated on that item, and its `AC-005.6` migration (the 12 items currently in `blocked`)
is part of the mechanism, not follow-up: a rule that binds only future parks leaves the entire
measured 46.3% untouched.

### Registry accounting — recorded rather than perpetuated

ROC's own **EXP-136..142 have no table rows at all** — they exist only as `##` sections in this
file — so the standing "8 active, AT cap" reading is partly an artifact of ROC's rows being invisible
to the very registry that caps them. EXP-143 is given a real row.

**And an ID COLLISION surfaced at fold-forward, recorded rather than papered over.** `main` has
authoritatively allocated **EXP-142** to a `test-requirement-gate` shrink-only-ratchet row, while
ROC carries a local `## EXP-142` section ("a screen is evaluated as a SCREEN") that never got a row.
**Same id, two different experiments.** ROC's EXP-140/141/142 were all minted against the stale
high-water v141 recorded, so this is that failure surfacing a second time. `EXP-143` itself was
verified free against `main` (whose high-water was EXP-142) before being kept, so this row is clean.

The renumbering of ROC's colliding sections is deliberately **NOT done here**: it rewrites other
retros' records, and this retro has no standing to relabel them silently (v141's own ruling that ROC
may not adopt-or-kill another project's rows cuts both ways). It is escalated as **the next retro's
first item**, together with the **per-instance experiment allowance now owed twice over**, which has
stopped being untidy and started blocking honest accounting.

---

## EXP-127 — KILLED at v149 (retro, OagEventSource 2026-08-24)

**Outcome: KILLED — 2/2 scoring opportunities, metric unmoved, because the mechanism was never
enforced.** Hypothesis PROMOTED, not discarded, to
`OI-OAG-RESOURCE-CLASS-CAPS-ARE-PROSE-AND-NOTHING-REFUSES-A-DISPATCH` (value 18 / cost 3).

**What it proposed** (§F2b, v131): declare a resource class on dispatch
(`full-store-scan`/`full-test-suite`/`type-aware-lint`/`external-api`/`light`), cap concurrency PER
CLASS independently of queue membership, and forbid a silent long-running command. Target metric
**CFR**; measurement *"agent deaths per session attributable to watchdog stalls (not API errors —
separate them)"* over 2 retros.

**Why it was killed rather than adopted or given a third strike.** It is at its declared horizon,
and it never had a chance to move anything: **nothing anywhere enforces a resource class.** This is
the `EXP-143` shape from v144 — *unscoreable BY CONSTRUCTION because nothing refuses* — reaching
its horizon. A row that cannot come back negative for the honest reason that its mechanism does not
exist is not an experiment; it is an unbuilt work item wearing a registry id.

**Why the hypothesis survives, and this is the part worth carrying.** It was **confirmed this
cycle**, not merely un-refuted. Four agents were dispatched in one turn with **no class declared on
any of them and nothing refused or warned**. Splitting the outcomes by the row's own measurement
rule:

| class | count | in the row's declared scope? |
|---|---|---|
| machine sleep (environment) | 4 | **no** — the row explicitly excludes API/environment deaths |
| deliberately stopped by the orchestrator | 3 | no — not deaths |
| **`no progress for 600s (stream watchdog did not recover)`** | **2** | **YES** |

Two watchdog-stall deaths, at four-way concurrency, on a machine also carrying four foreign ROC
emulator containers. **The prediction held and the control was absent.**

Precedent applied: v145's `EXP-140` ruling — *ten strikes and a stated fix is not a finding any
more; it is a backlog item that was invisible to the backlog.* Same treatment, one retro earlier in
the row's life, and applied by the project to its own row rather than to another instance's.

**Second independent witness already on record** (so this is not a single-session artifact):
thirteen leaked DynamoDB Local containers once drove load average to **19.85** and made a two-file
test run take **301 s instead of 877 ms — 340×**, producing reds that were green in isolation. Same
"machine as a finite resource" failure from a different direction, measured, and still uncapped.


---

## EXP-ROC-002 — RETIRED 2026-08-26 (v153, ROC retro): **KILLED on the VALIDITY AUDIT**, not on a strike count

**Outcome: killed — it was never an experiment.** §25a's validity bar rejects a row that describes work to be done, and this row's own third column says it: *"REGISTERED, not shipped in this retro (§17f.7): item `OI-ROC-006`."* The mechanism — firing rights derived from an item's declared owner rather than from the transition — was never built, so its stated kill condition (*"if instances continue after the change, the mechanism is wrong"*) could never fire: there was no change. Killing it for continuing instances would have been scoring a hypothesis against evidence it never had a chance to move.

**The finding is real and is NOT being discarded** — it lives on `OI-ROC-006`, where the work actually is, and today's three fresh instances have been added there rather than lost with this row. Retiring the duplicate registry entry is the honest bookkeeping; the row was double-counting one piece of work as both an open item and an experiment, and holding a cap slot for it.

**Evidence at retirement (2026-08-26), three new instances in one session:** the orchestrator could not append `validated` for `DEF-ROC-112` despite holding the evidence, and had to resume the tester to do it; a tester recorded `TOKENS=1` as a placeholder on `DEF-ROC-114`'s `validated` event because a dispatched agent cannot see its own `subagent_tokens`, and the orchestrator — who CAN see it — may not correct the field without spoofing `AGENT=tester`; and the same gap keeps §E's plumbing-vs-delivery split structurally stuck at 0.0% plumbing (`OI-ROC-008`).

**Original row, preserved verbatim:**

| EXP-ROC-002 | v145 (2026-08-20, ROC) — PROMOTED from the archived `## EXP-136`/`139`/`140` sections (ten instances accreted over three weeks with no row) | `process/machinery/state-graphs.json` + `.claude/skills/work-items/scripts/work-items.py` — firing rights derived from the item's DECLARED OWNER. **REGISTERED, not shipped in this retro** (§17f.7): item `OI-ROC-006`. | **Problem:** per-transition agent allowlists encode an assumption that a transition has ONE rightful owner. Measured **ten occasions across six roles** (documenter on docs-only work; cicd on an infra-owned defect; tester on verification-only; engineer on an ORDINARY use-case; solution-architect on an architecture-only fix; ui-designer, which appears on NO edge of the defect graph at all, unable to record that it reproduced a UI defect). Each was patched by widening a list, and the tenth arrived *inside* the experiment built to stop UI defects escaping to humans. Agents either STALL or append under a role they are not — and a spoofed `AGENT=` corrupts the `by_owner` attribution the retro uses to NAME the constraint. **Solution:** stop extending allowlists; derive "who may fire this" from the item's own declared owner and let the state graph constrain transition SHAPE only. | **gross lead time** — the blocked/wip component — with the count of role-spoofed or blocked transitions per 20 items as the leading indicator (10 known across the last ~60 items) | the class DISAPPEARS rather than shrinking; if instances continue after the change, the mechanism is wrong and this row dies rather than getting an eleventh patch | next 20 items | active (2/3) | **How this could be wrong (carried over from the archived section):** if any one of the ten was really a role doing work that was NOT its own, the answer is a narrower fix, not a replacement — check that first. **Do NOT score it as "those transitions now work."** Score it as: did an agent doing legitimate work get blocked, or have to spoof a role, again? <br><br>**SCORED v148 (2026-08-24) — STRIKE 1, NO EVIDENCE EITHER WAY, and that is recorded rather than dressed up.** No role-spoofed or blocked-legitimate transition was observed this cycle; equally, nothing was done that would have exercised the mechanism, so its absence is not evidence it works. **This is the 3-strikes clock and it is now running: unscored or unmoved at strike 3 and this row is KILLED, not re-tuned.** To make strike 2 informative, the next cycle must either produce a legitimate transition an agent was blocked from making (the positive case) or confirm none arose. A row that can only ever be scored 'nothing happened' is one this method deletes. <br><br>**SCORED v150 (2026-08-24) — STRIKE 2, NO INSTANCE OBSERVED, and that is reported as unmoved rather than dressed up.** No dispatch this cycle needed to record a transition its role has no edge for, so nothing exercised the hypothesis and `OI-ROC-006` remains unbuilt. Zero evidence either way. **This is the row's second consecutive unmoved scoring: the next ROC retro is strike 3, at which point §25a KILLS it** — a row that has never demonstrated anything does not get `under-question` limbo. If the mechanism matters, the way to save it is to build `OI-ROC-006` before then, not to score it again. |


---

## EXP-ROC-003 — RETIRED 2026-08-26 (v154, ROC retro): **ADOPTED — the metric moved, twice, in one day**

**Outcome: adopted.** Its bar was *"a dark deploy is named within ONE loop cycle instead of three pushes, and the deployed/trunk delta becomes a fact the loop states rather than something nobody is looking at."* Both halves were met, under the hardest available test — the condition recurred **twice** in one session and was named inside a cycle **both** times.

**First recurrence:** CI run `32939191067` left `Deploy: skipped` with three jobs red while the deployed `buildSha` stayed `cf5ab37`; the delta was read as a matter of course, traced to its cause within minutes (`DEF-ROC-115`'s quarantine collision, registered rather than left as folklore) and fixed the same turn. **Second:** five concurrent engineers pushed faster than CI concluded, three runs cancelled in a row, and trunk drifted **eleven commits** ahead of deployed — again named inside the cycle, and its root cause (`deploy.wip_limit=1` being a phantom cap) registered as `DEF-ROC-119`. Compare the founding case, which ran **three pushes** undetected.

**Integrated as plain practice, not scaffolding:** reading the deployed `buildSha` against trunk is now the routine way this loop establishes whether work shipped, and §F5e.1 question 2 generalises it — a control states its subject and proves it can reach it.

**The caveat carried forward, because adoption must not inherit a broken instrument:** `gh run watch --exit-status` returned **0** for a run whose conclusion was `failure` and whose deploy was skipped, and `gh run list` reported `pending` for a completed run. **No part of this practice may rest on the CI tool's exit code.** The trustworthy signal is the deployed `buildSha` read off the host and compared with trunk — which is exactly what `DEF-ROC-119`'s `AC-119-2` asks `loop-gate` to report as a first-class fact, so the human is not the one remembering to look.

**Original row, preserved verbatim:**

| EXP-ROC-003 | v148 (2026-08-24, ROC retro — focus question *"what was the largest contributor to gross lead time"*, answered against a finding the metrics could not see) | `.claude/tools/deploy-staleness.js` + `.claude/config/deploy-staleness/ROC.json` + `loop-gate` check 15 in `work-items.py` + `.claude/tools/deploy-staleness.test.js` (9 tests) | **Problem:** ROC's ONLY environment went **three pushes** with no deploy and NOTHING said so, while **deployment frequency read 6.57/active-day throughout**. Mechanism: a CI test job failed, `deploy-test` declares `needs:` on it, so the deploy was **SKIPPED — not failed**; a skipped job renders as a neutral dash and contributes nothing to the run's conclusion, so the run read "a test broke" when the consequence was "the environment is N commits stale". **ROOT CAUSE IS IN THE METRIC, not the workflow:** deployment frequency is a fold over ITEM EVENTS (an item entering `deploying`) — an INTENTION an agent recorded — so a push that never deploys emits nothing at all. The one DORA metric whose subject is the OUTSIDE WORLD is computed from statements about our own intentions. This is the absence-vs-ignorance family (eleven registered instances in this project) applied to the measurement layer: "no deploy event" read as "nothing to deploy" when it meant "we have no idea". Compounding it, `DEF-ROC-088` then sat in `validating` unable to meet an AC that reads *verified against the deployed host*. **Solution:** a gate check that asks the DEPLOYED HOST what it is running (`/api/health`'s `buildSha`) and compares it to trunk, reporting commits-behind, the deployed commit's age, and whether any of those commits touches a **deploy-trigger path** (the actionable subset — raw commits-behind would cry stale over a README). Three outcomes, never two: `current`/`stale` are measurements; no config, unreachable host, non-JSON body, or a sha the repo has never seen are **NOT ESTABLISHED** and say so. **ADVISORY, never blocking** — a gate blocks only on harm that stopping relieves, and refusing to pull work cannot un-stale an environment. | **deployment frequency** — specifically its HONESTY (does the reported rate correspond to artifacts actually reaching the environment); secondary **lead time** for items whose acceptance requires the deployed host | a dark deploy is named within ONE loop cycle instead of three pushes; and the deployed/trunk delta becomes a fact the loop states rather than something nobody is looking at | 3 retros | active (2/3) | **NEGATIVE if:** (a) the check never fires `stale` with `deployableChangesBehind: true` while a dark deploy nevertheless happens again — then it is measuring the wrong thing and dies; or (b) it fires so often on benign drift that it is ignored, measurable as `stale` advisories outnumbering real undelivered-work cases by more than ~5:1 — in which case the trigger-path subset is the knob, and if that cannot fix it the row dies rather than being re-tuned indefinitely; or (c) it reports `current` while the environment is demonstrably behind, which would make it worse than nothing. **The trap to check FIRST at every scoring:** a check that is always `NOT ESTABLISHED` is indistinguishable from one that works — so scoring MUST confirm it has produced at least one real `stale`/`current` VERDICT against the live host, not merely that it ran. **Explicitly NOT scored on 'did we build the tool'.** **Deliberately narrow, and this bounds the claim:** it detects that the environment is behind; it does NOT detect a deploy that succeeded and shipped something broken. Baseline at open: fired `stale`, 1 commit behind, `deployableChangesBehind: false` (correctly reading an items/views-only commit as expected drift, not undelivered work) against the live host at deployed `0154f97a` vs trunk `6ec5629`. 9 unit tests cover the stale-and-deployable case that no live run can produce on demand, plus every NOT-ESTABLISHED branch. <br><br>**SCORED v150 (2026-08-24) — STRIKE 1, POSITIVE INSTANCE on the discrimination the row was built for, NOT an adoption.** `loop-gate` check 15 ran this cycle and returned the THIRD outcome, not one of the two easy ones: ROC's deployed host reports `7d66968b2153` against `origin/main` at `71a075908b78` — **1 commit behind, deployed commit 0.4h old, and none of those commits touches a deploy-TRIGGER path, therefore EXPECTED drift and explicitly NOT a dark deploy.** That is the exact case the row's design note says a naive `commits-behind` counter would get wrong (it would have cried stale over a process-only commit). So the mechanism is live, it is being read, and it discriminates. **What has NOT happened is the adoption bar: it has not yet caught a REAL dark deploy** — the `DEF-ROC-086` class it was built for (a `needs:`-skipped `deploy-test` leaving three pushes undeployed while deployment frequency read 6.57/active-day). Until it either catches one or survives a full horizon reporting honestly, it stays active. Its stated NEGATIVE arm is untouched and still available.  <br><br>**STRIKE 2 — ROC v152 (2026-08-26). POSITIVE on the detection limb, and the condition RECURRED, which is what makes it a real datum rather than a quiet cycle.** The dark deploy happened again: CI run `32939191067` on `ccf63c2` left `Deploy Function App and Web App to AAS test: **skipped**` with three jobs red, and the deployed `buildSha` stayed `cf5ab37` while trunk had moved on — `DEF-ROC-111`'s **value-5** fix was pushed into that window and did not reach the host. **But it was NAMED INSIDE ONE CYCLE, which is this row's bar** ("named within ONE loop cycle instead of three pushes"): the deployed/trunk delta was read as a matter of course during this retro, the skip was traced to its cause within minutes, and the cause was fixed and pushed the same turn (`5cd51180`). Compare the founding case, which ran **three pushes** undetected. **THE CAUSE IS NOW A REGISTERED DEFECT rather than folklore** — `DEF-ROC-115`: the `DEF-ROC-076` quarantined probe was scanned ON DISK by `src/app`'s lint (red for everyone, four agents each paying to re-report it) while the quarantine test requires it stay UNCOMMITTED, so the two gates were **jointly unsatisfiable** and the commit that cleared the lint red necessarily tripped the quarantine. **CAVEAT ON THE SIGNAL, and it belongs on this row:** `gh run watch --exit-status` returned **0** for a run whose conclusion was `failure` and whose deploy was skipped — so the obvious automation for "did the deploy land" is itself unreliable, and the fact was established by reading the job list and the deployed `buildSha`, not the watcher's exit code. Any adoption of this row must not rest on that exit code. |
| EXP-OAG-003 | v146 (2026-08-21, OagEventSource retro) | `.claude/agents/orchestrator.md` — DORA/ToC section, triage-in-the-same-turn-you-register | **Problem:** `reported` is **11.22% of gross lead time at a median 24.1h across 84 items**, zero backfill — second only to pure queue wait, and the state's owner is the ORCHESTRATOR, so unlike queue wait it is entirely self-inflicted. It is not think-time: a defect sits in `reported` because nobody fired `triaged`. **Solution:** when registering a defect from a dispatch report that ALREADY carries its reproduction and cause, fire `triaged` (and `confirmed` where the report establishes the reproduction) in the SAME turn. The exception is preserved deliberately: no reproduction ⇒ `reported` is the honest state and triaging would be the phantom-fix failure `/defect` exists to prevent. Demonstrated same-day — `DEFECT-OAG-138` went `reported → triaged → confirmed → fixed` in one turn because the finding dispatch had reproduced it, located the cause and landed the fix. | **MTTR** (defect reported→resolved, currently median 112,902s / mean 227,196s over n=58); secondary: `reported` median/item | the `reported` median falls well below 24.1h and MTTR's reported→resolved span shortens by roughly the time defects currently idle before triage | 3 retros | active (0/3) | **NEGATIVE if** the `reported` median does not fall, **or** if it falls while `reproducing`/`fixing` rises by a similar amount — that would be the state boundary moving rather than the wait ending, which is relabelling and kills the row. **The dangerous failure mode to check FIRST at every scoring, because this change makes it cheaper:** a defect triaged WITHOUT a real reproduction is the phantom fix, so scoring must sample triaged defects and confirm each carries a reproduction that someone could re-run — a rise in `not_reproduced` or in rework on defects is this row causing harm and it dies rather than being tuned. Explicitly NOT scored on 'did the orchestrator adopt the habit'. | **ADOPTED v155 (2026-08-27, OagEventSource retro) — 3rd POSITIVE, and the target metric MOVED.** The row's claim was that triaging in the SAME TURN as registration removes orchestrator-owned `reported` dwell. Exercised **FIVE times in one cycle** (`DEFECT-OAG-142`, `-144`, `-145`, plus `DEFECT-OAG-106/100/115/129` in one pass) and every one went `reported -> reproducing` in **~60 seconds**, against the state's standing median of **79,185s (22h)**, n=89. `reported` is still 12.15% of GLT because the historical population dominates the median, which is exactly why this is scored on the NEW items' dwell rather than on the aggregate share — the confound EXP-123's share metric died of. Also note the flow-manager could NOT fire these (for a `defect`, `triaged` is orchestrator-only), so the behaviour necessarily lives in the orchestrator, which is where it is now folded. Integrated into `.claude/agents/orchestrator.md` as plain practice; scaffolding stripped.
| EXP-129 | v133 (2026-08-07, OagEventSource) | `.claude/agents/orchestrator.md` — new section "A BRIEF STATES THE PROBLEM, NEVER THE DESIGN" | **Problem:** the orchestrator delegates the decision and then specifies the answer in the brief — delegation breached in the one place it is invisible, because the orchestrator is not reading the code and the agent is. MEASURED 5-for-5 in one session (2026-08-06): a flag spec that would have made DEFECT-OAG-063 permanently unclosable (AC-063.4's prod limb was false-by-design under the flag's own default); a `deliberatelyNotEmitted` exception-set refused for a typed total projection whose completeness limb is the COMPILER (*"a set of exceptions is still opt-in"*); a `git rm --cached` diagnosis disproved by an index mtime (it was DEFECT-OAG-058's pathspec trap) after a full re-investigation the brief had caused; registering a self-inflicted regression as a NEW defect, overruled with *"recording your own unvalidated regression as a new defect launders the rework"*; and calling a 4.84-day dwell modal when it was a 640x outlier. Every one cost either a wrong build or an agent's budget spent overturning the orchestrator — both are rework and both land on `queue`, the standing constraint. **Solution:** briefs state problem + constraint + acceptance and stop; a hypothesis is labelled and made the FIRST thing to falsify; unverified beliefs are named as unverified so they are not inherited as evidence; and an overturn is recorded plainly in the item event rather than defended. | lead time for changes (median, trailing 30d) — rework at the constraint; secondary guard: CFR must not rise | Dispatches stop carrying designs, so agents spend their budget building rather than correcting, and the correction round-trip disappears from the critical path. Anticipated: the overturn rate falls well below 5-of-5, and trailing-30d median lead time falls from the 12,430s open-baseline. | 3 retros | active (2/3) | **Measurement:** count, per retro, the dispatches whose returned result OVERTURNED an orchestrator-specified design, as a fraction of dispatches that carried one. Baseline **5 of 5 (100%)** on 2026-08-06. **POSITIVE** if the fraction falls AND median lead time does not rise. **NEGATIVE** if the overturn rate holds at or above ~50%, or if briefs become so thin that agents return asking for direction (the opposite failure — under-specification is not the goal; the goal is specifying the PROBLEM richly and the SOLUTION not at all). Explicitly falsifiable: if agents overturn orchestrator designs at the same rate whether or not the brief carried one, then the brief was never the mechanism and this row must be KILLED. applies-to: every project where a non-reading orchestrator dispatches reading specialists. Founding: this retro's 5-for-5 tally + the owner's assessment *"you are flip flopping on solutions which tells me that the problem has gotten too complex for you"*.  ‖ **OagEventSource v135 retro (2026-08-07, 1st opp): POSITIVE 1/3, on a small but clean n.** Two dispatches this session carried a SOLUTION rather than only a problem, and the overturn pattern split exactly along that line. OVERTURNED: the flow-manager brief specified the mechanism ("append the birth event via `make wi-append … EVENT=registered`") and was overturned — `type: requirement` is a `kind: aggregate` and the machinery REFUSES appends on aggregates ("its state bubbles from children"); the agent verified this empirically against the real item rather than assuming it from source, and hand-carried the event in frontmatter as every existing `REQ-*` does. NOT OVERTURNED: the briefs that named only the PROBLEM — discovery (six holes to interrogate, no design) and cicd (four capability questions, no answers) — both returned work whose APPROACH stood, while correcting the orchestrator's FACTS in three places. That is the row's thesis in miniature: specifying the problem richly survives contact; specifying the mechanism does not. Overturn rate where a design was carried: **1 of 2 (50%)** against a baseline of 5 of 5 (100%). n is small and one of the two is a machinery constraint the orchestrator could have read — so this is a real but weak positive; do not adopt before a fuller sample. <br><br>**EVIDENCE + STRIKE 2 from ROC v150 (2026-08-24) — a NEGATIVE INSTANCE, and the violator was the orchestrator running this retro.** REPORTING, not retirement (shared row, §25a). Replenishing the below-floor Ready queue, my `product` brief prescribed the DESIGN instead of the problem — it dictated the literal event sequence *"`make wi-append … EVENT=created AGENT=product` then `EVENT=made_ready`"*. Both halves are wrong against `state-graphs.json`: a use-case's genesis event is `registered`, and **`made_ready` is legal only for `agent: flow-manager`**. The dispatch discovered this empirically (a REJECTED `wi-append`, correctly refused by the edge check), did the right thing — registered `CHK-ROC-012`/`SLC-ROC-027`/`UC-ROC-099`/`UC-ROC-100` complete with acceptance and reported the contract mismatch rather than working around it — and **the ready floor was still not cleared, requiring a second `flow-manager` dispatch.** Cost: one extra dispatch and ~9 min of the constraint's own wall-clock, caused entirely by a brief that specified commands it had no need to specify. The machinery was right, the agent was right, the BRIEF was wrong — which is this row's hypothesis landing on its author. Recorded because a rule the orchestrator itself breaks while holding the retro is the strongest available evidence that it is not yet plain practice. | **ADOPTED v155 (2026-08-27, OagEventSource retro) — 3rd scoring, POSITIVE, and the mechanism is now measurable rather than plausible.** The row: a brief STATES THE PROBLEM, never the design. Across **9 dispatches in one cycle** three agents overturned a design their brief had suggested, and each overturn was worth more than the brief: the `OI-PARTS-CHECK` engineer found the brief's proposed option-1 relocation SUPERSEDED by a v146 ruling and chose option 3 on the reason that retro debt is per-PROJECT while `.claude/state/` is per-TREE; the `OI-A-WEDGED-CI` engineer found the brief's "unverified WIP, do not read as green" claim STALE (the commit was already on `origin/main` and had fired live twice) and ran the suite rather than accepting either claim; the `DEFECT-OAG-141` audit engineer found the two bugs were JOINTLY UNSATISFIABLE, which no brief had framed. A brief that had specified the design would have got the design. Integrated into `.claude/agents/orchestrator.md`; scaffolding stripped.
| EXP-134 | v138 (2026-08-10, OagEventSource) | process-current.md §17g + `.claude/agents/solution-architect.md` (fault-set acceptance) + `.claude/agents/engineer.md` (generalisation-sweep ledger) | **Problem:** every §17 defect-prevention contract governs the SUCCESS path (17b executable wire claims, 17c observation, 17d test-validates-requirement, 17e gate honesty, 17f declared units), §12 acceptance requires no fault case, and the tester is adversarial only about input edges/ordering on a working path (§20). So the FAULT dimension — what happens when a write fails halfway, a resource is replaced, a marker expires, a record is poisoned, a consumer wedges — is owned by nobody. MEASURED 2026-08-10: an external reviewer produced 7 findings against OagEventSource; independent verification against live prod confirmed 6 and rated 2 of the recommended fixes HARMFUL. **5 of the 7 were fault-path defects** (DEFECT-OAG-080 partial write between two partitions where the FIRST write establishes the idempotency key so the redelivery is a silent no-op; DEFECT-OAG-083 pipe replacement losing the swap window with no outbound replay lane and zero outbound alarms; DEFECT-OAG-082 a 7-day dedup-marker TTL guarding PERMANENT feed items, so the documented DR command duplicates ~83% of the feed; a poison record; DEFECT-OAG-085 a frozen partition with no self-heal, 1 task, no health check). All five pass the happy path. Two REPRODUCED on demand once sought (080 3/3 incl. an OCC variant the reviewer missed; 082 against real DynamoDB Local) — so they were cheap to find and simply never sought. And the class RECURS undetected: DEFECT-OAG-069 fixed this exact shape in the feed-projector lane, recorded NO generalisation-sweep event, and left the identical shape in `normaliser-core` for an outsider to find. **Solution:** (1) solution-architect declares a boundary's FAULT SET as acceptance cases at the §12 gate for anything that persists/publishes/projects/checkpoints, each becoming a failing pinned test; floor of five classes drawn from the founding set; an UNDECLARED fault set is `needs-acceptance` and NOT Ready (fail closed, since the finding is that nobody asks unprompted). (2) engineer owes a generalisation-sweep LEDGER on any fault-class fix — every site of the same shape declared fixed or not-applicable-because; the ledger IS the deliverable. Routed UPSTREAM as ACCEPTANCE, explicitly NOT a red-team gate before the tester, because §F5a forbids a compensating promotion gate and EXP-123 records that misdiagnosis being made twice. | CFR (8.4% now) — specifically the fault-path share of it — plus MTTR secondary | over the next 3 slices/defects touching a persist/publish/project/checkpoint boundary: every one declares a fault set that is PINNED as tests, at least one fault-class defect is caught by OUR OWN acceptance or sweep rather than by an external reviewer or a prod incident, and every fault-class fix ships a sweep ledger. **Falsifiable in the direction that matters:** if external review or prod keeps surfacing fault-path defects while our own acceptance surfaces none, then declaring the fault set is not the lever and this is FAILED — the constraint would be that we cannot IMAGINE the fault, not that we forget to ask, and the remedy would be a fault CATALOGUE per infrastructure primitive rather than a per-boundary enumeration | 3 slices/defects touching such a boundary (or 3 retros) | active (0/3) | **applies-to:** any project with a persist/publish/checkpoint boundary — i.e. every event-sourced, streaming or messaging project (OagEventSource, AdixOut, ROC), and NOT applicable to pure-compute or read-only surfaces. Distinct from EXP-115 (live/whole-journey validation of the success path), EXP-121 (an INTERNAL control omitted in one of N compositions) and EXP-120 (facts on a wire you do not own). Founding evidence: `DEFECT-OAG-080` (reproduced 3/3, untracked until this review) and `DEFECT-OAG-069`'s missing sweep. **Opened AT cap-8 after retiring EXP-113, EXP-126 and EXP-130 (all ADOPTED): the registry was found at 10 active against a hard cap of 8, i.e. the cap is enforced nowhere; 10 -> 7 -> 8.** | **ADOPTED v155 (2026-08-27, OagEventSource retro) — POSITIVE, fired TWICE UNPROMPTED in one cycle, which is the bar for a habit rather than a rule.** §17g's fault-class sweep: on finding a fault, sweep the CLASS and commit the ledger. Neither dispatch was asked to. (1) The `DEFECT-OAG-141` audit engineer swept the scalar/list-collapse class across **9 sites**, fixed the one that was the bug (`Condition.<op>.<key>`) **and a latent second** (`Principal.AWS`, which also takes a list), and recorded 7 not-applicable with per-site reasons. (2) The `DEFECT-OAG-142` engineer swept *"where is ABSENCE treated as evidence without asking whether the thing still exists on the reference side?"* across **6 sites**, found its own fix was the only instance, and named `sequencer-guard.js` as the pattern to copy because it REPORTS the stale head instead of inferring from it. A latent bug found by a sweep nobody ordered is the outcome this row predicted. Integrated into `.claude/agents/engineer.md` + `solution-architect.md` as plain practice.


## EXP-ROC-001 — ADOPTED at v156 (2026-08-27, ROC retro)

**Outcome: ADOPTED — the metric moved, on the row's own chosen bar (defects CAUGHT, never checks ADDED).**

**Integration commit:** folded into `.claude/agents/ui-designer.md` as the section *"A screen
is accepted as a SCREEN, not as a set of conformant elements"* — experiment scaffolding
removed, behaviour intact, stated as plain operating practice.

**The decisive instance, v156.** `DEF-ROC-063` was a HUMAN report — *"the first History button
in the grid doesn't work, and seems to be overwritten by some unreadable text"* — filed
2026-08-18 and never re-reproduced. An engineer reproduced it this cycle against the deployed
host (60/60 checks, exit 0, six viewports including dark) and found it **NOT REPRODUCING**.
It then pinned what the human had actually seen, using precisely the screen-level limbs this
experiment introduced, against the pre-fix screen the falsifier still renders:

- `[reachability] rule-history` — **every** row's History control *"starts 106px BEYOND the
  right edge of config-list-panel, which paints no horizontal scrollbar"*
- rows 2-4 additionally **84px / 321px / 492px BELOW** the panel's fold — so the FIRST row's
  control was the only one visible at all, which is exactly what the human reported
- `[starved-cell]` — the Actions cell squeezed to a 92px column: *"History/Read-only wraps 4
  word(s) over 4 line box(es) (1.00 words per line)"* — the "unreadable text" half

**Why this is an adoption and not a third strike.** The element-level gate
(`@axe-core/playwright`, wcag2a/wcag2aa/wcag21aa) returned **0 violations** on that same
screen. The screen-level verdict returned 32 findings naming every limb. The human's sentence
and the screen verdict agree; the element gate and the human do not. That is the founding
hypothesis demonstrated on an independent, human-sourced instance.

**The anticipated effect's second clause also held:** *"human-reported UI defects of the 'it
passed the gate and is still bad' class fall to zero."* `DEF-ROC-063` was the last such report,
and its cause (`DEF-ROC-057`) had been fixed **22 minutes** after the human's spreadsheet was
saved. No new report of that class arrived this cycle.

**Non-vacuity, as the row demanded, is now enforced mechanically rather than by care:**
`make roc-screen-gate` runs 105/105 and its falsifier (17/17) **requires the verdict to be RED
on the pre-fix screen at all four viewports**. The row asked that the founding screen fail the
new acceptance when re-run against its pre-fix state; that is a committed test, not a promise.

**Carried into the agent file as a known GAP rather than silently dropped:** `screenVerdict` has
no OBSCURED limb — *"an actionable control whose centre hit-test returns some other element"*,
the literal reading of the human's words, is measured by nothing. The engineer that found this
declined to add a sixth limb to a module three tiers share under a defect that did not
reproduce. That judgement is endorsed and the gap is documented where the next designer reads.

**Also carried, because it invalidates measurements silently:** never launch the browser
default-headless for a geometry measurement. Playwright headless passes `--hide-scrollbars`, so
`offsetHeight - clientHeight` is 0 for every horizontally-overflowing box whatever the app does.
Measured both arms on the real deployed screen: default headless **0/10** findings,
`--hide-scrollbars` suppressed **10/10**.

**Row as retired:**

| EXP-ROC-001 | v145 (2026-08-20, ROC) — RENAMED from the colliding `## EXP-142` section registered 2026-08-18 from `DEF-ROC-057`/`DEF-ROC-058`; the section is archived, the hypothesis is unchanged | `.claude/agents/ui-designer.md` + the `ui-design-system` skill — screen-level acceptance at a SHORT viewport | **Problem:** a human opened ROC's Config screen and reported bad contrast plus a button reachable only by scrolling an inner panel — while the project's own gate (`@axe-core/playwright`, `wcag2a`/`wcag2aa`/`wcag21aa`) returned **0 violations at four viewports**. The apparatus is real and ran; it evaluates ELEMENTS against thresholds and never asks whether the SCREEN works, so 363px of inner overflow with no page-level scrollbar and a clipped PRIORITY column are invisible to it. **Solution:** a screen is accepted as a SCREEN — at a short viewport, with the whole layout in scope (space allocation, inner-overflow reachability, column clipping), not only as a set of conformant elements. | **change failure rate** (secondary: MTTR on UI defects) | at least one layout-class defect caught pre-delivery inside the horizon; human-reported UI defects of the "it passed the gate and is still bad" class fall to zero | next 3 UI-bearing use-cases | active (2/3) | **Why it is an experiment and not a fix:** it could easily come back negative — the new acceptance may just add viewport permutations and catch nothing a human would have reported (the coverage-theatre trap). Score it on defects CAUGHT, never on checks ADDED. Non-vacuity: the founding screen (`DEF-ROC-057`) must fail the new acceptance when re-run against its pre-fix state. <br><br>**SCORED v148 (2026-08-24) — STRIKE 1, and it is a POSITIVE INSTANCE on the row's own chosen bar (defects CAUGHT, never checks ADDED), but NOT an adoption.** `DEF-ROC-088` was found by RENDERING the deployed Decision Log and looking at it: `cleared` and `resolved` were missing from the dashboard's `OUTCOME_BADGE` map (7 entries against a 9-member union), so both lifecycle-CLOSING outcomes rendered through the unknown-future-outcome fallback as a grey lower-case raw word. **Exactly the coverage this row exists to test:** axe had nothing to say — text contrast was fine and the markup was valid — and no component test could catch it either, because every badge assertion checks an outcome that IS in the map plus one deliberate unknown-outcome case asserting the fallback works. The concrete artifact is `npm run screen:decision-log`. **Why this is strike 1 and not ADOPT: one defect is not a CFR movement** (CFR is 8.0%, materially unchanged), and adopting a hypothesis on a single data point is the trap the retro method warns about. Two more instances, or a CFR move, decide it. **Also recorded against the row's own coverage-theatre trap:** the mechanism did NOT come from added viewport permutations, it came from a human-equivalent look at one screen — so if this row is eventually adopted, the thing to adopt is LOOKING, not more scans. <br><br>**SCORED v150 (2026-08-24) — STRIKE 2, and the two halves point OPPOSITE ways, so they are recorded separately rather than averaged.** POSITIVE on clause 1: `DEF-ROC-091` was found by RENDERING the deployed Relink tab and reading what it said — it asserts *"Currently self-managed (owns its own rules)"* whenever `currentParent` is falsy, so `undefined` (**the probe that would establish it FAILED**) renders identically to `null` (genuinely self-managed), and the screen states a config fact it does not have. No element-level assertion can see that: every element is conformant and the SENTENCE is false. That is precisely the class this row exists for, and it is a defect caught by us, pre-human-report. **NEGATIVE on clause 2, in the same cycle:** `DEF-ROC-064` — *"it doesn't seem possible to link/break/move a site relating to a template — all buttons are greyed out"* — was **HUMAN-reported**, which is exactly the *"it passed the gate and is still bad"* class this row claims will fall to zero. It did not fall to zero; it arrived again, on the same screen family, and took three fix commits (`05fc6b3`, `aa74b6f`, `7d66968`) because the refusal shipped but a discarded catch kept it off the screen. So the mechanism DEMONSTRABLY finds defects and has NOT yet displaced the human as the finder. Not adopted. **Strike 3 is due at the next ROC retro and it is adopt-or-KILL** — the deciding evidence is one clean cycle where a screen-class defect is caught before a human reports one, not more checks added. |

### Original registered section, moved here at adoption (v156) so it is not an
### orphan in the live registry — the row it belonged to is above.

## EXP-ROC-001 — a screen is evaluated as a SCREEN, at a short viewport, or it is not evaluated

**Registered** 2026-08-18, from ROC `DEF-ROC-057` / `DEF-ROC-058` (the `/defect` gap-closing retro).

**The gap, stated so it cannot be misread as a threshold problem.** A human opened ROC's Config
screen and reported bad contrast and a button they had to scroll an inner panel to reach. The
screen genuinely is poor: half the viewport is empty while the rules table is starved into ~35% of
the width, which balloons rows (a status list wraps ONE WORD PER LINE), clips the PRIORITY column
off the right edge unreadably, and pushes `Edit`/`History` behind **363px** of inner overflow with
no page-level scrollbar to hint at them. The project's own gate — `@axe-core/playwright` with
`wcag2a`/`wcag2aa`/`wcag21aa` — returns **0 violations of any kind on that screen at four
viewports.** The apparatus is not absent or lazy: the per-use-case e2e specs run live fully-themed
axe, painted-pixel contrast, ≥24px targets and no-reflow. It is **mis-scoped**, in four ways that
compound:

1. every scan is bound to the ONE action its use-case delivered, so components pass individually
   while their **composition** fails;
2. one viewport (`devices["Desktop Chrome"]`) — and **every fault here vanishes at 1920×1080 and
   appears at 1366×768**;
3. axe's `color-contrast` rule judges **text only**, so non-text contrast (WCAG 1.4.11) is
   unchecked — measured on this screen at **1.24:1** for buttons and **1.18:1** for a row divider
   against a 3.0 requirement;
4. **nothing, human or agent, ever looks at the render.** A screenshot settled in seconds what two
   numeric probes had disagreed about.

**Which step should have caught it, and why it didn't.** The **ui-designer**. Its methodology
already carries a WCAG 2.2 AA acceptance checklist and navigation/IA + click-path budgets — the
obligations exist. What happened is that the *automatable subset* (per-action axe) was built, and
the un-automated remainder (does the assembled screen use its space, is every action reachable,
does it look right) was never re-assigned to anything. So it silently became nobody's, and the
green number made the absence invisible. This is the same shape as `DEF-ROC-042`/`045`/`046`/
`053`/`054` — a documented, invoked mechanism that asserts nothing — sitting in the UI quality gate
itself, which is why the human saw the screen before any gate did.

**The experiment.** For any UI-bearing slice, the ui-designer's acceptance must include a
**whole-screen sweep of each assembled page** (not each action), run at **at least one short
viewport — 1366×768 — in addition to the default**, carrying two assertions that need no colour
judgement and therefore cannot be argued with:

- **reachability**: for every scroll container, no actionable element may sit outside its visible
  box on first paint;
- **no clipping**: no table header or column may be cut by its container's right edge;

plus **a saved screenshot per page per viewport that the ui-designer must actually view** before
signing off. Non-text contrast (1.4.11) is measured and reported, with exemptions recorded
**visibly** as an allowlist (the `test-requirement-gate` precedent) rather than by silent omission —
because the naive check over-flags intentional subtlety, and pretending otherwise would just build
a gate everyone learns to ignore.

**Target metric: change failure rate** (secondary: MTTR on UI defects). The prediction is specific:
UI defects of the layout/reachability/contrast class should be found **by the sweep at build time
rather than by a human after delivery**. `DEF-ROC-057` is the baseline instance — reported by the
human, zero gates tripped.

**Anticipated effect.** At least one such defect caught pre-delivery within the scoring horizon,
and no repeat of a human-reported reachability or clipping defect on a screen the sweep covers. The
honest failure mode to watch for: the sweep becomes a green number nobody reads, in which case this
experiment has reproduced the very pattern it exists to close and must be scored a failure, not
renewed.

**Scoring horizon.** The next three UI-bearing slices, or 30 days, whichever comes first.

**Applies-to predicate (§25a).** Any slice or defect whose surface includes a rendered screen —
i.e. it touches `src/dashboard/**`. It does **not** apply to API-only or domain-only work, and it
does not apply to the per-action a11y specs, which stay exactly as they are: this **adds** a
screen-level tier, it does not replace the component-level one.

**Deliberately not done here.** No process version bump: no cross-agent rule changed — this
sharpens the ui-designer's own acceptance bar, which §25/§36 makes that role's to own. Bump only if
the sweep turns out to need enforcement outside the ui-designer's remit.

---


## EXP-ROC-012 — ADOPTED (v159 retro, ROC 2026-08-29)

**Result: the primary metric moved 94x, and it is the clearest score in this registry's history.**

Its own bar was *"reconcile latency at the NEXT retro's open is below 12h (baseline 37.4h, and rising for three retros)"*. **Measured at this retro's open: 0.4h across 3 commits**, and 0h after the close's fold-back. The three-retro rise (20.6h v155 -> 23.3h v156 -> 37.4h v157) is broken.

| limb | baseline | measured | verdict |
|---|---|---|---|
| reconcile latency at retro open | 37.4h, rising 3 retros | **0.4h** | **PASS** |
| active rows scored against an unbuilt slice | 1 (`EXP-ROC-004`) | **0** (paused, clock stopped) | **PASS** |
| improvement slices with no `**Status:**` line | 8 | **10 at first read -> 0 after remedy** | **FAILED, then fixed** |

**The third limb genuinely failed and is recorded as failed rather than smoothed:** at this retro's open the count had gone the WRONG WAY, 8 -> 10, because two slices opened since v157 (`IMP-033`, and the count included newly-aging ones) carried no status. The remedy applied was to give all ten a real DECISION — two SUPERSEDED, one DELIVERED-but-unstamped, seven QUEUED with reasons, of which two are recorded as BLOCKED ON THE OWNING PROJECT because ROC has no standing to retire another instance's cited slices (§25a v143/v145). Stamping them to move a number would have been the compliance-not-effect failure this registry exists to catch.

**What actually moved limb 1, folded into the orchestrator as plain practice:** commit process-layer work CONTINUOUSLY as it is produced, and run `make project-foldback` at the close — never batch to the end of a cycle. This retro produced five parent-repo commits spread across the cycle rather than one at the close, which is the entire mechanism. Checks 18 and 19 stay in `loop-gate` as the guard; the row is retired because the hypothesis is now demonstrated, not because the checks are removed.

**Guarded metric held:** CFR unchanged — neither limb touches delivery behaviour.

Original row, preserved verbatim:

| EXP-ROC-012 | v157 (2026-08-28, ROC retro — focus question *"what was the largest contributor to gross lead time"*; this is the SUBORDINATE move, and it targets the ONE producer in this system that had no gate on its output) | `.claude/skills/work-items/scripts/work-items.py` (`loop-gate` check 18 `reconcile-latency` + check 19 `retro-output-unbuilt` / `retro-output-aging` / `retro-output-unbuilt-elsewhere`) + `.claude/skills/work-items/scripts/test_work_items.py` | **Problem:** the retro is the only part of this system whose OUTPUTS nothing checks, and three independent measurements this cycle say the outputs are not landing. (1) **Fold-back:** `instance/ROC` is **12 commits and 37.4h** ahead of `main` with the integration tree CLEAN the whole time — so every one of those fold-backs would have succeeded on one command — while reconcile latency rose across three consecutive retros (20.6h v155 -> 23.3h v156 -> 37.4h v157) and each of those retros recorded step 8a as done. (2) **Improvement slices:** 33 files, **8 with no `**Status:**` line at all**, several QUEUED since 2026-06-06, and `IMP-033` — opened by the v150 retro four days ago — has its mechanism (`park_remedy`) in **ZERO lines of machinery and ZERO items** while `EXP-ROC-004` sat at strike 1 of 3 being scored against it. (3) `EXP-ROC-006`'s own problem statement already records the recurrence: an AdixOut principle-failure promised an improvement slice that was never built. **Solution:** two `loop-gate` limbs. Check 18 reads unmerged `main..HEAD` commits AND the integration tree's cleanliness, and BLOCKS only when the tree is clean (fold-back would exit 0 — one command, always available), staying ADVISORY when it is dirty (fold-back would exit 3 by design, and blocking on a remedy the loop cannot reach is the DEF-ROC-083 unsatisfiable-gate failure). Check 19 BLOCKS on an open slice cited by an ACTIVE registry row THIS project owns, is ADVISORY for another project's rows (§25a v143/v145 gives no standing) and ADVISORY for the rest of the aging queue. | **gross lead time** — the reconcile-latency component directly, and the constraint indirectly, since every retro lever that is never built is a cycle of the constraint left unpaid. Guarded on **CFR**, which must not rise: neither limb changes any delivery behaviour. | reconcile latency at the NEXT retro's open is **below 12h** (baseline 37.4h, and rising for three retros); the count of active rows scored against an unbuilt slice is **0** (baseline 1 — `EXP-ROC-004`, now PAUSED not scored); the count of slices with no `**Status:**` line **falls** from 8. | 3 retros | active (0/3) | **NEGATIVE — kill, do not re-tune — if any of:** (a) reconcile latency does NOT fall, meaning fold-back was never skipped for want of a reminder and the real cause is elsewhere; (b) the block is routinely cleared by marking slices terminal rather than by building or genuinely closing them — satisfying the control without achieving its purpose, this project's most-registered failure family, and the reason check 19 keeps reporting a paused row in `retro-output-aging` every run rather than letting it vanish; or (c) check 18 fires while the remedy is unavailable, i.e. it blocks a tree that cannot fold back — that is the DEF-ROC-083 failure and it kills the limb outright rather than earning a threshold tweak. **This row must be scored on the LATENCY and the COUNTS, never on whether the limbs exist.** |


## EXP-ROC-009 — KILLED at strike 2, on its own stated terms (v160, ROC 2026-08-29)

v157 wrote the death condition itself: *"with the commitment that if the `reported` median does not fall at v158 the row DIES at strike 2 with no further argument."*

| | v157 baseline | v160 measured | verdict |
|---|---|---|---|
| `reported` median/item | **46,703s** (13.0h) | **73,588s** (20.4h), n=112 | **ROSE — killed** |

**The row is killed rather than argued for, because it forbade the argument.** That is the point of writing a death condition in advance, and honouring it is worth more than this particular hypothesis.

**But one measurement artifact must be recorded, or the next retro will read this wrong.** This cycle took **28 declines** — deciding aged intake inventory that had been sitting for up to 14.7 days. Deciding an item moves it out of `reported` into a terminal state, which ADDS its whole accrued dwell to the completed-item median. So doing exactly the throughput work the owner asked for **mechanically raises this number in the short run**. The rise is therefore NOT evidence that the work was wrong, and it is NOT a reprieve for this row either: the hypothesis was that a minimum defer horizon would make arrivals carry decisions that schedule, and nothing here shows it did.

**What replaces it:** §F9d and §F9d.2 (v158) attack the same constraint from the ARRIVAL side rather than the decision-quality side — which is the diagnosis this row's failure supports. §F9d.2's measurement (trailing-14-day arrival:completion ratio) is deliberately count-independent in a way this row's median was not.

**Successor guidance for whoever scores §F9d:** read the ratio, and read `reported` median only alongside the decline volume for the same window. A median over completed items is confounded by exactly the act of clearing a backlog.

Original row, preserved verbatim:

| EXP-ROC-009 | v156 (2026-08-27, ROC retro — focus question *"what was the largest contributor to gross lead time"*; this is the SUBORDINATE move for the exploitable constraint) | `process-current.md` §F9b + `.claude/agents/orchestrator.md` | **Problem:** the orchestrator registered **seven** findings in one cycle (`DEF-ROC-125` … `DEF-ROC-131`, `OI-ROC-012`) and triaged **one** — piling six items onto `reported`, the state the same retro then named as the exploitable constraint (**29.78% of GLT, median 8.7h/item, n=98**). Every finding was real and §F8a rightly forbids closing one to shrink a number, so the waste is not the finding: it is that discovery and decision were two acts, and the second may never come. The discovering role always holds the context to decide. **Solution:** a finding is registered WITH its triage decision in the same act — triage now, an explicit dated defer with a near date tied to a named precondition, or a decline with a reason. Intake gains NO cap (Little's Law governs WIP not backlog, v126); the rule acts on the ARRIVAL. | **lead time** — specifically the `reported` state share (29.78%) and its median dwell (31,485s) | the median dwell in `reported` FALLS, because arrivals carry their decision instead of waiting for the orchestrator's next sweep; and the count of items entering `reported` with no decision trends to zero | 3 retros | active (1/3) | **NEGATIVE — kill, do not re-tune — if any of:** (a) the `reported` median does not fall while registration volume holds, meaning the dwell was never caused by the decision gap; (b) findings stop being REGISTERED — if the cost of deciding at registration suppresses discovery, that is strictly worse than the inventory it was meant to prevent, and is the §F8a violation this rule must not cause; measure it as findings-registered-per-cycle, which must NOT drop; or (c) decisions become reflexive far-dated defers, i.e. the rule is satisfied without moving anything — the same control-satisfiable-without-achieving-its-purpose family as v155 §F8a.2, checkable because §F8a.2's total-age ceiling now catches exactly that. **Score on the median, never on whether the rule was recited.** **v157 STRIKE 1 — NEGATIVE ON THE METRIC, cause MEASURED, mechanism REPAIRED, row NOT killed. Read the reasoning, because it declines a literal application of condition (a) and must be held to that.** COMPLIANCE IS 9/9: every defect registered since the v156 close carried a decision in the SAME COMMIT that created its file — verified with `git log -S defer_until`, not from the item text, which carries no timestamp for a frontmatter scalar. THE METRIC WENT THE WRONG WAY ANYWAY: `reported` rose **29.78% -> 31.32%** of GLT and its median rose **31,485s -> 46,703s (8.7h -> 13.0h)**, becoming the #1 owner. THE CAUSE, which neither this row nor v156 anticipated: **six of the nine decisions were the identical `defer_until: 2026-08-28`, written in one batch, expiring inside 13 hours.** Condition (c) foresaw reflexive FAR-dated defers and pointed the total-age ceiling at them; what happened was the mirror — reflexive NEAR-dated defers, which a 30-day ceiling on a one-day-old item can never reach. The guard was aimed at the wrong end of the range. Condition (a)'s antecedent therefore holds while its stated meaning (*the dwell was never caused by the decision gap*) is FALSIFIED by direct measurement: the gap was closed and the dwell did not fall, because the decision vocabulary let it reopen in 24h. REPAIRED IN THIS RETRO, and folded into this row rather than opened as a new one because it is the same hypothesis with teeth: `_defer_is_decision` (a defer under 7d in the future is not a decision, since the gate grants that window for free anyway) plus `loop-gate` check 17 `undecided-arrival`, which reads the CYCLE clock where check 4 reads 7 days. **THE COMMITMENT, so this is not a wriggle:** if the `reported` median does not fall at v158 with the horizon rule in force, this row DIES at strike 2 with no further argument. Guard reading: findings-registered-per-cycle **9 -> 1** this cycle, but that is a cycle whose whole output was machinery, NOT evidence of suppression; it is the number to watch hardest at v158. |


## EXP-ROC-007 — ADOPTED (v163 retro, ROC 2026-08-29), with a guard movement recorded rather than hidden

**The hypothesis was that capacity EXISTED and the cap was refusing it.** That is now settled beyond doubt.

| | baseline | v163 | |
|---|---|---|---|
| `wip` occupancy actually reached | 1 of 8 (v159) | **8 of 8** | the cap bound nothing |
| intake depth | 42 | **24** | **POSITIVE** (the row's named condition) |
| completions, trailing 14d | 83 | **119** | +43% |
| trailing-30d median lead time | — | 3,066 s | held |

**GUARD MOVEMENT, stated plainly because the row said "CFR and rework flat" and CFR is NOT flat:** CFR rose **7.8% -> 8.8%** and `dev-validating` rose **8.4% -> 9.2%**.

**Why this is adopted anyway, and the reasoning is falsifiable rather than convenient:** the CFR rise is attributable with SPECIFIC EVIDENCE to `EXP-ROC-008`'s intended mechanism — failures becoming *recordable*, which that row predicts as its GOOD outcome. Two `build_failed` events were recorded this session that a less honest cycle would have swallowed: `DEF-ROC-149`'s engineer volunteered both of its own CI reds and asked for the events to be fired. A CFR that rises because more real failures are being written down is not a quality regression.

**If that attribution is wrong, this adoption should be revisited.** The test is next retro: if CFR keeps rising with no comparable attribution — no volunteered failures, no newly-recordable class — then the raise did cost quality and this archive entry is the place to start looking.

**What is folded in, and where:** nothing new. §F9d.1 (v159) already carries the adopted behaviour — *a pull of K < `wip_limit` is a FINDING about the dependency model, not an outcome; look ACROSS TYPES before concluding the set is full; resource-class exclusivity must be named and rare, not the accidental default.* This row is retired as **superseded by adoption**: the knob is set, the practice is written, and keeping a registry slot for a settled question is what the cap exists to prevent.

**Its own successor is `EXP-ROC-015`**, which asks the next question this result raises: 8x occupancy bought only 1.43x throughput, so what is now binding?

Original row, preserved verbatim:

| EXP-ROC-007 | v153 (2026-08-26, ROC retro — focus question, the owner's: *"is a wip limit of 4 too high or too low; if it is too low and we can work on more things without collisions up it to 8"*) | `work/ROC/queues/policy.csv` (`wip.wip_limit` **5 &rarr; 8**) | **Problem:** the cap BOUND a real pull today — WIP hit 5/5 and `DEF-ROC-113` could not be pulled — while only **3** agents were actually working, so capacity existed and the cap refused it. Two measured reasons the cap and the capacity disagree. FIRST, **`wip_limit` counts OCCUPIED items, not ACTIVE ones**: `UC-ROC-102` held a slot for **12.0h** in `deploying` with zero agents on it (against a `deploying` median of 166s, n=52), and the gate's own line already distinguishes `occupied = N active / M idle`. SECOND, the correction the owner should have from this retro rather than a number alone: **the agents doing the work are 13.05% of gross lead time** (engineer 8.69% at a median 1379s/item, tester 4.36% at 1378s), while `blocked`/`external` is **33.47%** at a median **7.5 d/item** and intake `reported` is **26.60%** at 12.4h. Parallelising the 13% cannot move lead time much; this is elevating a NON-constraint, taken deliberately because the cap was refusing free capacity, not because it is the binding lever. | **Solution:** raise `wip.wip_limit` 5 &rarr; 8. **ONE knob only** — `ready.wip_limit` stays at 4 even though it is the likely next binding constraint, because tuning two at once makes neither attributable. v152's `loop-gate` check-1 extension to `deploying`/`prod-deploying` is what makes the raise safe rather than reckless: it stops slots silting up with idle items, which is the failure mode a bigger cap would otherwise amplify. | **lead time for changes** (median + p85); guard metrics **CFR** and rework rate, which must not rise | **BASELINE:** engineer 8.69% / tester 4.36% of GLT; `blocked` 31.32%; overall rework 3.5%; use-case `dev-validating` failure rate 8.4% (9/107). **POSITIVE** if trailing-30d median lead time falls or holds while intake depth (42) falls, with CFR and rework flat. | 3 retros | active (2/3) | **NEGATIVE — revert to 5, do not re-tune — if any of:** (a) **collisions rise super-linearly.** At width 3 today there were FOUR, all handled with zero work lost, but two are STRUCTURAL and scale ~O(n&sup2;) in concurrent agents: the shared git index held foreign staged work twice, and BOTH engineers edited the same two architecture registries (`architecture/dependencies/class-deps.mmd`, `edge-ledger.md`) — every source-changing item touches those, so 3 agents give 3 pairs and 8 give **28**. `commit-isolated` removes the index race but CLAUDE.md limit 1 is explicit that it does NOT protect co-owned files. (b) **False reds from machine load.** `def016HealthBuildIdentity` timed out in the full run today and was **19/19 green in isolation**; the precedent is non-linear (13 leaked containers &rarr; load 19.85 &rarr; a two-file run at **301s instead of 877ms, 340&times;** &rarr; four agents killed in a row). A false red costs a whole diagnosis, so this is the limb most likely to fire. (c) lead time p85 RISES — Little's Law: WIP = throughput &times; lead time, so raising WIP without raising throughput just lengthens the queue, which is the textbook outcome when you elevate a non-constraint. **Explicitly NOT scored on 'did we raise the number'.** applies-to: any project whose WIP cap counts occupied rather than active items AND whose agents share one working tree. **Follow-on already named, not built:** make the cap count ACTIVE items — the machinery already computes the split, and that would make '8' mean 8 things being worked rather than 8 things in flight. **Housekeeping done here:** `EXP-ROC-005` was an ORPHAN — three `wip.*` knobs cited it and no row existed in the registry or the archive, so the current limit had never been scoreable; those knobs now cite this row.  <br><br>**FIRST EVIDENCE, same day, at width 4-5 (2026-08-26). The predicted collision class is REAL and it is limb (b), not limb (a).** `commit-isolated` held perfectly — four concurrent engineers, zero swept commits, and the DEF-ROC-110 engineer independently verified `git diff HEAD` on a co-owned file showed only its own line. **What degraded instead was every SHARED-TREE GATE**, because each one scans the working tree and therefore reports the UNION of all agents' uncommitted work rather than the state of trunk. Four measured instances in one session: (1) `check-docs`/`def042-mmd-renderability` went red from the DEF-ROC-112 engineer's in-flight `class-deps.mmd` label, costing the DEF-ROC-114 engineer a verification detour (it correctly re-checked against an isolated copy rather than trusting the shared result); (2) `def016HealthBuildIdentity` timed out under load and was **19/19 green in isolation**; (3) `make typecheck` went red on TWO untracked files belonging to the DEF-ROC-085 and DEF-ROC-116 engineers, reported by the DEF-ROC-110 engineer as not-its-own; (4) `test-requirement-gate` read **1143 against a 1142 ratchet** for one engineer and **PASS at 1142** minutes later, because the extra case was another agent's file passing through the tree. **NONE of these reached CI** — all four files were untracked, and trunk stayed green. **THE COST IS NOT CORRUPTION, IT IS ATTRIBUTION**: every agent that meets a red must first establish whose it is, and three of the four did exactly that and paid for it. This sharpens the row's own kill condition (b) — the failure mode at width is not lost work but *gates that can no longer answer 'is this mine?'*, which scales with the number of agents holding uncommitted files, not with the number of items. **NOT yet a revert trigger**: zero work lost, trunk green throughout, and four defects delivered in the session. Watch whether the attribution cost per agent rises at 6-8. **Candidate mitigation, cheaper than reverting the cap:** a gate invoked by an agent should report which findings belong to paths that agent declared, the same way `commit-isolated` scopes a commit.  <br><br>**SECOND EVIDENCE, same day — the row's revert condition (c) FIRED, and investigating it EXONERATED the cap.** Five engineers ran in parallel. **The BUILD half succeeded outright**: five fixes landed (`DEF-ROC-085/105/110/115/116`), zero work lost, `commit-isolated` held, and the one co-owned-file collision (`class-deps.mmd` swept into a neighbour's commit) cost nothing but ordering. **The PUSH/DEPLOY half broke completely**: runs `32948954934`, `32949101341` and `32949232206` were each **cancelled** by GitHub's concurrency group as the next push superseded them — correct, ordinary behaviour — and with CI at ~7-9 minutes **no run survived to reach its Deploy job**. Trunk reached `fb526c19` while the host still served `b1d33b49`: **nine commits undeployed**, unreported. **THE TEMPTING CONCLUSION IS WRONG AND THIS ROW MUST NOT ADOPT IT.** Raising WIP did not create a collision problem; it **exposed a serialisation control that was never wired**. `policy.csv` declares `deploy,wip_limit,1` and **NO state maps to a `deploy` queue** (measured: the queue map's codomain is `intake, ready, rework, waiting, wip`), so that cap is unenforceable by construction and `loop-gate` check 3 can never fire on it. `deploy` already has the RIGHT NUMBER declared — it has no queue. Reverting `wip` to 5 would slow build parallelism that demonstrably worked, to protect a pipeline stage that should simply be single-file. Registered as **`DEF-ROC-119`** (value 5), with the generalised fix — an orphan check over `policy.csv` — because **`EXP-ROC-005` was the same class found the same day** (three live knobs citing a row that exists nowhere, so the limit in force had never been scoreable). **DECISION: the cap STAYS at 8; the deploy serialisation gets built.** The honest scoring note is that this row can no longer be scored on lead time alone until `DEF-ROC-119` lands, because a dark deploy corrupts the very measurement — that is a CONFOUND on this experiment, declared now rather than discovered at the next retro. <br><br>**SCORED v156 (2026-08-27) — STRIKE 1, no collision at width, but the honest reading is that width was never the binding constraint.** Peak concurrency this cycle was 5 live dispatches with `wip` at 6-7 against the raised limit of 8, and **zero collisions were recorded**. Two potential file collisions were avoided by agents DECLARING claimed paths in their briefs and reporting rather than merging (one engineer stopped and reported; another deliberately left `package.json` alone because a peer held it). So the raise did not hurt. But §F reports agent work-effort at **0.17% of GLT**, so capacity was idle 99.83% of the time and a WIP limit was never what bound throughput — which means this row cannot demonstrate a lead-time gain no matter how wide it goes. Next scoring should consider KILLING it as unfalsifiable-in-context rather than re-tuning. **v157 STRIKE 2 — UNMOVED, and the reason is the interesting part.** `wip` occupancy was **1 of 8** all cycle (`DEF-ROC-123`, and it had NO recorded activity for 23h of that), so the raised cap did not bind once. The row asked whether 8 beats 5; the measurement says the binding constraint is not the cap at either value — it is that almost nothing is IN flight. §B this cycle: `orchestrator`/`reported` **31.32%** and `external` **30.95%** against engineer 7.81% + tester 4.17% = **11.98%** for everyone doing the work. Two strikes with no signal; at strike 3 this row is KILLED, not re-tuned, and the honest finding recorded instead is that WIP width was never the lever. |


## EXP-ROC-008 — ADOPTED (v164, ROC 2026-08-29). §F5e is now plain practice.

**Primary condition, decisively met.** *"POSITIVE if CFR RISES toward its true value as failures become recordable (a rise is the good outcome here)."* CFR **7.4% -> 8.8%**. And the rise is attributable rather than drifting: two `build_failed` events were recorded this session that a less honest cycle would have swallowed — `DEF-ROC-149`'s engineer volunteered BOTH of its own CI reds and asked for the events to be fired.

**Second condition — *"materially fewer control faults found BY ACCIDENT because they were caught deliberately"* — met, with the count shown rather than asserted.** Of the control faults surfaced this session:

| found DELIBERATELY, by a §F5e-shaped question | how |
|---|---|
| 4 silent `historical` mutations (`DEF-ROC-143`) | an exhaustive 672-pair sweep, not a spot check |
| `git log --all` ref-set divergence between lanes (`DEF-ROC-149`) | *what do you actually measure* — asked of the tool's own input |
| the deploy-lane limb answering about the wrong run (`DEF-ROC-142`) | *what would make you fire* — three RED scenarios built first |
| the `markerScope` two-field residue | the engineer disclosed its OWN remaining hole |
| indentation is not a wrapped-line signal | the engineer MEASURED the orchestrator's suggested fix before building it |
| `browser.css` host-dependence (`DEF-ROC-150`) | a deliberate consequence of running the tier on a second host |

| still found BY ACCIDENT | how |
|---|---|
| the live probe's mount-before-fetch race | a CI run was cancelled, forcing a re-run |
| `DEF-ROC-144`'s permanently-lost event | a tester's first probe attempt happened to use a different payload |

**Roughly 6 deliberate to 2 accidental**, against a baseline where **nine faults were found in one session and NOT ONE by anything looking for them.** That is the shift the row predicted.

**Honest qualification, recorded rather than smoothed:** the two accidental finds were among the most consequential of the session — the probe race invalidated two greens I had already reported, and `DEF-ROC-144` was permanent silent data loss. §F5e shifted the *proportion*; it did not eliminate the class. A future retro seeing accidental finds dominate again should reopen this rather than assume it was settled.

**Folded in as plain practice:** §F5e's three questions — *what would make you fire*, *what do you actually measure*, *what can you NOT say* — are already written into the process body and are cited by name in dispatches. §F9f (a live probe is not evidence until run adversarially) is its direct descendant and carries the mechanism forward. The registry slot is released because the question is settled, not because the practice stops.

Original row, preserved verbatim:

| EXP-ROC-008 | v154 (2026-08-26, ROC retro — focus: nine controls found faulty in one session, every one reading healthy) | process-current.md §F5e + `DEF-ROC-119` (orphan check) + `DEF-ROC-120` (event reachability) + `DEF-ROC-117` (three gate blindnesses) | **Problem:** in ONE session, **nine** controls were found faulty and **not one** was found by anything looking for them — all were tripped over by agents doing other work. Three kinds. **PHANTOM** (declared, never wired): `deploy,wip_limit,1` with **no state mapping to a `deploy` queue**; `EXP-ROC-005` cited by three live knobs with **no row anywhere**; §F5a's push-and-dispatch-are-one-act checked by nothing (three testers left validated state on one disk); `wi-append`/`wi-project` writing item files **nothing commits** (a day of registrations local-only while `wi-validate` read clean, because it reads the disk). **MISCALIBRATED**: `paintsScrollbarX` measuring Playwright's `--hide-scrollbars` rather than the app — it mis-filed a value-5 defect against three screens; `make-refs-tracked` reading a guarded `if [ -f X ]` as an invocation; the TRG parser reading `test.skip(cond,msg)` as a bare test; reachability inspecting only below/right. **VOCABULARY-LIMITED**: `deploy_failed` illegal from `validating`, so a change failure during validation is unrecordable **by any role** and CFR reads a false 0%; a blocker probe correctly answering *NOT OBSERVED* reported as **unreadable** because the contract admits only `standing`/`cleared`. | **Solution:** §F5e — three questions asked of every gate, probe, cap and metric before it is called done: **what would make you fire** (demonstrate it red, generalising EXP-122's non-vacuity limb beyond tests), **what do you actually measure** (subject reachable; a probe carries a positive control), and **what can you NOT say** (an expressible not-established answer, and a legal way to record bad news). Plus §F5e.2: a declared control with no possible subject is **deleted or wired in the same cycle**. | **change failure rate** — primary, because CFR is provably under-reported today and the fix makes it recordable; secondary **MTTR** (a control that cannot fire lengthens time-to-detect). | **BASELINE:** CFR 7.4% all-time (known low — `DEF-ROC-120` blocks one class of failure from being recorded at all); nine control faults found this session, **zero** by a control-directed check. **POSITIVE** if CFR RISES toward its true value as failures become recordable (a rise is the good outcome here), and if the next comparable session finds materially fewer control faults *by accident* because they were caught deliberately. | 3 retros | active (2/3) | **NEGATIVE — kill, do not re-tune — if any of:** (a) §F5e becomes a checklist recited in notes with no demonstrated red — audit: sample controls touched in the horizon and count how many carry an actual failing-case demonstration versus a claim that they would fail; (b) the count of accidentally-discovered control faults does not fall, meaning the three questions are not being asked where controls are actually built; (c) it slows control authorship enough that people stop adding gates — the opposite failure, and the one to watch, since this project's gates have repeatedly earned their cost. **NOT scored on 'did we write §F5e'.** applies-to: any project whose delivery is guarded by committed gates, probes and caps rather than by review. **Non-vacuity of the row itself:** if CFR does not move and no control-fault count changes, this was ceremony and it dies. <br><br>**SCORED v156 (2026-08-27) — STRIKE 1, and the honest signal is NEGATIVE for §F5e as a DETECTOR while strongly CONFIRMING the class it names.** Six new instances of control-reads-healthy-while-the-thing-it-guards-fails were found this cycle (`DEF-ROC-125` loop-gate reporting 3 containers clean at load average 56; `DEF-ROC-126` board-sweep printing nothing outstanding over 190 stale items; `DEF-ROC-127`; `DEF-ROC-128`; `DEF-ROC-129`; `DEF-ROC-130`). **Not one was found by asking §F5e's three questions.** Every one was found by USING the control in anger and noticing the answer was impossible — a build failure whose recovery read n=0, a resume file that had lost 37 ids, a retro counter reading 0/3 after two real closes. That is evidence the taxonomy is right and the interrogation ritual is not what surfaces instances. Next scoring must find at least one instance surfaced BY the questions, or kill the ritual and keep the taxonomy as plain practice. **v157 STRIKE 2 — WEAK POSITIVE, direction correct, magnitude small.** CFR **7.4% -> 7.8%** all-time, which is the RISE this row predicts as its good outcome (failures becoming recordable). Corroborating and directly attributable to §F5e's second question (*what do you actually measure*): `DEF-ROC-140` was found this cycle by asking what the test-requirement gate's parser could not see, and the answer was EVERY PLAYWRIGHT SUITE — `test.describe(...)` matched no branch of its call regex, so 16 suites in 11 files were invisible and 7 cases mis-counted. That is exactly the miscalibrated-control class this row was opened on, found by directed asking rather than by tripping over it. Not scored higher because a 0.4pt CFR move over one cycle is not yet a trend. |

---

## EXP-ROC-006 — KILLED at strike 3 (v172, ROC 2026-08-29)

**Outcome: KILLED — never demonstrated a metric move.** Three scoring opportunities, no measurable effect on the provably-done-work-parked-short-of-a-tester wait it targeted. In the v171/v172 session `loop-gate` check 1 did not fire once — but that is unscoreable rather than positive, because every tester was dispatched promptly, so the check had nothing to catch. A row that cannot be told apart from "nothing happened" at its third opportunity is exactly what the 3-strike rule exists to retire.

**THE MACHINERY IS RETAINED AS PLAIN PRACTICE, NOT REVERTED** (§25a): check 1's scope over `deploying`/`prod-deploying` on the same ref-bearing evidence bar is sound, cheap, and load-bearing — it stays in `work-items.py`. What is retired is the HYPOTHESIS ROW and its claim to a DORA move, not the code. Its founding case (`2026-08-26-roc-uc-102-shipped-100pct-broken-behind-a-read-only-live-probe.md`) stands.

Original row, preserved verbatim:

| EXP-ROC-006 | v152 (2026-08-26, ROC retro — focus question, the owner's: *"how did you think the simulator worked if you then tested it above and it did not — this means that after building and pushing to dev the tester did not do its job"*) | `.claude/skills/work-items/scripts/work-items.py` (`DEPLOY_PENDING_STATES`; `loop-gate` check 1 scope + state-dependent remedy) + process-current.md §F5d + `process/principle-failures/2026-08-26-roc-uc-102-shipped-100pct-broken-behind-a-read-only-live-probe.md` | **Problem:** `UC-ROC-102` shipped the Simulator's Publish control **100% unable to publish** (every screen publish a `400`, from ship until a human reported it — `DEF-ROC-111`) with every gate green. The owner's question is the mechanism: **no tester was ever dispatched, and none could be.** The item sat in `deploying` for **12.0h** against cicd's own `deploying` median of **166s** across 52 items (**260x**), because ROC deploys by PIPELINE and under a pipeline deploy no agent fires `deployed` — the orchestrator owns it and missed it, so the item was parked short of validation BY CONSTRUCTION. And nothing named it: `loop-gate` check 1 closes the window at 4h but covers VALIDATING states only, while check 11 covers `deploying` at 24h — **12.0h fell in the gap between the two thresholds**, so the same gate run that BLOCKED on `UC-ROC-104` at 11.5h in `dev-validating` said nothing about it. **This is a RECURRENCE**: identical mechanism on AdixOut 2026-07-22 (`UC-ADIX-015`), whose principle-failure entry promised an improvement slice that was never built. | **Solution:** extend check 1's scope to `deploying`/`prod-deploying` on the SAME evidence bar it already uses — a ref-bearing done-work event proving the work is finished — so provably-done work parked short of a tester is named at 4h in every state rather than 4h in three states and 24h in two. The remedy text is **state-dependent**: at a deploy-pending stall it names `EVENT=deployed AGENT=cicd` and the same-turn tester dispatch, because "dispatch the tester" has no legal edge from `deploying` and a remedy the sole writer REJECTS is the `DEF-ROC-084` class (its aged-backlog remedy named an event `wi-append` refuses, 7/7). | **lead time for changes** (the `deploying`→`validating` wait); secondarily **CFR** (a defect reaching a human because no tester ran is a change failure the metric never saw) | **BASELINE, this cycle:** `UC-ROC-102` 43,213s in `deploying` and named by nothing; cicd `deploying` median 166s (n=52); use-case `dev-validating` tester failure rate **8.4% (9/107)** — the tester DOES reject when dispatched, so the deficit is dispatch, not rigour. **POSITIVE** if, over the horizon, no item dwells >24h in a deploy-pending state unnamed, and the p85 use-case lead time falls or holds. | 3 retros | active (2/3) | **NEGATIVE — kill, do not re-tune — if any of:** (a) it fires mostly on items where the deploy genuinely had NOT landed, i.e. it is reporting normal deploy latency as a stall and 4h is simply the wrong threshold for a state whose median is 166s — then the answer is a per-state threshold, not this scope change; (b) the block is cleared by firing `deployed` WITHOUT confirming the CI run, which would convert a real gate into a rubber stamp and is strictly worse than silence (audit: any `deployed` event whose note cites no run id or whose ref is not on trunk); (c) items simply move the stall one state right, dwelling in `dev-validating` instead — the dispatch was never the constraint and capacity is. **NOT scored on 'did we widen the set'.** **NON-VACUITY PROVEN at landing** (§EXP-122 limb (ii)): with `STALL_STATES` reverted to `VALIDATING_STATES`, **3 of the 5 new tests fail** (`test_stalled_deploying_with_a_ref_blocks`, `test_stalled_deploying_remedy_names_the_deployed_event_not_the_tester`, `test_deploy_pending_states_are_a_subset_of_stall_states`); restored, the full parent suite is **572 OK (18 skipped)**. applies-to: every project whose deploy is pipeline-triggered rather than agent-run — i.e. any project where no agent fires `deployed`. **Companion rule routed WITHOUT a row** (§25a: a fix is not an experiment): §F5d.1/.2/.4, that a tier which cannot exercise the change is not confirmation of it, that a two-sided contract gets ONE declared list, and that you run the whole tier before calling it green. <br><br>**SCORED v156 (2026-08-27) — STRIKE 1, POSITIVE instance on the row's own bar.** `deploy-staleness` was consulted three times this cycle and made exactly the discrimination the row exists for: at one point it reported `verdict=stale, behind=10` but **`deployableChangesBehind: false`** — 10 commits behind with NO deploy-triggering path among them — so no deploy action was owed and none was taken. A naive behind>0 check would have manufactured work. It also read `verdict=current, behind=0` correctly at the cutover. Not an adoption yet: the row's negative condition (fires mostly where the deploy genuinely did not matter) needs a wider sample. **v157 STRIKE 2 — UNMOVED, no sample.** Check 1's `deploying` scope fired zero times this cycle because zero items entered `deploying`: the cycle's delivery was `DEF-ROC-140` plus three loop-gate limbs, all parent-repo machinery with no deploy stage. Recorded as UNSCORED-therefore-a-strike per §25a's three-strike rule rather than quietly skipped, which is how a row lives for ever without evidence. NOT negative: the mechanism is wired and demonstrable, it simply had no subject. |

---

## EXP-ROC-013 — ADOPTED (v173, ROC 2026-08-29)

**Outcome: ADOPTED — the metric moved, and it moved on the mechanism rather than around it.**
Target was *lead time — specifically the `blocked` share of GLT and the count of items parked on an unbounded
external wait*. Evidence from the v172/v173 session:

* **Six decisions reached a verdict through the file rather than through a conversation** (`OD-ROC-001`..`006`).
  Four of them the owner answered **in a single message**, which is the mechanism's whole claim: a decision written
  down with options and a recommended default is answerable in one pass, where a question in a transcript needs the
  owner present at the moment it is asked.
* **`UC-ROC-083` unblocked after 29.1 days** — the oldest live instance of `blocked`, this project's top contributor
  to gross lead time.
* **`DEF-ROC-035` closed after 12.2 days** in a park it *could never have left*: its liveness predicate read a
  deployed app setting nobody was going to provision. §F9e is what converted an unbounded wait into a decision.
* **`OD-ROC-006` cost nothing at all** — raised with a default and a date, and resolved upstream inside the window
  before anyone spent effort on it. An escalation that costs nothing when it turns out not to matter is the cheap
  half of the hypothesis working.

**Two honest qualifications, recorded so the adoption is not read as stronger than it is.**
(1) The owner was PRESENT and answered directly; the harder claim — that an unanswered decision reaches its
decide-by date and the default fires cleanly — was exercised only by `OD-ROC-006`, and that one resolved itself
upstream rather than by default. The silent-default path remains lightly evidenced.
(2) A near-miss the same session: a defer dated **7 days out was refused by `loop-gate` as deciding nothing**,
because it sat inside the window the gate already grants for free — the reflexive near-dated defer v157 named,
produced by the orchestrator within an hour of its own retro citing that pattern. §F9e's teeth are what caught it.

**INTEGRATION:** §F9e is already written as plain process practice and `open-decisions.md` is live in the project;
nothing needs rewriting into an agent file. The scaffolding removed here is the registry row only.

Original row, preserved verbatim:

| EXP-ROC-013 | ROC | **Problem:** the loop STOPS at every owner judgement. `blocked` is **27.28%** of gross lead time at a median **8.8 days** (n=15) and `orchestrator`/`reported` is **33.78%** at a median 37h (n=105) — the top two GLT owners, and both are the same event: the loop reaches a decision it does not own and halts. Founding case `DEF-ROC-035`, escalated 2026-08-25 with three clean options and **unanswered four days later**, item parked 11.9d. Root cause: the process had no representation for a decision that is OWED BUT NOT YET MADE — a question has only asked/answered and no default, so the only legal behaviour while unanswered was to wait. Owner's words: *"you can hardly work for an hour without my help."* | **Solution:** §F9e — never ask a blocking question. Block the item with a checkable predicate, record the options plus a RECOMMENDED DEFAULT and its reasoning, publish to `work/<project>/open-decisions.md` with a DECIDE-BY date, and keep working. **When the date passes unanswered, the default is TAKEN.** The default must be the REVERSIBLE option, so its cost if wrong is bounded by the work done before it is overturned. | **Target DORA metric: lead time** — specifically the `blocked` share of GLT and the count of items parked on an unbounded external wait. | **Measurement (scored at each of the next 3 retros):** baseline `blocked` = **27.28%** of GLT, median **8.8d/item**, n=15; items on an unbounded external park = **6** (DEF-ROC-009 31.8d, DEF-ROC-035 11.9d, DEF-ROC-068 7.8d, DEF-ROC-073/074 7.8/8.8d, UC-ROC-022 43.0d, UC-ROC-083 28.8d). POSITIVE if the `blocked` median falls AND no cycle ends idle for want of an answer. **NEGATIVE — kill, do not re-tune — if any of:** (a) decisions are published but a default is NOT taken when its date passes, i.e. the surface became a nicer waiting room — audit: every `open-decisions.md` row whose decide-by is in the past must show a verdict; (b) a taken default causes rework costing more than the wait it saved; (c) the `blocked` median does not fall while park volume holds. | active (0/3) | v159, ROC 2026-08-29 |

---

## EXP-ROC-015 — KILLED (v175, ROC 2026-08-29): it caused a data-loss race

**Outcome: KILLED, and not for the usual reason.** This row is not being retired for failing to move a metric — it is
retired because the change it scored **created a defect that silently destroys the single source of truth**.

`DEF-ROC-162`: `wi-project` loads a snapshot of all 373 item files and writes them all back, with no lock, no re-read
and no compare-and-swap. Any `wi-append` landing during the run is **silently and permanently destroyed** — both
commands exit 0, neither warns, and `wi-validate` reports the store CLEAN afterwards, because `derived == fold(events)`
holds just as well over a log with an event missing from it. A real `confirmed` event was lost this way and was caught
only by the `DEF-ROC-154` re-read-from-HEAD habit.

**That race needs two concurrent writers, and §F13 supplied the second.** Under the prohibition it replaced,
`wi-append` and `wi-project` were serialised inside the orchestrator and the window did not exist.

**The ledger, stated plainly:** v163 named the orchestrator's serialisation of state events as the constraint. v164 —
the owner, within the hour — falsified that with one number: **agent work-effort is 0.2% of gross lead time**, so a
serialisation inside 0.2% cannot explain a 99.8% wait figure. This row was scoped down to "a real but SECONDARY
inefficiency" on the spot and **never scored a single positive**. So it bought at most ~0.2%, and it cost silent
permanent loss of the SSOT.

**The lesson is about the retro that opened it, not about the mechanism.** A rule that looks like pure overhead may be
holding a safety property that nothing states — the prohibition existed against a MEASURED hazard (an engineer editing
`work-items.py` froze every state change in the project for hours) and its protective effect on the append/project race
was never written down, so removing it looked free. **Before retiring a constraint, ask what it is preventing that
nobody has written down** — and prefer to leave it in place until that question has an answer.

**NOT reverted with it:** the v11 rights model (`OI-ROC-006`) is independent and stays — it governs whose judgement an
event records, not who types the command, and it correctly refused the orchestrator three times in one session.
§F13a (turn-ending) is likewise untouched.

Original row, preserved verbatim:

| EXP-ROC-015 | ROC | **Problem:** raising `wip` occupancy from **1 of 8 to 8 of 8** — an eight-fold rise in concurrency — produced only **1.43x** the completions (83 -> 119 trailing-14d) and 1.40x the agent work-effort. Badly sub-linear, and the gap identifies the constraint: `orchestrator`/`reported` is the #1 GLT owner at **33.90%**, median **58,718 s/item**, n=114, for the **seventh** consecutive read. Root cause is SELF-INFLICTED: the orchestrator instructs every dispatched agent *"do NOT run any `wi-*` command"*, so every state event in the project queues behind one actor by explicit instruction — an actor that also writes every dispatch brief, commit message, correction and report. | **Solution:** §F13 — a dispatched specialist fires the state events for the transitions it OWNS; the orchestrator fires only what it legitimately owns (`pulled`, `triaged`, `blocked`/`unblocked`, `made_ready`, the CI-confirmed pipeline `deployed`). The blanket prohibition is RETIRED and may be issued only against a NAMED, LIVE resource-class conflict (concretely: another dispatch mid-edit on `work-items.py`), which the brief must state. Now legitimate because `OI-ROC-006` landed — 101 per-transition allowlists removed, firing rights derived from the item's declared owner — so a specialist recording its own work is the rights model working, not a spoof. | **Target DORA metric: lead time.** **SCOPE CORRECTED v164 (owner):** this row is NO LONGER claimed as the constraint's fix — agent work-effort is **0.2% of GLT**, so a serialisation inside 0.2% cannot explain a 99.8% wait figure. It is a real but SECONDARY inefficiency; the constraint is turn-ending (`EXP-ROC-016`). Scored on its own narrow terms only: does distributing state events reduce orchestrator-held dwell at all. | **Measurement (scored at each of the next 3 retros):** baseline `reported` median **58,718 s**, `orchestrator` share **33.90%**, occupancy-to-completion ratio **8.0x concurrency : 1.43x throughput**. POSITIVE if completions rise super-linearly relative to occupancy AND the `reported` median falls. **NEGATIVE — kill, do not re-tune — if any of:** (a) `dev-validating` failure rate RISES from **9.2%** (already the highest stage in the system), meaning self-recorded state is claimed without the evidence an orchestrator would have demanded; (b) `make wi-validate` starts failing, i.e. distributed writes corrupt the log; (c) attribution quality falls — a rise in events whose `AGENT=` is contradicted by their own note. **Guarded:** `AGENT=` is still never spoofed, and `TOKENS=`/`DURATION_MS=` remain the orchestrator's to attach on events it owns (`OI-ROC-008`'s residue is unchanged — a specialist firing its own event carries no cost figure, and must NOT invent one). | active (0/3) | v163, ROC 2026-08-29 |

