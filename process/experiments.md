# Experiment registry (process v32 §25a; pruning v45)

Every routed change is an experiment. One row per change. The orchestrator
scores rows at every retro (§26). Statuses: `active` | `validated` |
`validated-by-null-hypothesis` | `under-question` | `retirement-trial`.

**This file holds ONLY LIVE experiments.** When an experiment reaches a terminal
state — `integrated` (behaviour folded into its agent file as plain practice),
`retired`, or `reworked→EXP-NNN` — its row is **PRUNED from here** and replaced
by one terse line in `process/experiments-archive.md` (the index of what we've
learned and folded in). The full row stays recoverable in git. The registry must
stay small: each retro prunes the rows that reached terminal state (v45 §25a).

**Horizon** counts SCORING OPPORTUNITIES (slices/iterations where the change
could have shown its effect), not calendar retros. "No opportunity" extends.
**Retirement-trials run 4–5 scoring opportunities** before "retired" may be
concluded; an attributable metric drop reinstates early.
**Validated rows trigger INTEGRATION (v34):** the owning agent file is rewritten
so the behaviour becomes core operating practice (scaffolding removed, mechanism
intact); then the row is pruned to the archive (v45). EXP-011 scores the
integration+pruning policy itself.

**Applies-to (v38):** each experiment names the KIND of work that exercises it
(in its row's scoring notes as `applies-to: ...`). An experiment is a scoring
opportunity ONLY for matching work. At work selection the orchestrator lists the
applicable active experiments (§10/§25a).

> Archive (terminal-state, pruned): see `experiments-archive.md` — EXP-001, 002,
> 003, 004, 005, 006, 007, 009, 010, 012, 014, 015, 017, 020, 030, 032, 034.

| id | routed | artifact(s) | change | target metric | anticipated effect | horizon | status | scoring notes |
|----|--------|-------------|--------|---------------|--------------------|---------|--------|---------------|
| EXP-008 | v32 (2026-06-07) | §25a + §26 + this file + retro command | changes-as-experiments lifecycle (scoring, retirement, null-hypothesis) | ALL (meta) — guards simplicity of agent defs | every routed change scored within horizon; ≥1 under-question item resolved within 3 retros; agent-def line count stops monotonic growth | 3 retros | active | self-referential: scored by whether the registry is actually maintained and acted on. v45: pruning added — registry now holds live-only. v48 retro (2026-06-10): maintained — 7 validated rows integrated+pruned, all live rows scored or carried with reason |
| EXP-011 | v34 (2026-06-07) | §25a + retro command + agent files at each integration | validated-experiment INTEGRATION + **PRUNING (v45)**: rewrite owning agent defs so validated behaviour is core practice (no vNN/EXP scaffolding), THEN remove the row from experiments.md to a one-line archive entry | per-agent median task time, mechanism compliance, agent-def + registry simplicity (line/row count) | integrated agents perform ≥ as well with shorter defs; registry stops accreting (live-only) | 3 integrations | active | Pass 1 (7db8d99) integrated EXP-001/002/004/005/006/007. **v45 retro (2026-06-10, human-directed):** registry was accreting — 10 terminal rows still present; added the prune-to-archive rule and pruned EXP-001/002/003/004/005/006/007/009/010/032 (66→~56 lines). **Pass 2 (v48 retro, 2026-06-10):** integrated+pruned EXP-012/014/015/017/020/030/034 — behaviours already plain practice in process §§9a/12a.5/12b/6/F2/F3/F9 + agent defs; bracket-citations stripped; registry 30→23 rows. 2/3 integrations scored — mechanisms (gate-4 timing, pull loop, proactive replenish) all fired post-integration |
| EXP-013 | IMP-007 (2026-06-08) | .claude/tools/impacted-tests.js + Makefile + tester.md | make impacted-tests SINCE=<sha>: model-diff → impacted-specs + uncovered-changed-nodes | tester median task time (<900s); CFR (uncovered changed node surfaced) | tester planning is a tool run not hand-assembly | 2 (C5 slices) | active (1/2, partial) | s009: planning 12min vs 18 hand-assembly (-33%); OI-42 inflation cap now CLOSED. Full payoff at next C5 slice |
| EXP-016 | v38 (2026-06-08) | ui-designer.md §3a + tester.md | visual-structural correctness: assert layout GEOMETRY + audit inherited surfaces | CFR (visual/layout defects reaching prod) | shape-carrying surfaces fail a test when geometry is wrong | 2 | **validated** (2/2+) — integrate+prune next retro | board geometry (S002) + s009 table + s014 + **s002/s004 observatory GEO guards (left→right flow, 3-lane, no stacked-column)** all assert geometry. Strong, repeated. **DEFECT-006 sharpening (2026-06-10):** an added/overlay surface (the detail drawer) must leave the UNDERLYING view's geometry UNCHANGED — assert the underlying bbox + page scrollHeight are identical with the new surface open vs closed (a "no-reflow" invariant), not just that the new surface is positioned. The in-flow pane passed "pane left ≥ rail right" yet reflowed the page +690px. |
| EXP-018 | v38 (2026-06-08) | §25a applies-to + §10 | experiment-applicability: each experiment declares applies-to; work-selection lists applicable up front | meta — scoring honesty | experiments scored only on work that exercises them | 3 selections | active (1/3) | s009 selection listed applicable experiments; scoring used those predicates. 2 more |
| EXP-019 | IMP-009 (2026-06-08) | playwright.config (workers:4) + Makefile validate-impacted | attack tester EXECUTION: parallel validation + run impacted∪core not full suite | tester median task time (<900s) + lead time | per-slice smoke execution drops | 2 slices | active (1/2, partial) | s015 was a no-code slice (not a clean test). Structural cut in place; a code-bearing slice needed. applies-to: any slice validation |
| EXP-021 | v40 (2026-06-08) | process §F1 + items/items.csv + dora.py | work-item model: REQ/CHK/SLC/UC/DEF with canonical parent + rebuilt child index; per-item DORA from ledger | measurement granularity | every item's lead/wait/service computable; tree traverses both ways | 2 slices | active (1/2) | observatory: REQ-OBSERVATORY + CHK-1..7 + UCs registered clean, no drift. 1 more (items reaching done) |
| EXP-022 | v40 (2026-06-08) | process §F2 + queues/policy.csv + flow-manager.md + dora.py | UNIFORM queue model: every queue min_items + wip_limit, retro-tuned; same four metrics per queue | gross lead time (dwell) + throughput | dwell falls without starvation; retro tunes each queue | 2 buffer-tunes | active (0/2) | floor-breach detection works (fired on observatory intake). No buffer TUNE yet — carry |
| EXP-023 | v40 (2026-06-08) | process §F5 + §9 + intake.md | two-gate model: keep intake + infra-deploy; remove vision/slice/arch gates, each → a named assurance | gross lead time (gate wait) guarded by CFR | gate wait drops; no quality regression | 3 slices | active | applies-to: every slice. Gate removals are null-hypothesis experiments — watch CFR |
| EXP-024 | v40 (2026-06-08) | flow-manager.md + orchestrator.md delta | dedicated flow-manager owns queues/costing/buffers/dispatch, separate from orchestrator | orchestrator context size + flow-decision quality | orchestrator stays small; flow decisions owned/consistent | 2 slices | active (1/2) | observatory: flow-manager owned registration/costing/floor-signal cleanly + returned tight. 1 more |
| EXP-025 | v40 (2026-06-08) | process §F5 + intake.md + /defect | defect-as-high-value-intake: defects re-enter via intake, JTBD-framed/costed, pre-empt head of Ready | MTTR + value-first integrity | defects interrupt visibly-costed, not silently | 2 defects | active (1/2) | DEFECT-001 (2026-06-10): confirmed → prioritised fix-NOW (pre-empt) → fixed → prod re-check → gap experiment. 1 more |
| EXP-026 | v40 (2026-06-08) | process §F6 + flow-manager.md + loop-run.md | parallel dispatch by maximal independent set (DAG + claimed-path registry), §40 flags | build wall-clock + gross lead time | concurrency fills to N where independence exists | 2 slices ≥2 indep UCs | **validated** (2/2) — integrate+prune next retro | s001: UC2/3/4 as 3 concurrent engineers — parallelism eff 1.00, 0 collisions. s013/s014/s015 wave (2026-06-11/12): 3 concurrent engineers on disjoint seams, par_eff 0.86–0.89, both shared-file events resolved by commit-order/mine-only-blob isolation with zero rework — the isolation pattern (stage only your hunks via constructed index blobs on co-owned files) should fold into engineer.md at integration |
| EXP-027 | v40 (2026-06-08) | process §F7 + flow-manager.md + edge-ledger.md | collision detection → correct dependency tree; hidden-edge→0 + false-edge null-hypothesis trials | CFR (hidden edges) + GLT (false edges) | collisions caught mechanically + fed back | 3 parallel slices | active (2/3, strong) | s001: scaffold + server.js seams caught PRE-build by §39 scheduling → 0 collisions/0 rework. s013/s015 wave: genuine hidden edge S13UC2→S15UC2 (ObservatoryView composition root) caught at integration, serialised by commit order (~5min, 0 rework), edge added to use-case-deps + edge-ledger — the mechanism fired end-to-end; refactor candidate noted (tab-wiring registry pattern would dissolve the edge). 1 more |
| EXP-028 | v40 (2026-06-08) | process §F4 + dora.py flow + dora/flow.md | time-thief attribution report as primary retro input | meta — makes the largest GLT contributor visible | retro attacks the ranked constraint, not the slowest agent | 2 retros | active (1/2) | applies-to: all. Score: does the retro's chosen change target the top thief? v48 retro: yes — constraint=orchestrator (median 900s, bookkeeping-heavy); changes target it directly (orchestrator→fable for decision quality at boundaries; EXP-038 scripts continue for the mechanical share) |
| EXP-029 | v40 (2026-06-08) | process §F8 + retro command | retro cadence: slice-completion + event-triggered, tunable | meta — bound retro overhead without losing signal | retro fires often enough to learn, rarely enough not to dominate | 3 slices | active | observatory ran many event-triggered retros (human-raised); cadence holding. Watch overhead. v48: event-triggered retro on a model release (Fable 5) — correct trigger class, scoped tight |
| EXP-031 | v42 (2026-06-09) | process §F9.4 + orchestrator.md | keep-trucking: slice/retro/chunk boundaries are autonomous; orchestrator never ends a turn with a continue-vs-pause question | gross lead time (boundary idle) | 0 boundary stop-and-ask turns; loop advances unprompted | 2 slices | active (1/2) | s002: trucked through 6 UCs + chunk advance + tight retro, 0 stop-asks. **REWORKED v46 (2026-06-10):** FAILED across CHK-4/defects — stopped *asking* but kept *ending the turn* with a report + "resume/refresh to confirm", parking the loop; human re-prompted ~5×, ~7 closures each ended the turn = project's biggest GLT thief. New rule: ending the turn at a non-gate boundary IS the stop → dispatch next work IN THE SAME TURN; reports inline; end only at gate/requirement-complete/human-blocking. Re-scored from here. **s014 delivery run (2026-06-10→12): validated (2/2)** — loop trucked through 2 defect pre-emptions, 3 session-limit cuts (resumed without being asked beyond a wake), slice/verdict boundaries all advanced in-turn; background-notification wake pattern worked. Integrate+prune next retro |
| EXP-033 | v43 (2026-06-09) | tester.md + slice done-condition (§F5) | validate UI/observability slices against LIVE repo state, not just fixtures; the DEFAULT mounted view must show real, non-zero, correct data (≥1 figure hand-cross-checked) | CFR (green tests masking a useless real view) | no slice ships green while its real-data default view is empty/wrong | 2 UI slices | active (1/2, sharpened) | DEFECT-001 (2026-06-10) confirming data point: deployed default showed 0 while /stage-flow had real data → sharpened to the DEFAULT view + caught/fixed via the live prod re-check. **DEFECT-002 (2026-06-10):** non-zero isn't enough — WIP was non-zero but STUCK/wrong; the real-data check must hand-verify a 'now' figure against GROUND TRUTH (does WIP match actually-in-flight items?), not just non-emptiness. **DEFECT-005 sharpening (2026-06-10):** the meta-gap across DEFECT-001/003/004/005 is acceptance testing STRUCTURE/PRESENCE (component renders, has a `data-source` attr, non-zero) while the surface is unusable to a HUMAN. Validate operator-facing surfaces for **human-meaningfulness** — readable source refs (not `row:N`), units on figures, coherent numbers — not just presence. Routed to ui-designer.md + tester done-condition. **DEFECT-007/008 (2026-06-10):** 4th/5th data points (count mislabelled "throughput"; id with no "why") — now a CONCRETE mechanism: the **figure legibility checklist** in ui-designer.md (unit present + unit matches metric name + references human-meaningful + empty≠zero), emitted as acceptance conditions per surface. Strong pattern (DEFECT-004/005/007/008) — checklist should pre-empt the next |
| EXP-037 | DEFECT-004 (2026-06-10) | orchestrator.md + flow-manager.md (§F1) | keep registry + queues CURRENT with the ledger: on every UC done, do the ledger row AND the flow-manager item-state transition AND the dequeue together — never record the ledger alone and let items.csv/queues drift | CFR + data-integrity (UI views contradicting each other) | the three views (ledger/items.csv/queues) never disagree about the same item; no stale "ready" on done work | 2 slices | active | applies-to: any pull-mode slice with item-state. Founding data point: DEFECT-004 (s005 UCs built but left `ready` → tree/map/queues incoherent). s004 bubbled correctly; s005 drifted because state wasn't transitioned per-UC. Pairs with EXP-035 (render reconciles against the authoritative registry) |
| EXP-036 | DEFECT-003 (2026-06-10) | ui-designer.md + engineer.md + orchestrator.md | observability/live surfaces must SIGNAL staleness/disconnection (non-colour-redundant) and re-fetch on reconnect — never present stale data as live; orchestrator keeps a persistent server for the operator + verifies on ephemeral ports (never pkills the shared one) | CFR + data-trust (operator misled by stale-as-live) | a live view never shows old data without a clear stale/disconnected cue; orchestrator never kills the operator's running view | 2 live-view slices | active | applies-to: any live/monitoring surface + any time the project is a long-running app the operator watches. Founding data point: DEFECT-003 (map froze on 4/1 while backend was down — orchestrator had killed :3001; no stale signal). Fixed: stale banner + reconnect re-fetch |
| EXP-035 | DEFECT-002 (2026-06-10) | engineer.md | derived CURRENT-STATE metrics (WIP/in-flight/live counts) reconcile against the authoritative registry — an open event counts only if its entity exists + is non-terminal; raw event pairing alone is not trusted | CFR (data-integrity defects on derived metrics) | no "now" figure shows phantom/stuck state from orphan/abandoned events; pinned by a test | 2 metrics | active | applies-to: any metric/aggregation deriving 'now' state from an event log. 2nd instance of "derived metric trusts the raw log" (1st: strict-CSV line drop → tolerant parser; this: phantom WIP → reconcile vs items.csv). Founding data point: DEFECT-002 (engineer wip stuck at 3 on dropped UC-S003-* → fixed, live wip=0). **SHARPENED DEFECT-009 (2026-06-10):** reconciliation that's too strict HIDES genuine activity — gating WIP on items.csv-membership suppressed product's in-flight work (chunks/slices/meta). Rule: for a 'now' metric the PRIMARY signal is recency/activity; reconcile against the registry as a SECONDARY exclusion (terminal=done), never the gate. The WIP model produced BOTH failure modes (DEFECT-002 too-much, DEFECT-009 too-little) before landing on recency≤30min + secondary-terminal. **DEFECT-010 (2026-06-10) — 3rd turn:** the secondary-terminal check ITSELF hid recent active work on delivered (terminal) items (defect-fixes/rework). Final rule = **RECENCY-ONLY** (open ≤30min, no close; no registry/terminal check in the WIP path — registry kept only for queue-depth coherence). **Craft lesson (→ product.md): the simplest predicate that satisfies the cases wins; each added "safety" exclusion on a metric is a new failure surface — 3 defects from one predicate proved it.** sha 6b21b6f. **DEFECT-011 (2026-06-10) — 4th turn, gap-closing:** the predicate held (recency-only confirmed right) but the CONSTANT was wrong — 30min horizon assumed "tasks finish in single-digit minutes", falsified the day Fable-era agents ran 29–32min tasks; product's genuinely-running decompose vanished from WIP while the human watched both tracks run. Fix: horizon→2h (≥4× observed max, ≪ phantom ages), sha e8f1d8e. Sharpened rule: **duration/threshold constants in metrics must be derived from observed ledger distributions (and re-checked when the model tier changes, EXP-039 interplay), never from beliefs about task length** |
| EXP-038 | v47 (2026-06-10) | process §26 + dora.py (log-decision) + retro command | retro hunts the cycle's most-repeated MECHANICAL operations and builds/extends a committed script for them, so hand-bookkeeping leaves the context window (one allowlisted command, not N Read+Edit cycles) | orchestrator context/overhead (the standing constraint) + gross lead time | the dominant by-hand op each cycle becomes a script; orchestrator per-turn bookkeeping cost falls; constraint moves off "orchestrator" | 2 retros | active | applies-to: every retro. Human-directed (2026-06-10): 272 ledger + 21 decision-log + 9 defect records by hand in observatory, constraint=orchestrator. First script: `dora.py log-decision` (decision-log append; dogfooded — RETRO-V47 row written by it). Score: did the next 2 retros each scriptify a real repeated op + did orchestrator overhead drop? v48 retro (1/2, partial): no new repeated mechanical op surfaced this cycle (model-assessment retro, no slice work between v47 and v48); log-decision reused for the v48 row. Genuine scoring opportunity = next slice-cadence retro |
| EXP-040 | DEFECT-012 (2026-06-11) | product.md + flow-manager.md + queues/staging.csv + engineer practice | handoffs are visible buffers + agents self-record their own open/close ledger rows at actual start/finish (orchestrator records only flow events it owns): product appends decomposed items to staging.csv before task_end; flow-manager drains at every sweep; board renders the staging queue | CFR (data-trust: invisible/phantom work on the board) + WIP/queue truthfulness | no decomposed item is ever invisible (staging depth>0 during the handoff window); no orphan task_start / late task_end rows from proxy bookkeeping (DEFECT-012's root cause: orchestrator recorded product's start 4min early once and its end 26min late once, plus 2 orphan starts in one session) | 2 decompose handoffs | active | applies-to: every product decompose/replenish + every agent dispatch. Founding data: DEFECT-012 (fix bc2ff5f). The DEF-012 engineer self-recorded its rows same-session — early compliance signal. **s014 run scoring (1/2): partial pass** — every agent self-recorded across the 3-day run (engineers/testers/ui-designer/flow-manager, including through session-limit cuts where rows survived the dead reporter); ONE miss class: a tester keyed its rows by slice slug → DEFECT-013/EXP-041 sharpened the rule to work-item ids. No decompose handoff occurred yet (staging path unexercised — carry) |
| EXP-041 | DEFECT-013 (2026-06-12) | engineer.md + tester.md + flow-manager.md + ledgerAggregator coherence | ATOMIC PULL: whoever pulls performs queue-row removal + items.csv→in-flight + ledger rows keyed by the WORK-ITEM id (never a slice slug) in one act; flow-manager sweeps reconcile, never originate; stage-flow emits a coherence_warning when a recent open in-event belongs to an item that is planned/absent in the registry | CFR (data-integrity: registry lying about in-flight work) | 0 planned-while-in-flight windows; any future drift self-surfaces as a board coherence warning instead of a human report | 2 pulls | active (1/2, strong) | applies-to: every pull + every self-recorded ledger row. Founding data: DEFECT-013 (3 items planned while building/validating; 1 slug-keyed WIP row) — 4th defect in the views-disagree family (004/012/013 + the EXP-037 founding case); the family pattern is "a multi-step bookkeeping act with a non-atomic owner". **Proved on day one:** UC-S015-4's pull was atomic+item-keyed (0 drift), and the coherence warning caught a REAL recurrence live (UC-S018-1, via the unrouted ui-designer def — 10min after manual repair) → ui-designer.md routed. The detector works; the ritual now covers all stage-entering agents. **DEFECT-015 sharpening (2026-06-12): the atomic-act rule applies to REPAIRS too** — the orchestrator's own half-repair (state flipped, queue row left) created a transient ready incoherence that the DEFECT-004 detector caught and the next atomic pull healed; flow-manager.md reconcile clause now binds repairs. Detector true-positive count: 2/2. 1 more pull |
| EXP-039 | v48 (2026-06-10) | engineer.md + orchestrator.md + documenter.md frontmatter + process §15a | model tiering on Fable 5 release: engineer opus→**fable** (long-horizon TDD agentic build is Fable's stated strength; engineer is the highest-volume agent, n=58, and CFR=45% is the standing quality problem), orchestrator opus→**fable** (the ToC constraint, median 900s; EXP-031's turn-ending failure was a judgment failure — buy judgment at the constraint), documenter sonnet→**haiku** (median 60s rewriting work; null-hypothesis downgrade). solution-architect/ui-designer stay opus; product/cicd/tester/flow-manager stay sonnet — no evidence their tier limits them | CFR (engineer), gross lead time + boundary-idle (orchestrator), cost (documenter, null) | CFR falls from 45% toward ≤30% over next 2 slices; 0 turn-ending-at-boundary violations; documenter quality unchanged at 1/5 cost. Token cost 2× on engineer is the bet — revert any agent whose metric doesn't move | 2 slices | active | applies-to: every slice. Score per-agent: engineer by CFR + rework count, orchestrator by boundary-idle + per-turn overhead, documenter by doc-defect count (expect 0 change). If CFR doesn't improve, engineer reverts to opus FIRST (biggest cost delta). **Scoring 1/2 (s014 slice, 2026-06-12): directional PASS.** Slice-window CFR ~14% (1 tester fail — a11y focus race — across ~7 deploys) vs 45% baseline; fable engineers also FOUND+FIXED 3 latent trunk defects unprompted (NaN-now time bomb, time-of-day-dependent pin, real-data spec gate) and invented the mine-only-blob commit isolation; orchestrator boundary-idle: 0 stop-asks across a 3-day continuous run. Cost: engineer runs of 4+h observed (task durations grew — the DEFECT-011 horizon lesson). documenter-on-haiku: docs mostly accurate at 1/5 cost, but the 2nd pass documented an IN-BUILD feature (UC-S013-3 drill) as shipped despite an explicit only-if-landed instruction — caught at orchestrator review, corrected. 1 verification miss in 2 runs: watch; if it repeats, documenter→sonnet is the revert. **Scoring 2/2 (s015 delivered 2026-06-12): VALIDATED for engineer+orchestrator tiers** — s015 slice-window tester-fail rate 0% (4/4 UCs first-pass), s014 ~14%, vs 45% pre-Fable baseline; CFR target beaten decisively. Orchestrator boundary-idle 0 across the run. Engineer→fable and orchestrator→fable are KEPT (integrate as plain assignment next retro). documenter-on-haiku stays under watch (1 miss) — not validated, carry as its own thread. Cost accepted: quality gain >> 2× token rate |
