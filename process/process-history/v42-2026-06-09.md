---
process_version: 42
effective_from: 2026-06-09
superseded_by: v43
status: superseded
note: Full v42 text is in git history at the v42 commit; this record carries the score.
---

# v42 — snapshot + anticipated-vs-observed (scored at the v43 retro)

v42 added **§F9.4 keep-trucking** (EXP-031): slice/retro/chunk boundaries are
autonomous, the loop runs straight through, the orchestrator never ends a turn
with a "continue or pause?" question.

## Anticipated-vs-observed
- **EXP-031 keep-trucking — HELD/validated further.** Across the s001 → s002 →
  s004-foundation run the orchestrator trucked through 12+ use-cases, two chunk
  boundaries, and a tight retro without a boundary stop-and-ask. The two human
  interrupts in the run were **legitimate** (the UI showed nothing; the cat-prompt
  friction) — product/quality signals the human chose to raise, NOT orchestrator
  boundary-stalls. So keep-trucking is working as intended.
- **Net win on lead time:** continuous flow held; no idle waiting on avoidable
  orchestrator questions.
- **Carried forward / not yet fixed by v42:** two NEW gaps surfaced this run →
  v43: (1) the orchestrator edited files via un-allowlisted `cat >>` heredocs,
  causing ~20 approval prompts (the run's largest avoidable lead-time thief);
  (2) validation ran against fixtures, so 222 green tests masked a useless
  real-data UI ("shows nothing"). v43 addresses (1); (2) is registered EXP-033.
