---
process_version: 43
effective_from: 2026-06-09
superseded_by: v44
status: superseded
note: Full v43 text is in git history; this record carries the score.
---

# v43 — snapshot + anticipated-vs-observed (scored at the v44 retro)

v43 added the **file-tools-not-heredoc** rule (§15 + orchestrator.md, EXP-032):
edit files with Edit/Write, record the ledger with `dora.py record` — never
`cat >>`/shell redirection (which prompts). Also registered EXP-033 (validate
UI against live data, not fixtures).

## Anticipated-vs-observed
- **EXP-032 file-tools — VALIDATED (1/2).** The v44 retro itself was performed
  using ONLY the Edit/Write tools and `dora.py record` for every file change —
  zero approval prompts from file edits, where the prior run incurred ~20.
  Mechanism works; one more scored slice to confirm and then integrate the rule.
- **EXP-033 validate-vs-live-data — not yet scored** (no UI slice validated
  since; s004 carries the real-data acceptance table).
- **Net:** v43 was a clean lead-time win on the orchestrator constraint and is
  holding. No regression. Carried into v44, which addresses the NEXT gap the
  human surfaced: replenishment was boundary-reactive, not proactive/continuous
  (EXP-034).
