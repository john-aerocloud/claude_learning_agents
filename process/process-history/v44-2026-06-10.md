---
process_version: 44
effective_from: 2026-06-09
superseded_by: v45
status: superseded
note: Full v44 text is in git; this record carries the score.
---

# v44 — snapshot + anticipated-vs-observed (scored at the v45 retro)

v44 added **proactive continuous replenishment** (§F3, EXP-034): product works
ahead of the engineer (dispatched in the same parallel batch as each build wave),
keeps Ready ≥ floor, decomposes the next chunk while the current builds;
below-floor = refill-now, never tolerated.

## Anticipated-vs-observed
- **EXP-034 — partial (1/2).** The behaviour change fired: at the s004 build the
  orchestrator dispatched product to decompose CHK-4 AHEAD in the same parallel
  batch as the UC-S004-2 render build (exactly the new rule). BUT both long
  agents hit infrastructure timeouts (socket closed / request timed out), so the
  decompose-ahead produced only `s005/slice.md` before dying. Mechanism
  dispatched correctly; one clean slice still needed to confirm the overlap +
  Ready-stays-stocked outcome. Carry.
- **EXP-032 — validated 2/2 → integrated this retro.** The v44 and v45 retros ran
  entirely on Edit/Write + `dora.py record` with 0 file-edit prompts. Folded into
  §15 + orchestrator.md as plain practice; row pruned to the archive.
- **Net:** no regression; the proactive-replenishment intent is right but
  under-tested due to infra flakiness, not logic. v45 keeps it active and adds
  the registry-pruning rule the human flagged.
