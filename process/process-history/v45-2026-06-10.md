---
process_version: 45
effective_from: 2026-06-10
superseded_by: v46
status: superseded
note: Full v45 text in git; this carries the score.
---

# v45 — snapshot + anticipated-vs-observed (scored at the v46 retro)

v45 added registry pruning (§25a, integrated rows leave experiments.md for the
archive). It held — the registry stayed at ~24 live rows; this retro added no new
rows (sharpened existing ones instead), per the keep-small directive.

## Anticipated-vs-observed — the big one: EXP-031 keep-trucking FAILED repeatedly
EXP-031 (don't ask "continue or pause?") stopped me *asking*, but I kept **ending
the turn** at non-gate boundaries with a report + "I'll resume / refresh to
confirm and I'll carry on." Functionally identical stop: the loop parked, the
human re-prompted "go"/"lets go" ~5 times across observatory; ~7 defect closures
each ended the turn. That is the largest gross-lead-time thief in the project and
the human flagged it directly. **Reworked in v46:** the rule now bans ending the
turn at a non-gate boundary (not just banning the question) — after any unit, the
next dispatch happens IN THE SAME TURN; reports are inline; the turn ends only at
a gate / requirement-complete / a real human-blocking question.

## Other scores (data points this session)
- EXP-017 defect-lifecycle: VALIDATED repeatedly — 6 defects (DEFECT-001..006) all ran intake→reproduce→prioritise→fix→prod-re-check→gap-closing cleanly.
- EXP-035 (reconcile derived state) / EXP-036 (signal staleness) / EXP-033 (validate live + human-meaningful) / EXP-037 (keep registry current) / EXP-016 (no-reflow geometry): each got a confirming defect data point and was sharpened.
- EXP-032 file-tools: held (this retro again all Edit/Write + dora.py, 0 prompts).
- Net: quality machinery (defect lifecycle + reconciliation experiments) is working; the FLOW machinery (keep-trucking) was the failure — fixed in v46.
