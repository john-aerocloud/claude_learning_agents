# Process v67 — snapshot (taken late at the v68 retro, 2026-06-26)

> NOTE: this snapshot was taken at the v68 retro, NOT at v67's own slice
> completion — because the v67→v68 retro was OVERDUE (the meta-failure v68 fixes:
> the automatic §F8 retro was offered to the human instead of firing, so no v67
> snapshot was made at the time). The retro-debt gate (EXP-083) now prevents this.

## v67 change-set (effective 2026-06-25, target: gross lead time guarded by CFR)
- **EXP-082 — deploy preflight + verified-target binding** (cicd.md §20a): the
  deploy target runs a fail-fast preflight (clear stale SST state lock, assert
  Docker daemon up, credential valid, CI/deploy binding resolves to the verified
  `origin`/account) before any irreversible step; `ci-watch` passes the resolved
  `origin` repo explicitly. Founded on the SLC-023 §F5 deploy failing twice
  (stale lock + Docker-down) and ci-watch reading the wrong repo.
- EXP-080 (verify-at-source) INTEGRATED + pruned (2/2).
- Buffers/N unchanged (min_items=2 / wip_limit=4 / N=4).

## Anticipated vs observed (v67 changes, scored at v68)
- **EXP-082 (deploy preflight):** anticipated 0 deploys failing on a stale-lock /
  daemon-down / wrong-target-binding that a preflight would catch, over 2 deploys.
  **Observed (1 data point so far):** the SLC-025 / DEFECT-031 redeploys this
  window went through `sst deploy` without a repeat of the stale-lock / Docker /
  wrong-repo failures of SLC-023 — consistent with the preflight working, but the
  window's deploy failures were a DIFFERENT class (false-green: stale committed
  bundles + unpackaged asset), which EXP-082 does not target. EXP-082 stays active
  (1/2-ish, no contrary evidence); the false-green class is handled by the v68
  graduated delivery-principles rule, not EXP-082.

## DORA at v67 close (cumulative)
lead=2543s, freq=5/active-day, cfr=18% (18/93 deploys), mttr=2189s, defect-intake
2/active-day. Constraint: engineer.

## What v67 did NOT cover (→ v68)
The false-green family (DEFECT-OAG-028/030/031 + the CI-green CC3 close) — "green
build/CI ≠ running service." v68 banks this in the delivery-principles skill and,
more importantly, fixes the meta-failure (no retro fired) with the mechanical
retro-debt gate, and defines the graduate-to-skill lifecycle to keep /process lean.
