# v68 snapshot (2026-06-26) — superseded by v69

Full v68 text is retained inline at the top of `process-current.md` (the v68
header block). This snapshot records the anticipated-vs-observed score for the
v68 change-set, taken at the v69 retro.

## v68 change-set — anticipated vs observed

- **EXP-083 (MECHANICAL retro-debt gate).** Anticipated: the retro fires at the
  cadence as a checkable loop property, 0 instances of the loop advancing past a
  due retro or the retro being offered to the human.
  **Observed (1st firing):** the gate FIRED correctly on the SLC-009 close
  (RETRO DUE, exit 2) and the loop did NOT advance — mechanical enforcement
  WORKED as designed. BUT the firing also exposed that threshold=1 is too
  aggressive (a validation-only close should not force a full retro). Net:
  enforcement validated; cadence needed right-sizing → EXP-085 (v69). The gate
  self-correcting on its first real firing IS the intended behaviour. Still
  active (1/2 toward integration); KEEP the mechanism.
- **EXP-084 (graduate-to-skill lifecycle).** Anticipated: active EXP-row count
  does not net-grow over 2 retros; no graduated rule's defect class recurs.
  **Observed at v69:** active rows held steady (v69 added EXP-085 but it extends
  EXP-083 rather than introducing a new standalone subject; no proven-stable row
  was left un-graduated this window). No graduated defect class recurred (no new
  prod defects, no MTTR events). Still active (1/2).
- **False-green family (delivery-principles skill).** No recurrence this window
  (no false-green close; SLC-009 was validated PASS at the running service).

## DORA at v69 (no regression since v68)
cumulative: lead=2543s freq=5/day cfr=18% mttr=2189s; window(last 12 deploys)
cfr=0% lead=1334s. Trivial closes only (SLC-009 validation-only; TD-002 dead
oag-feed.ts delete). No new prod defects, no MTTR pairs, no queue starvation.
Constraint stays the **engineer**. Buffers/N UNCHANGED.
