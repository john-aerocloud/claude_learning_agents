# v69 snapshot (2026-06-26) — superseded by v70

Full v69 text is retained inline at the top of `process-current.md` (the v69
header block). This snapshot records the anticipated-vs-observed score for the
v69 change-set, taken at the v70 retro (the SLC-026/027/028 clean-wave retro).

## v69 change-set — anticipated vs observed

- **EXP-085 (retro cadence right-sized — routine batches to 3, incidents
  immediate).** Anticipated: routine slice/chunk closes batch to ~3 before a
  retro fires (no per-close retro on a clean run); every prod defect resolve /
  deploy failure still forces an immediate retro; no learnable incident missed.
  **Observed (FIRST batched fire, positive):** the three CLEAN routine closes —
  SLC-026 (13:15:00), SLC-027 (13:15:10), SLC-028 (13:15:20) — BATCHED to 3
  before `retro-debt` read RETRO DUE (exit 2, "routine 3/3, incidents 0"). No
  per-slice retro fired on the clean run; one retro covers the batch — exactly
  the right-sized cadence the v69 change targeted. The incident-never-batched
  leg was NOT exercised this window (0 prod defects, 0 deploy failures, DLQ 0) —
  carry that leg for a future incident. Net: the batched-routine cadence is
  behaving as designed. Still active (1/3).

- **EXP-083 (MECHANICAL retro-debt gate — carried from v68).** Anticipated: the
  gate fires at the cadence as a checkable loop property; 0 instances of the
  loop advancing past a due retro or the retro being offered to the human.
  **Observed (positive):** `make retro-debt` fired at routine 3/3 and the loop
  did NOT pull SLC-029 ahead of it; this retro is being run to drain the gate;
  the gate was NOT offered to the human as a choice. Mechanical enforcement held
  end-to-end across a 3-close batch (the v69 batching rides ON TOP of the v68
  mechanism without weakening it). Still active (1/2).

## DORA at v70 (clean wave — NO regression, CFR did NOT rise)
cumulative: lead=2543s freq=5/day cfr=17% mttr=2189s (deploys only; 42
defect-intakes excluded); window(last 12 deploys): cfr=0% lead=166s. The
SLC-026/027/028 wave added 0 defects, 0 MTTR pairs, 0 deploy failures, DLQ 0 —
CFR did NOT rise. Constraint at the cumulative level computes **orchestrator**
(plumbing); at the engineer/build level the backend SERIALIZED on
`src/core/normaliser-core.ts` (par_eff=0.84) exactly as the flow-manager
predicted (the §F6 shared-file seam). Buffers/N UNCHANGED — no starvation, no
over-WIP (backend serial-by-seam + FIDS parallel both ran as scheduled).

## The headline — the v68/v69 anti-false-green machinery VALIDATED
This was a CLEAN backend wave, the direct contrast to the prior serviceType wave
(DEFECT-028/030/031 false-greens) on the SAME surface. The bundles-current gate
(DEFECT-030 fix) PASSED (rebuilt+committed bundles → deployed artifact matches
source); CI green BOTH lanes; consumer rolled to primary (e5587a7); and per the
verify-at-source / validate-running-service doctrine the tester read the deployed
BUILD_SHA at the running consumer and validated the NEW fields on events the NEW
consumer ACTUALLY ingested (recovery/generalAviation observed on a live
FlightCreated; 14 live OagFlightTakenOff with correct offGround shape). NO
false-green, NO defect, DLQ 0. The whole stack — EXP-082 (deploy preflight) +
bundles-current gate + validate-running-service + EXP-080 verify-at-source
(integrated v67) — produced a clean wave where the surface previously
false-greened 3×.
