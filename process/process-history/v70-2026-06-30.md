---
process_version: 70
effective_from: 2026-06-26
superseded_by: v71
snapshot_date: 2026-06-30
status: superseded
---

# v70 snapshot (LIGHT retro — clean-wave scoring, no new rule, 2026-06-26)

Target: **CFR/quality** (the false-green family was the headline) confirmed by a
clean wave, plus the EXP-085 retro-overhead balance (this was its first batched
fire). Per EXP-085 this was a LIGHT retro: the SLC-026/027/028 backend wave
(OagFlightTakenOff / recovery+generalAviation genesis / FlightRemoved+
diversion-enrichment) closed CLEAN — 0 new defects, 0 MTTR pairs, 0 deploy
failures, 0 queue starvation, DLQ 0 — so NO new cross-agent rule was added; the
retro SCORED the active machinery and banked one planned experiment (EXP-086).

**Headline (v70):** the v68/v69 anti-false-green machinery was VALIDATED on the
direct contrast to the prior serviceType wave (DEFECT-028/030/031 false-greens) on
the SAME surface. bundles-current gate PASSED; CI green both lanes; consumer rolled
to primary (e5587a7); tester validated NEW fields on events the NEW consumer
actually ingested. NO false-green, NO defect.

**v70 scores recorded:** EXP-082 1/2 positive; EXP-083 1/2 positive (fired at
routine 3/3, loop did not advance, not offered to human); EXP-085 1/3 positive —
FIRST BATCHED FIRE (3 clean routine closes batched before RETRO DUE; incident leg
unexercised, carried). Banked EXP-086 (normaliser genesis-handler split,
trial-DEFERRED until after SLC-029). DORA: cumulative lead=2543s freq=5/day cfr=17%
mttr=2189s; window(12) cfr=0% lead=166s. Buffers/N UNCHANGED.

---

## Anticipated-vs-observed scoring of the v70 change-set (filled at the v71 retro, 2026-06-30)

v70 added **no new cross-agent rule** — it was a scoring-only LIGHT retro plus the
EXP-086 bank. So the "v70 change-set" to score is (a) the EXP-086 trial-deferral
decision and (b) the LIGHT-retro decision itself.

| v70 item | anticipated | observed at v71 | verdict |
|----------|-------------|-----------------|---------|
| **EXP-086 deferred until after SLC-029** | Defer measures real parallelism gain across the FULL handler set (incl. SLC-029's carrier/codeshare UCs) rather than a synthetic subset; avoids actioning the refactor mid-wave. | SLC-029 has now landed + validated PASS (2026-06-26). The deferral held exactly as planned: no premature mid-wave refactor, and the seam-concentration is now confirmed across the complete backend handler set (par_eff still 0.84, unchanged by SLC-029 because SLC-029 added two MORE handlers to the same file). The deferral condition is satisfied; v71 ACTIONS EXP-086 (→ IMP-011, `planned`→`active`). | **CORRECT — net win** (the deferral bought a complete-handler-set measurement at zero cost; no rework). |
| **LIGHT retro (no new rule on a clean wave) — EXP-085 mechanism** | A clean routine wave with little to learn should NOT pay full-retro overhead or accrete a speculative rule; bank only the one planned experiment. | The clean wave genuinely produced no new defect class — the next learnable event (DEFECT-OAG-032 build-identity pin) was a CI-red drift that arrived AFTER v70, on the SLC-029 push. The light retro correctly did not invent a rule on zero incident signal. | **CORRECT — net win** (no rule-bloat on a clean wave; the v71 incident is handled at v71 where its evidence lives). |

**Net assessment:** the v70 LIGHT retro + EXP-086 deferral were both net wins
across throughput, CFR, frequency, recovery — nothing to revert. The one thing v70
did NOT do — record the SLC-029 dequeue/gate-resolution events as the slice shipped
— was a ledger-currency miss (DEFECT-004 class) reconciled at v71 (see v71). That
miss is a §F1 currency lapse, not a v70-rule defect.

---

(Full v70 header text is preserved at the top of `process-current.md` until v71
supersedes it; this snapshot is the durable record + the anticipated-vs-observed
scoring the §26/step-5 retro requires.)
