---
process_version: 74
effective_from: 2026-07-02
supersedes: v73
status: superseded-by-v75
---

> **[v74 anticipated-vs-observed, added at v75 snapshot 2026-07-02]**
> v74 was a LIGHT retro (scored EXP-087 durable-fix + v73 close-drift + EXP-085
> incident-leg; buffers/N unchanged; constraint named = tester, window lead
> inflated by the once-off UC-O8 migration validation).
>
> Observed (v74→v75 window — CHK-10 SLC-034 prod-promotion build under §F5a
> continuous-deployment re-scope):
> - **§F5a held cleanly on first real use.** SLC-034 was built to CD (no reviewer
>   gate): sandbox→dev→acceptance-dev deploy GREEN, `deploy-prod` job now ACTIVE
>   (if:false removed, environment:prod no-reviewer). The ONLY human touch was the
>   §F5a first-time-infra gate (prod bootstrap) — exactly the one gate §F5a
>   preserves. No spurious human-approval step re-appeared. EXP-091 reads POSITIVE
>   on first evidence.
> - **EXP-090 honest-RED held.** `deploy-prod` fails at the fail-fast validate step
>   (`OAG_PROD_DEPLOY_ROLE_ARN` absent) — not guarded, not softened. The red is the
>   truthful "not done until provisioned" signal, handed back to the human, not a
>   defect.
> - **Same-item parallel-dispatch collision recurred (4th collision overall, 2nd of
>   the hand-maintained-allowlist class).** cicd's new `infra/policies/sst-deploy-prod.json`
>   tripped the engineer-owned `bus-resource-access-policy` pin's hand-maintained
>   `DEPLOY_ROLE_FILES` literal. Recovered in-cycle (engineer allowlisted the file in
>   its own test tree), but it is the SAME class as the UC-R1/AC-R1.9 double-claim:
>   a cross-owner contract carried as a hand-maintained literal that a legitimate new
>   file trips. This is now a **pattern (2 sightings)** but the fix is PROJECT-LOCAL
>   (derive the allowlist from a glob of `infra/policies/sst-deploy-*.json`), routed
>   to open-item OI-BUSPOLICY-ALLOWLIST — NOT a process principle change (§ improvement
>   routing: narrowest owner = the project test + policy files, not process-current).
> - **Constraint UNCHANGED and inherent:** the binding time-thief remains the §F5
>   deploy-gate holds (prod bootstrap UC-C8/C10; UC-EB3/O8). This is gate-bound by
>   design (§F5a: first-time-infra is the only remaining human gate) — not a process
>   defect to elevate. Ready-queue throughput (8/day, dwell ~7176s) and parallelism
>   efficiency (0.90) are healthy. No buffer/N tune warranted on this evidence.

# Current Process — v74

> v74 LIGHT retro (2026-07-02): scored EXP-087 durable-fix (build-identity 4th
> sighting caught by SHA-gate + assert-build-identity guard), v73 close-drift
> (rule 2 positive / rule 1 partial on cross-session offline closes), EXP-085
> incident-leg fired again. SLC-031 + CHK-3 closed. Target CFR; constraint = tester
> (once-off UC-O8 migration validation inflated window lead). Buffers/N unchanged.
> Full v74 process text: git history at the v74 commit.
