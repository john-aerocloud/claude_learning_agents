---
process_version: 75
effective_from: 2026-07-02
supersedes: v74
status: superseded-by-v76
---

> **[v75 anticipated-vs-observed, added at v76 snapshot 2026-07-02]**
> v75 was a LIGHT retro (scored §F5a/EXP-090/EXP-091 machinery on first real use;
> banked ONE narrowly-routed learning — cross-owner test allowlists must DERIVE
> from the owned files, routed to project-local open-item OI-BUSPOLICY-ALLOWLIST,
> NOT a process-current change; buffers/N unchanged; constraint = tester, window
> lead inflated by the once-off UC-O8 migration validation).
>
> Observed (v75→v76 window — CHK-10 SLC-032/033/036 closes + SLC-034 prod-CD
> orchestrator close + CHK-6 FIDS-SPA chunk bubble):
> - **§F5a / EXP-090 / EXP-091 held again through the whole promotion pipeline.**
>   SLC-032 (promotion pipeline), SLC-033 (dev+acceptance-dev stage, UC-C6/C7
>   green ef8176d), SLC-034 (prod CD, UC-C8..C11b, prod-validate-oag 42/42,
>   E-76 ratio 1.0 live traffic, orchestrator item_done 181c8bd) all closed;
>   the ONLY human touch was the §F5a first-time-infra prod-bootstrap gate. No
>   spurious approval step re-appeared. EXP-090 honest-RED held until bootstrap,
>   then went green. SLC-036 (FIDS env-selector) closed clean 3/3; CHK-6 bubbled
>   DONE (6 slices). The full dev→acceptance→prod CD pipeline is live and prod is
>   ingesting.
> - **DEF-OAG-038 (the one incident) resolved defect-as-spec (e32260e)** — 5
>   sub-causes (4 lint, cc6 stale genesis assertion, slc030 CredentialsProviderError,
>   uceb2 dev deploy-role allowlist, prod-smoke over-strict) all fixed genuinely;
>   ci.yml green both jobs. This is the same cross-owner-allowlist class (uceb2
>   sub-cause) that OI-BUSPOLICY-ALLOWLIST addresses — reinforces the v75 routing,
>   does not re-open it as a process principle.
> - **Derived-state currency lapse (a §F1 / DEFECT-004 recurrence, not new).**
>   `state.md` at v76-resume still showed SLC-033 in-flight and many done UCs as
>   in-flight/intake, though the ledger had SLC-033/034 item_done. The ledger was
>   authoritative and correct; the derived queue-membership view lagged. This is
>   the exact class v73 made mechanical (CLOSE-ON-GREEN atomic + RECONCILE-FIRST).
> - **v73 RECONCILE-FIRST gate is UNIMPLEMENTED.** `make ledger-drift` (the v73
>   mandated resume precondition) has no Makefile target — the rule was legislated
>   but never wired. Routed to v76 as a concrete implementation follow-up
>   (OI-LEDGER-DRIFT-TARGET). This is why the state.md drift above went uncaught
>   until manual inspection at resume.
> - **Constraint UNCHANGED and inherent:** the binding time-thief remains the §F5
>   deploy-gate holds (prod bootstrap). Gate-bound by design. Ready throughput and
>   parallelism healthy; no buffer/N tune warranted.

--- v75 active block at snapshot (verbatim head) ---

See process-current.md v75 section (retained inline through v76).
