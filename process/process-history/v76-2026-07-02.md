---
process_version: 76
effective_from: 2026-07-02
supersedes: v75
status: superseded-by-v77
---

> **[v76 anticipated-vs-observed, added at v77 snapshot 2026-07-02]**
> v76 was a LIGHT retro (scored §F5a/EXP-090/EXP-091 held through the whole
> promotion pipeline; EXP-085 4/4; routed two implementation follow-ups —
> OI-LEDGER-DRIFT-TARGET + OI-BUSPOLICY-ALLOWLIST; buffers/N unchanged).
>
> Observed (v76→v77 window — SLC-035 secrets/config migration = the LAST CHK-10
> slice; CHK-10 bubbled DONE):
> - **Triage-verify-vs-rebuild worked exactly as briefed.** Most of SLC-035 was
>   already satisfied by live state — a ground-truth probe confirmed: secrets exist
>   in dev (032614958995) + prod (842827416606); GitHub holds only role ARNs; the
>   deploy-role IAM is prefix-scoped (not `*`). No secret was duplicate-created.
>   The only GENUINE build was DEFECT-OAG-039 (below) + the OI-009/OI-028 decision-
>   record (84094e8) + the secrets-placement runbook. This is the intended "close
>   against evidence, don't rebuild" pattern.
> - **DEFECT-OAG-039 — the anti-false-green discipline caught a REAL running-service
>   break the CI-green never would have.** Secrets existed + IAM was correct, but the
>   dev Fargate ingest consumer crash-looped (exitCode 1) because the container env
>   secret id was bound to the IAM WILDCARD ARN (`-*`), which GetSecretValue rejects
>   (a `-*` is valid only in an IAM Resource, never as a GetSecretValue secretId).
>   The tester caught it at AC-C12.4 live validation (checkpoint=0, no event ever
>   ingested); the engineer fixed it (sst.config.ts:1656 → bare-name `OAG_EVENT_HUB_SECRET_ID`,
>   c4a794a), redeployed dev, and verified genuine-green (task RUNNING, checkpoints
>   advancing 51, 50+ ingest.message on c4a794a). Recovered same-session.
> - **The v76 OI-LEDGER-DRIFT-TARGET follow-up was VINDICATED in the same window.**
>   Exactly the drift v76 predicted recurred: after recording item_done for
>   UC-C12/13/14 the FIRST board sync mapped them to Backlog because state.md had
>   not yet been regenerated; and UC-C1/UC-C2 (SLC-032, both closed on green CI)
>   derived as in-flight because a later out-of-order dequeue-repair row shadowed
>   their item_done in `dora.py project-state`. Both required a manual regenerate +
>   reconcile before the board was honest. This is a 2nd concrete sighting that the
>   v73 close-drift class is not yet mechanically gated — OI-LEDGER-DRIFT-TARGET is
>   the right fix and is now HIGHER priority.
> - **§F5a held:** the dev redeploy was a fix-forward on already-provisioned dev
>   infra, correctly NOT treated as the first-time-infra human gate. No spurious
>   gate.
> - **Constraint:** the engineer briefly (the DEFECT-OAG-039 fix blocked the
>   SLC-035→CHK-10 bubble); otherwise flow-manager (cumulative plumbing). No queue
>   starvation. Buffers/N unchanged.

--- v76 active block retained inline in process-current.md through v77 ---
