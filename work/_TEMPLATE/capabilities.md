# Functional capabilities required for the next iteration

Owned by the CICD agent. Lists only what the *next* slice needs — nothing ahead
of need.

- Environments: <prod-only | + test | + flags>
- Test approach: <unit/acceptance/in-prod validation method>
- Continuous deployment: <pipeline steps, what gates the deploy>
- Rollback assets to maintain: <migrations, scripts>
