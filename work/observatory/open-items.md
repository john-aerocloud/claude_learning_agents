
## From s005 (2026-06-10)
- **OI-EPHEMERAL-BROWSER-TARGET (capability, cicd):** `make browser-observatory` hardcodes :5173, colliding with the operator's running server. Engineers had to use `OBSERVATORY_E2E_PORT=<n> npm --prefix … run test:browser`. Add a `browser-observatory-ephemeral` make target (non-default port) as the default for engineer/CI runs so verification never contends with the operator's :5173. Surfaced by DEFECT-006 + UC-S004-5/6 work.
- **OI-SERVER-RESTART (CLOSED 2026-06-10):** `viteApiPlugin.js` now watches `server/` via `server.watcher.add(SERVER_DIR)` and calls `server.restart()` (debounced 300ms) on any `.js` change under `server/` (excluding `__tests__/`). Verified on ephemeral :5208: `apiMiddleware.js` edit auto-restarted in under 1s with no manual intervention; client `App.jsx` edit stayed HMR-only (no full restart). 476 unit tests green. Commit: see trunk.
- **OI-FLAKY-WATCHER (CLOSED 2026-06-10):** `watcher.test.js` 1/5 chokidar timing flake — de-flaked alongside the DEFECT-002 fix (`3b735ce`): settle the pre-existing write before watcher start + event-driven assertion via positive-control write; 8/8 stable. Trunk fully green.

## From s002-pipeline-map (2026-06-09)
- **OI-S002-1 (residual, carry):** AC6.5 SSE reconnect (read-layer drop→restart→EventSource reconnect→next change updates) not exercised end-to-end — code path present + `reconnecting` state unit-tested, but no committed browser spec drives a server restart. Needs a spec task (next slice). Source: tester E2E.
- **OI-S002-2 (product/ui):** the live `baseline.md` constraint is an AGENT ("tester"), not a queue — so no queue box highlights on the real repo (correct fail-soft). Follow-up: surface a non-queue constraint name in a labelled constraint CHIP (out of CHK-2 scope; candidate for CHK-3 DORA panel). Source: UC-S002-5.
- **OI-S002-3 (flow observation, no action yet):** s002 ran largely SERIAL (UC1→…→UC6) because UC3/UC4/UC5/UC6 all attach to one render seam (PipelineMap.jsx/MapContainer.jsx), unlike s001's disjoint route files. UI slices are inherently more serial. Single data point — watch CHK-3/CHK-4 before any process change.

## RE-VISION (2026-06-09, human-directed) — value-stream map
- **DEFECT (confirmed):** the pipeline map shows nothing useful — queue CSVs are header-only/transient and the inner-dev-loop stages (where work actually sits) are not drawn. "Pulled off the queue → nothing indicates it."
- **VISION CHANGE:** the CORE view should be the WHOLE process value-stream — every stage labelled (Intake → Ready → Capabilities → UI-Design → Build/TDD → Deploy → Validate → Done, + Rework loop, + gates), each showing throughput + dwell + in-flight WIP + rework, sourced from the ledger stage events. Supersedes the thin 4-box CHK-2 map.
- **Read-layer gap:** no endpoint aggregates the ledger into per-stage flow; CHK-1 serves raw files only. A ledger→per-stage-flow endpoint is needed.
- Action: product to re-vision (re-intake); likely re-scope CHK-2/CHK-3 into a stage value-stream map.
- **PROCESS GAP (meta):** s002 passed 222 tests but the real UI was useless — validation ran against FIXTURE repos, not live repo state, so green masked an empty/meaningless operator view. Tester validation + slice done-condition must include "operator-facing view shows REAL, non-empty, meaningful data from the actual repo." Candidate principle-failure + experiment at next retro.
- **OI-S004 (read-layer):** shared `parsers/csv.js` is STRICT (columns:true) and silently drops ledger lines whose `note` has unescaped commas/quotes (~64 lines → engineer throughput read as 4 not 7). UC-S004-1's aggregator ships its own tolerant line parser (correct). Flag: any future ledger-reading endpoint must use the tolerant parser, or harden csv.js. Source: UC-S004-1.
