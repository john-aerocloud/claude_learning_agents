{
  "name": "observatory",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Delivery Observatory — local read-only read layer over the agent-pipeline repo (CHK-1).",
  "scripts": {
    "server": "node src/server/server.js",
    "test": "vitest",
    "test:ci": "vitest run",
    "start:check": "node scripts/start-check.js"
  },
  "dependencies": {
    "chokidar": "^3.6.0",
    "csv-parse": "^5.5.6",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "supertest": "^7.0.0",
    "vitest": "^2.1.1"
  }
}
