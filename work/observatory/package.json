{
  "name": "observatory",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Delivery Observatory — local read-only read layer + Vite SPA, single server on :3001.",
  "scripts": {
    "dev": "node src/server/dev.js",
    "start": "node src/server/dev.js",
    "server": "node src/server/server.js",
    "test": "vitest",
    "test:ci": "vitest run",
    "start:check": "node scripts/start-check.js"
  },
  "dependencies": {
    "chokidar": "^3.6.0",
    "csv-parse": "^5.5.6",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.1",
    "supertest": "^7.0.0",
    "vite": "^5.4.8",
    "vitest": "^2.1.1"
  }
}
