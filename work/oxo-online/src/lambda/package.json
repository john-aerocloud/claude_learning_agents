{
  "name": "oxo-online-lambda",
  "version": "0.1.0",
  "private": true,
  "description": "oxo-online game backend Lambda handlers (slice 004 — create-game).",
  "scripts": {
    "build": "tsc && for f in tsconfig.*.json; do tsc --project \"$f\"; done",
    "test": "vitest run"
  },
  "dependencies": {
    "@aws-sdk/client-apigatewaymanagementapi": "^3.1063.0",
    "@aws-sdk/client-dynamodb": "^3.654.0",
    "@aws-sdk/client-ssm": "^3.1063.0",
    "@aws-sdk/lib-dynamodb": "^3.654.0",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^22.5.0",
    "@types/uuid": "^10.0.0",
    "aws-sdk-client-mock": "^4.0.1",
    "typescript": "^5.5.4",
    "vitest": "^2.1.1"
  }
}
